import { IconAdjustments, IconRecycle, IconWorldDownload } from '@tabler/icons-react'
import ImageCallToPreview from 'shared/components/ImageCallToPreview'
import Features, { Feature } from 'shared/components/Features'
import { l } from 'to-line'

Master CSS [Runtime Rendering](/docs/rendering-modes/runtime-rendering) observes **changes in DOM class names** at browser runtime, generates corresponding CSS rules, and injects them into the **running style sheet**.
<div className="flex flex:wrap@<sm gap:50 my:50">
    <Features className={l(props.img ? 'w:35%@sm' : 'grid-cols:3@sm')}>
        <Feature>
            <IconWorldDownload className="app-icon-primary" />
            <div>
                **Fixed style cost**
                
                All features work out of the box with ~16KB transfer cost
            </div>
        </Feature>
        <Feature>
            <IconAdjustments className="app-icon-primary" />
            <div>
                **Fully automatic**
                
                Capture any program-generated class names
            </div>
        </Feature>
        <Feature>
            <IconRecycle className="app-icon-primary" />
            <div>
                **CSS lifecycle**
                
                CSS is generated on demand and frees memory when not in use
            </div>
        </Feature>
    </Features>
    {props.img && <div className="flex:1|1|65%@sm">
        <ImageCallToPreview {...props.img} imgClass="outline:1|solid|divider outline-offset:-1 r:6" />
    </div>}
</div>