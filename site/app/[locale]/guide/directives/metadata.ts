import define from 'internal/utils/metadata'

const metadata = define({
  title: 'Directives',
  description: 'Use Master CSS stylesheet directives for entries, settings, theme tokens, managed definitions, source scanning, composition, and native CSS preservation.',
  category: 'Syntax Tutorial',
  fileURL: import.meta.url,
})

export default metadata
