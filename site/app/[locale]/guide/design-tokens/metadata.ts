import define from 'internal/utils/metadata'

const metadata = define({
    title: 'Design Tokens',
    description: 'Use a token-first approach for colors, spacing, typography, radius, elevation, motion, and responsive design.',
    category: 'Fundations',
    order: 1,
    fileURL: import.meta.url
})

export default metadata
