import define from '~/site/docs-shell/utils/metadata'

const metadata = define({
  title: 'Route-level Styles',
  description: 'Write route, page, and component-specific CSS with @reference, native selectors, @compose, variants, container queries, and native keyframes.',
  category: 'Authoring',
  order: 3,
  fileURL: import.meta.url
})

export default metadata
