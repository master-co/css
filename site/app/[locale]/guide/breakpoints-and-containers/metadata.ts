import define from 'internal/utils/metadata'

const metadata = define({
    title: 'Breakpoints and Containers',
    description: 'Define viewport breakpoint tokens and container size tokens for responsive layout, container queries, and width caps.',
    category: 'Design Foundations',
    fileURL: import.meta.url
})

export default metadata
