export const code1 = `
    <section class="home-section">…</section>
`

export const code2 = `
    .home-section {
        background-color: blue;
        padding: 2rem;
        text-align: center;
    }

    .home-section:hover {
        background-color: red;
    }

    @media (min-width: 1024px) {
        .home-section {
            padding: 3rem;
        }
    }
`

export const code3 = `
    <section class="bg:blue bg:red:hover p:xl p:2xl@md text:center">…</section>
`

Consider a common styling task:

"Create a section with a blue background, a red hover state, centered text, and larger padding when the viewport is at least `1024px`."

(!) In a traditional CSS workflow, the first step is often naming the thing before the style has proved reusable.
<Code name="home.html" lang="html">
    {code1}
</Code>

(!) Then you create a stylesheet rule, selector state, and media query for that name.
<Code name="home.css" lang="css">
    {code2}
</Code>

This works, but it creates an abstraction before the design decision has earned one.

(o) With Master CSS, the local decision can stay in markup until it becomes shared vocabulary. <Dropped p1={code1 + code2} p2={code3} target="class" />
<Code name="home.html" lang="html">
    {code3}
</Code>
<details>
    <summary>Generated CSS</summary>
    <Class2CSS>
        {'bg:blue bg:red:hover p:xl p:2xl@md text:center'}
    </Class2CSS>
</details>
