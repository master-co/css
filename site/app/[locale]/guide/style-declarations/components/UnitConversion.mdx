export const code1 = `
    <!-- @MARK 3rem -->
    <p class="font:3rem">
`
export const code2 = `
    <!-- @MARK font:12x -->
    <p class="font:12x">
`

Use the `x` unit when a value should follow the project base-unit rhythm and emit `rem`.
<Code lang="html">
    {code1}
</Code>

(o) Use multiplier units instead of relying on implicit numeric conversion. <Dropped p1={code1} p2={code2} target="class" />
<Code lang="html">
    {code2}
</Code>

With the default `base-unit: 4`, `12x` becomes `3rem`.

Bare numbers are emitted as written. For example, `font:48` emits `font-size: 48`, not `3rem`.

Use explicit units when you need to preserve a value exactly as written.
