export const code1 = /* eslint-disable @master/css/prefer-canonical-classes, @master/css/sort-classes -- intentional shorthand comparison source */ `
    <!-- @MARK text-align:center --><!-- @MARK font-size:16px --><!-- @MARK font-weight:bold --><!-- @MARK border-style:solid -->
    <p class="text-align:center font-size:16px font-weight:bold border-style:solid">
`
export const code2 = `
    <!-- @MARK text-center --><!-- @MARK font:16px --><!-- @MARK font:bold --><!-- @MARK b-solid -->
    <p class="text-center font:16px font:bold b-solid">
`
/* eslint-enable @master/css/prefer-canonical-classes, @master/css/sort-classes */

Syntax: <code><span className="language-mcss mcss token value"><span className="mcss token"><span className="Mkey token">property-name</span><span className="Mvalueroot token"><span className="Mcolon token">:</span><span className="Mvalue token"><span className="Mnumber_unit token"><span className="Mnumber token">value</span></span></span></span></span></span></code> -> <code><span className="language-mcss mcss token value"><span className="mcss token"><span className="Mkey token">property</span><span className="Mvalueroot token"><span className="Mcolon token">:</span><span className="Mvalue token"><span className="Mnumber_unit token"><span className="Mnumber token">value</span></span></span></span></span></span></code>

Native CSS shorthands usually combine several longhand properties. Master CSS also has shorter names for single declarations when the property and value together make the target property clear.
<Code lang="html">
    {code1}
</Code>

(o) Use context-aware shorthands and semantic static utilities. <Dropped p1={code1} p2={code2} target="class" />
<Code lang="html">
    {code2}
</Code>

From the example above:

- `mcss text-center` is the semantic utility for `mcss text-align:center`.
- `mcss font:16px` resolves to `mcss font-size:16px` because numeric font values are treated as font sizes.
- `mcss font:bold` resolves to `mcss font-weight:bold` because `bold` is a font weight value.
- `mcss b-solid` is the semantic utility for `mcss border-style:solid`.

This keeps classes short while still generating explicit CSS properties. It also avoids accidental resets from native shorthand behavior, where omitted subproperties can be reset to their initial values.

For example, `font:16px` is not translated to the native `font` shorthand. It generates `font-size`, which is usually what the class author intended.
