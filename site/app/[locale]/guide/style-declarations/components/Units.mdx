Syntax: <code><span className="token value mcss language-mcss"><span className="token mcss"><span className="token Mkey">property</span><span className="token Mvalueroot"><span className="token Mcolon">:</span></span><span className="token Mvalueroot"><span className="token Mvalue"><span className="token Mnumber_unit"><span className="token Mnumber">value</span><span className="token Munit">unit</span></span></span></span></span></span></code>

Use native [CSS units](https://developer.mozilla.org/en-US/docs/Learn/CSS/Building_blocks/Values_and_units) whenever the value should stay explicit: `px`, `em`, `%`, `ex`, `ch`, `rem`, `vw`, `vh`, `vmin`, `vmax`, `cm`, `mm`, `in`, `pt`, `pc`, `ms`, `s`, `deg`, and more.
```html
<!-- @MARK w:50% --><!-- @MARK font:10vw -->
<div class="w:50% font:10vw">
```

If you omit the unit, Master CSS uses property-specific conversion and sensing rules. Use explicit units when that behavior would make the class less obvious.
