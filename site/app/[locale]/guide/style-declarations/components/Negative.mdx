Place a minus sign before a direct value, or before the complete named token class:

```html
<div class="mt:-2rem -ml-sm"></div>
```

Negative token syntax requires a numeric token and a property that accepts negative values. For example, `-m-sm` is supported, while `-p-sm` is rejected because padding cannot be negative. Direct values retain native CSS validity rules.
