export const code1 = `
    <ul class="block>li:hover@md font:14>li:hover@md text-center>li:hover@md">…</ul>
`
export const code2 = `
    <!-- @MARK >li:hover@md -->
    <ul class="{block;font:14;text-center}>li:hover@md">…</ul>
`

When several declarations share the same selector or query suffix, group the declarations first and write the suffix once.
<Code lang="html">
    {code1}
</Code>

(o) Group styles with the same states. <Dropped p1={code1} p2={code2} target="class" />
<Code lang="html">
    {code2}
</Code>

Inside `{...}`, separate declarations with semicolons. After the closing `}`, append selectors and conditional queries exactly as you would on a single declaration.
