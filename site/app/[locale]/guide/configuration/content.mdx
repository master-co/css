import RulesOverview from '~/site/app/[locale]/guide/syntax-rules/components/Overview.mdx'
import VariablesOverview from '~/site/app/[locale]/guide/variables/components/Overview.mdx'
import UtilitiesOverview from '~/site/app/[locale]/guide/utilities/components/Overview.mdx'
import AtRulesOverview from '~/site/app/[locale]/guide/at/components/Overview.mdx'
import SelectorsOverview from '~/site/app/[locale]/guide/selectors/components/Overview.mdx'
import FunctionsOverview from '~/site/app/[locale]/guide/functions/components/Overview.mdx'
import ComponentsOverview from '~/site/app/[locale]/guide/reusing-styles/components/Overview.mdx'
import ScreensOverview from '~/site/app/[locale]/guide/screens/components/Overview.mdx'
import { config } from '@master/css'

## Setup [sr-only]
```bash action=create
@master/css@rc
```
If you don't have a configuration file, [create](/reference/create-app) one first.

---

## Options
### `.animations`
Customizing animation [animations](/guide/configuration#animations) for your design system.
<AnimationsOverview />

### `.functions`
Customizing [functions](/guide/functions) for your design system.
<FunctionsOverview />

### `.at`
Customizing [at-rules](/guide/at) for your design system.
<AtRulesOverview />

### `.rules`
Customizing [rules](/guide/syntax-rules) for your design system.
<RulesOverview />

### `.selectors`
Customizing [selectors](/guide/selectors) for your design system.
<SelectorsOverview />

### `.utilities`
Customizing [utility](/guide/utilities) classes for your design system.
<UtilitiesOverview />

### `.components`
Creating [reusing styles](/guide/reusing-styles) for your design system.
<ComponentsOverview />

### `.variables`
Customizing [variables](/guide/variables) for your design tokens.
<VariablesOverview />

### `.screens`
Customizing [screen sizes](/guide/screens) for your design tokens.
<ScreensOverview />

### `.extends`
Extend custom or external configuration.
<DocProp types={["any[]"]} />
```js name=master.css.js
export default {
    extends: [
        require('@master/ui'),// [!code ++]
        require('./styles/btn.css'),// [!code ++]
        …
    ]
}
```

### `.important`
Make all generated CSS declarations `!important`.

<DocProp defaultValue="false" types={['boolean']} />

(!|) Using `js important: true` should be considered as a last option, as it's a compromise.
```js name=master.css.js
export default {
    important: true// [!code ++]
}
```
Generated CSS:
```css
.hidden {
    /* @MARK !important */
    display: none !important;
}

.full {
    /* @MARK !important */
    width: 100% !important;
    /* @MARK !important */
    height: 100% !important;
}
```

### `.rootSize`
Specify the conversion factor for `rem` and `em`.

<DocProp defaultValue="16" types={['number']} />

Here's a common use case with `rootSize: 10`:
```js name=master.css.js
export default {
    rootSize: 10// [!code ++]
}
```

Generated CSS rules:
```css
.font\:16 {
    font-size: 1rem;   /* rootSize: 16 */ /* [!code --] */
    font-size: 1.6rem; /* rootSize: 10 */ /* [!code ++] */
}
```

And you will set the font size of the root to `62.5%`:
```html
<html class="font:62.5%">
```

### `.baseUnit`
This base unit determines the size scale and ensures visual consistency across products.
<DocProp defaultValue="4" types={['number']} />
For example, with the default `baseUnit: 4`, the size scale `1x, 2x, …` will be `4, 8, …`.
```html
<div class="m:4x"></div>
```
Generated CSS:
```css
.m\:4x {
    margin: 1rem; /* 4x = 4*4 = 16px, 16px / 16 = 1rem */
}
```

### `.scope`
Limit the generated CSS rules to a specific scope with CSS selectors.
<DocProp defaultValue="''" types={['string']} />

(!|) Don't make it part of your coding style, but as a last resort to solve problems.
```js name=master.css.js
export default {
    scope: '#app'/* [!code ++] */
}
```
All Master CSS syntax will only be applied if the `<body id="app">`.
```html
<html>
<!-- @MARK app -->
<body id="app">
    <!-- @MARK mt:1 text:center -->
    <div class="mt:1 text:center"></div>
</body>
</html>
```
Generated CSS:
```css
/* @MARK #app */
#app .mt\:1 {
    margin-top: 0.0625rem
}

/* @MARK #app */
#app .text\:center {
    text-align: center
}
```

### `.defaultMode`
The mode is to be applied by default when no mode is specified.
<DocProp defaultValue="'light'" types={["'light'", "'dark'", 'string', 'boolean']} />
See [default mode](/guide/variables-and-modes#default-mode) for more information.

### `.modeTrigger`
Sets how the theme should drive and generate CSS rules.
<DocProp defaultValue={JSON.stringify(config.modeTrigger)} types={["'class' | 'media' | 'host'"]} />
See [mode triggers](/guide/variables-and-modes#mode-triggers) for more information.