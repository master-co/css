import define from 'internal/utils/metadata'

const metadata = define({
  title: 'Migrating from CSS-in-JS',
  description: 'Move styled-components and Emotion styles to Master CSS by converting static styles, preserving dynamic values, and keeping reusable component contracts explicit.',
  category: 'Migration',
  fileURL: import.meta.url
})

export default metadata
