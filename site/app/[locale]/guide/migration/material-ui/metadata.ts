import define from 'internal/utils/metadata'

const metadata = define({
  title: 'Migrating from Material UI',
  description: 'Move Material UI projects toward Master CSS by migrating app-owned layout, sx styles, theme tokens, and overrides without breaking component behavior.',
  category: 'Migration',
  fileURL: import.meta.url
})

export default metadata
