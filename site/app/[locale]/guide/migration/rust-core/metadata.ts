import define from 'internal/utils/metadata'

const metadata = define({
  title: 'Migrating to Rust-backed sessions',
  description: 'Migrate tooling integrations from the removed TypeScript object model to native and Wasm Master CSS sessions.',
  category: 'Migration',
  fileURL: import.meta.url
})

export default metadata
