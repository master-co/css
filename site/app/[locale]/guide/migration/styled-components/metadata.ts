import define from 'internal/utils/metadata'

const metadata = define({
    title: 'Migrating from Styled Components',
    description: 'Use the migration quick start to replace styled-components code only where Master CSS makes the result clearer.',
    category: 'Migration',
    unfinished: true,
    fileURL: import.meta.url
})

export default metadata
