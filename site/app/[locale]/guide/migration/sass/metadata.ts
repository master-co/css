import define from 'internal/utils/metadata'

const metadata = define({
    title: 'Migrating from Sass',
    description: 'Move Sass variables, maps, mixins, placeholders, and nested component styles toward Master CSS without losing useful CSS structure.',
    category: 'Migration',
    fileURL: import.meta.url
})

export default metadata
