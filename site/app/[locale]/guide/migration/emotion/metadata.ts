import define from 'internal/utils/metadata'

const metadata = define({
    title: 'Migrating from Emotion',
    description: 'Use the migration quick start to replace Emotion styles only where Master CSS improves the codebase.',
    category: 'Migration',
    unfinished: true,
    fileURL: import.meta.url
})

export default metadata
