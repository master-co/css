import brands from 'internal/data/brands'

Migration guides for common source styling systems.

<IconButtons url="/guide/migration" className="mt:2xl">
    {[
        { ...brands['tailwindcss'], disabled: false },
        { ...brands['css'], disabled: false, path: 'css' },
        { ...brands['v1'], disabled: false, name: 'Master CSS v1' },
        { ...brands['styled-components'], disabled: false },
        { ...brands['bootstrap'], disabled: false, path: 'bootstrap' },
        { ...brands['emotion'], disabled: false },
        { ...brands['sass'], disabled: false },
    ]}
</IconButtons>

---

Each source-specific guide is intentionally short while the detailed migration notes are rebuilt. Start with the [quick start](/guide/migration), then use the source page to focus the first inspection pass.
