import define from 'internal/utils/metadata'

const metadata = define({
    title: 'Migrating from Bootstrap',
    description: 'Use the migration quick start to move Bootstrap-styled screens toward Master CSS in small, reviewable batches.',
    category: 'Migration',
    unfinished: true,
    fileURL: import.meta.url
})

export default metadata
