import define from 'internal/utils/metadata'

const metadata = define({
  title: 'Migrating from Bootstrap',
  description: 'Move Bootstrap-styled screens toward Master CSS while preserving vendor CSS, JavaScript-driven components, and cascade-dependent overrides.',
  category: 'Migration',
  fileURL: import.meta.url
})

export default metadata
