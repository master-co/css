## Overview [sr-only]
<Default />
In native CSS, it's not possible to reuse queries through CSS variables. Master CSS uses `atTokens` to name *media queries*, *feature queries*, *container queries*, and other `@` suffix expressions so they can be applied directly in markup.
<Overview />

---

## Basic usage
### Add a media query token
Create a media query token, such as `@landscape`, to represent landscape orientation.
<Code lang="js" name="master.css.js">{require('./tests/add-a-media-query/master.css.js?raw')}</Code>
When displayed in landscape orientation, the image is presented in a 2:1 aspect ratio.
<Code lang="html">{require('./tests/add-a-media-query/template.html?raw')}</Code>
<details>
    <summary>Generated CSS</summary>
    <div>
        <Code lang="css" beautify>{require('./tests/add-a-media-query/generated.css?raw')}</Code>
    </div>
</details>

### Add a feature query token
Create a feature query token, such as `@supports-backdrop`, to apply styles based on `backdrop-filter` support.
<Code lang="js" name="master.css.js">{require('./tests/add-a-feature-query/master.css.js?raw')}</Code>
Applies translucent background only when `backdrop-filter` is supported.
<Code lang="html">{require('./tests/add-a-feature-query/template.html?raw')}</Code>
<details>
    <summary>Generated CSS</summary>
    <div>
        <Code lang="css" beautify>{require('./tests/add-a-feature-query/generated.css?raw')}</Code>
    </div>
</details>

### Add a feature name token
Create a feature name token to shorten the syntax of a feature query.
```js name=master.css.js
export default {
    atTokens: {
        h: 'height' // [!code highlight]
    }
}
```
Use the token `h`:
```html
<!-- @MARK h -->
<div class="hidden@container(h<=200)">…</div>
```
<details>
    <summary>Generated CSS</summary>
    <Class2CSS>
        {['hidden@container(h<=200)']}
    </Class2CSS>
</details>
