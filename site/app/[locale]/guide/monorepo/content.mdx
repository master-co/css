## Foreword [sr-only]
The [Language Server](/reference/language-server) and [ESLint](/reference/eslint) both support the monorepo.

By default, language server will form independent workspaces based on the location of the __master.css__ configuration files or `package.json` files that declare Master CSS package dependencies, independent of the package manager's workspace. The same goes for ESLint, which forms a new working directory based on the location of its configuration file.

Officially integrated packages or extensions will automatically read the __master.css__ configuration based on the current running directory. This means that you can start using it with zero configuration as long as you create a configuration file in the workspace, or declare a Master CSS dependency in that workspace's `package.json` when using the default configuration.

---

## One project, one workspace
Inside one `package.json` project, Master CSS supports one workspace instance. That workspace has one configuration graph, one extraction context, and one set of tool integrations for the current project boundary.

Master CSS does not split a single package project into separate utility workspaces from multiple extraction outputs. For example, do not model one package as independent `admin.css` and `shop.css` extraction areas and expect the language service, ESLint, and build integrations to treat them as separate Master CSS workspaces.

When different applications need cleanly separated CSS extraction, configuration, linting, and editor behavior, create separate projects in the monorepo. A typical structure is `projects/admin` and `projects/shop`, with each project owning its own `package.json`, `master.css`, and integration config.

(i) This creates separate project boundaries for `projects/admin` and `projects/shop`.
<ExplorerView>
{[
    { name: 'projects', children: [
        { name: 'admin', children: [
            { name: 'package.json' },
            { name: 'eslint.config.js' },
            { name: 'master.css' }
        ]},
        { name: 'shop', children: [
            { name: 'package.json' },
            { name: 'eslint.config.js' },
            { name: 'master.css' }
        ]}
    ]},
    { name: 'package.json' },
    { name: 'master.css' }
]}
</ExplorerView>

This keeps each application's generated CSS, source scanning, diagnostics, and editor state aligned with the same project root. Shared design tokens can still live at the repository root and be imported by each project workspace.

### Share the configuration
In each workspace, import the shared CSS config files and add workspace-specific definitions after them.
```css name=projects/admin/master.css
@import '../../master.css';

@master {
    --color-primary: #4f46e5;

    .app-shell {
        @compose "min-h:100vh bg:base";
    }
}
```
Later definitions in the workspace file can override root options, variables, tokens, general utilities, and matching main style buckets.

(x) Keep multiple application extraction targets inside one package project.
<ExplorerView>
{[
    { name: 'app', children: [
        { name: 'admin.css' },
        { name: 'shop.css' },
        { name: 'globals.css' }
    ]},
    { name: 'package.json' },
    { name: 'master.css' }
]}
</ExplorerView>

(o) Give each application its own package project and Master CSS workspace.
<ExplorerView>
{[
    { name: 'projects', children: [
        { name: 'admin', children: [
            { name: 'package.json' },
            { name: 'master.css' }
        ]},
        { name: 'shop', children: [
            { name: 'package.json' },
            { name: 'master.css' }
        ]}
    ]},
    { name: 'master.css' }
]}
</ExplorerView>

Finally, using a monorepo and having multiple workspaces doesn't mean you have to create multiple configurations. In most cases, you just need [one workspace entry](#one-workspace-entry).
