import RuntimeRenderingIntro from '../../rendering-modes/components/RuntimeRenderingIntro.mdx'

<RuntimeRenderingIntro img={{
    ...require('~/site/public/images/vuejs-dev.png').default,
    dark: require('~/site/public/images/vuejs-dev@dark.png').default,
    alt: 'Vue.js and Master CSS',
    width: 665, height: 400 }} />

---

A full-stack framework, such as [Nuxt.js](/guide/installation/nuxtjs), is recommended for better application performance and experience.

---

## Quick start
<StepSection>
    <Step $row>
        <StepL>
            ### <StepNum />Clone the example
            Copy-paste the commands to quickly start using the https://vuejs.rr.rc.css.master.co example.

            You can skip all [installation](#installation) steps.
        </StepL>
        <StepR>
            ```bash name=Terminal
            npm create @master/css@rc <project> --example vue.js
            cd <project>
            npm run dev
            ```
        </StepR>
        <StepEnd/>
    </Step>
</StepSection>

---

## Installation
<StepSection>
    <Step $row>
        <StepL>
            ### <StepNum />Create a project
            If you don't have a Vue.js project, create one first. It's recommended to refer to [Quick Start - Vue.js](https://vuejs.org/guide/quick-start.html).
        </StepL>
        <StepR>
            ```bash name=Terminal
            npm init vue@latest
            cd <project>
            ```
        </StepR>
    </Step>
    <Step $row>
        <StepL>
            ### <StepNum />Initialize configuration file
            Run the command to create a configuration file [master.css.ts](/guide/configuration).
        </StepL>
        <StepR>
            ```bash action=create
            @master/css@rc
            ```
        </StepR>
    </Step>
    <Step $row>
        <StepL>
            ### <StepNum />Install Master CSS
            Install Master CSS Vue into your project via package managers.
        </StepL>
        <StepR>
            ```bash action=install
            @master/css.vue@rc
            ```
        </StepR>
    </Step>
    <Step $row>
        <StepL>
            ### <StepNum />Set up CSS runtime engine
            - Import your custom configuration `master.css.ts`
            - Initialize the runtime engine using `CSSRuntimeProvider`
        </StepL>
        <StepR>
            ```html name=src/App.vue
            <script setup lang="ts">
            import CSSRuntimeProvider from '@master/css.vue'
            import config from '../master.css'
            </script>

            <template>
                <CSSRuntimeProvider :config="config">
                    …
                </CSSRuntimeProvider>
            </template>
            ```
        </StepR>
    </Step>
    <Step $row>
        <StepL>
            ### <StepNum />Prevent flash of unstyled content
            Add `html <html hidden>` to avoid [FOUC](https://en.wikipedia.org/wiki/Flash_of_unstyled_content) caused by the runtime engine not yet injecting CSS rules.
        </StepL>
        <StepR>
            ```html name=index.html
            <!doctype html>
            <!-- @MARK hidden -->
            <html lang="en" hidden>
            ```
        </StepR>
    </Step>
    <Step $row>
        <StepL>
            ### <StepNum />Launch server
            Run `npm run dev` to start your Vite development server
        </StepL>
        <StepR>
            ```bash name=Terminal
            npm run dev
            ```
        </StepR>
    </Step>
    <Step>
        ### <StepNum />Start using Master CSS
        Now style your first element using Master CSS syntax!
        ```html name=src/App.vue
        …
        <template>
            <CSSRuntimeProvider :config="config">
                <h1 class="font:40 font:heavy italic m:12x text:center">Hello World</h1>
            </CSSRuntimeProvider>
        </template>
        ```
        Open your browser to watch the changes.
        <HelloWorld url="localhost:3000" />
    </Step>
</StepSection>