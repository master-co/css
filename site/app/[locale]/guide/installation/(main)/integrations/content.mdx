import brands from 'internal/data/brands'

Installation guides for popular frameworks and build tools integrated with Master CSS.
<IconButtons url="/guide/installation" className="mt:12x">
    {[
        brands['nextjs'],
        brands['svelte'],
        brands['nuxtjs'],
        brands['laravel'],
        brands['astro'],
        brands['blazor'],
        brands['react'],
        brands['vuejs'],
        brands['angular'],
        brands['vite'],
        brands['webpack'],
    ]}
</IconButtons>

---

Don't have a preferred framework or tool yet? Try the [online playground](/play) or C&P [CDN](/guide/installation/cdn), or set up the [runtime](/reference/runtime) from scratch is by initializing it in your app’s entry point.