import brands from 'internal/data/brands'

Installation guides for popular frameworks and build tools integrated with Master CSS.
<IconButtons url="/guide/installation" className="my:xl grid-cols:6@sm grid-cols:8@lg">
    {[
        brands['nextjs'],
        brands['svelte'],
        brands['nuxtjs'],
        brands['laravel'],
        brands['astro'],
        brands['blazor'],
        brands['react'],
        brands['lit'],
        brands['angular'],
        brands['vite'],
        brands['webpack'],
        brands['vscode'],
    ]}
</IconButtons>

---

Don't have a preferred framework or tool yet? Try the [online playground](/play), use the [quick start](/guide/installation), or set up the [`@master/css-runtime`](https://www.npmjs.com/package/@master/css-runtime) package from scratch by initializing it in your app’s entry point.
