<StepSection>
    <Step $row>
        <StepL>
            ### <StepNum />Create a project
            If you don't have a Nuxt project, create one first.
        </StepL>
        <StepR>
            ```bash name=Terminal
            npx nuxi@latest init my-app
            cd my-app
            ```
        </StepR>
    </Step>
    <Step $row>
        <StepL>
            ### <StepNum />Add Master CSS
            Run the installer from the Nuxt project root.
        </StepL>
        <StepR>
            ```bash name=Terminal
            npm create @master/css@rc -- --framework nuxt --yes
            ```
        </StepR>
    </Step>
    <Step $row>
        <StepL>
            ### <StepNum />Launch server
            Run the development server.
        </StepL>
        <StepR>
            ```bash name=Terminal&action=run
            dev
            ```
        </StepR>
    </Step>
    <Step>
        ### <StepNum />Start using Master CSS
        Now style your first component using Master CSS syntax.
    </Step>
    <HelloWorld url="localhost:3000" />
</StepSection>

```vue name=app.vue
<template>
    <h1 class="m:2xl italic font:5xl font:heavy text:neutral">
        Hello World
    </h1>
</template>
```
