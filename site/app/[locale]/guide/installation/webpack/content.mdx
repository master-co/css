<StepSection>
    <Step $row>
        <StepL>
            ### <StepNum />Create a project
            If you don't have a Webpack project, create one first.
        </StepL>
        <StepR>
            ```bash name=Terminal
            mkdir my-app
            cd my-app
            npm init -y
            npm install -D webpack webpack-cli webpack-dev-server
            ```
        </StepR>
    </Step>
    <Step $row>
        <StepL>
            ### <StepNum />Add Master CSS
            Run the installer from the Webpack project root.
        </StepL>
        <StepR>
            ```bash name=Terminal
            npm create @master/css@rc -- --framework webpack --yes
            ```
        </StepR>
    </Step>
    <Step $row>
        <StepL>
            ### <StepNum />Launch server
            Run your Webpack development command.
        </StepL>
        <StepR>
            ```bash name=Terminal
            npm run dev
            ```
        </StepR>
    </Step>
    <Step>
        ### <StepNum />Hello world
        Now style your first element using Master CSS syntax.
    </Step>
    <HelloWorld />
</StepSection>

```html
<h1 class="m:2xl italic font:5xl font:heavy text:neutral">
    Hello World
</h1>
```
