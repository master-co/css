import define from 'internal/utils/metadata'

const metadata = define({
    title: 'Static Extraction in Webpack',
    description: 'Guide to setting up Master CSS static extraction in your Webpack project.',
    category: 'Integrations',
    vercelOG: true,
    fileURL: import.meta.url
})

export default metadata