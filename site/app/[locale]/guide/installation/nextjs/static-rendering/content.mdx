<StepSection>
    <Step $row>
        <StepL>
            ### <StepNum />Create a Next.js project
            If you don't have a Next.js project, create one first. It's recommended to refer to [Create Next App](https://nextjs.org/docs/app/api-reference/cli/create-next-app).
        </StepL>
        <StepR>
            ```bash name=Terminal
            npx create-next-app@latest my-app --ts --app --no-tailwind
            cd my-app
            ```
        </StepR>
    </Step>
    <Step $row>
        <StepL>
            ### <StepNum />Install Master CSS
            Install the Master CSS Next.js integration into your project via package managers.
        </StepL>
        <StepR>
            ```bash action=install
            @master/css.next@rc
            ```
        </StepR>
    </Step>
    <Step $row>
        <StepL>
            ### <StepNum />Set up static mode
            Register the Master CSS Next.js integration in `static` mode. This creates the generated stylesheet and wires Master CSS stylesheet imports into Turbopack CSS processing.

            (i) Static mode scans source files as the correctness baseline. During Turbopack builds, a scanner loader also feeds visited JavaScript and TypeScript modules into the scanner for incremental updates.
        </StepL>
        <StepR>
            ```js name=next.config.mjs
            import { withMasterCSS } from '@master/css.next' // [!code ++]

            /** @type {import('next').NextConfig} */
            const nextConfig = await withMasterCSS({ // [!code ++]
                /* your Next.js config */ // [!code ++]
            }, { // [!code ++]
                mode: 'static' // [!code ++]
            }) // [!code ++]

            export default nextConfig
            ```
        </StepR>
    </Step>
    <Step $row>
        <StepL>
            ### <StepNum />Import stylesheets
            Import Master CSS from `app/globals.css`. This stylesheet is also the project CSS entry.
        </StepL>
        <StepR>
            ```css name=app/globals.css
            @import '@master/css';

            .main {
                background-color: var(--color-primary);
            }

            @theme {
                --color-primary: #ff0000;
            }

            @components {
                hero-title {
                    @compose m:0 font:5xl font:heavy fg:strong;
                }
            }
            ```
        </StepR>
    </Step>
    <Step $row>
        <StepL>
            ### <StepNum />Launch server
            Run `npm run dev` to start your Next.js development server
        </StepL>
        <StepR>
            ```bash name=Terminal
            npm run dev
            ```
        </StepR>
    </Step>
    <Step>
        ### <StepNum />Start using Master CSS
        Now style your first element using Master CSS syntax!
        ```tsx name=app/page.tsx
        export default function Home() {
            return (
                <h1 className="hero-title main m:2xl">Hello World</h1>
            )
        }
        ```
        Open your browser to watch the changes.
        <HelloWorld url="localhost:3000" />
    </Step>
</StepSection>
