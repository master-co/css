import brands from 'internal/data/brands'

Installation guides for popular frameworks and build tools integrated with [rendering modes](/guide/rendering-modes) of Master CSS.

## Web frameworks
<IconButtons url="/guide/installation">
    {brands.filter(({ name }) => ['Next.js', 'Svelte', 'Nuxt.js', 'Laravel', 'Astro', 'Remix', 'Blazor', 'React', 'Vue.js', 'Angular'].includes(name))}
</IconButtons>

## Build tools
<IconButtons url="/guide/installation">
    {brands.filter(({ name }) => ['Vite', 'Webpack'].includes(name))}
</IconButtons>

## CDN providers
<IconButtons url="/guide/installation">
    {brands.filter(({ name }) => ['esm.sh'].includes(name))}
</IconButtons>

---

<StepSection>
    <Step $row>
        <StepL>
            ### <StepNum />Start in one step
            Quickly create a blank Master CSS project locally with hot updates using the official CLI.
        </StepL>
        <StepR>
            ```bash name=Terminal
            npm create @master/css@rc <project>
            cd <project>
            npm run dev --open
            ```
        </StepR>
        <StepEnd/>
    </Step>
</StepSection>

---

Don't have a preferred framework or tool yet? Try the [online playground](/play) or copy and paste [CDN](/guide/installation/cdn) to quickly start using Master CSS.

