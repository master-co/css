import ConfiguredExample from '~/site/reference/ConfiguredExample'

## Give a repeated value a project name

CSS custom properties let multiple declarations share a value. A Master CSS theme makes those values available to token-aware classes. Start with one spacing token and the class that consumes it:

<ConfiguredExample source={'@theme { --spacing-card: 1.5rem; }'} classes={['p-card']} />

This example compiles the theme with the current preset before generating the class. Change the theme value when every consumer should change together; choose a local value when the change belongs to one element.

## Add modes after the shared value works

1. Check the [namespace's consumers](/reference/tokens/spacing#consumers) before naming a token.
2. Verify the emitted variable and the declaration that references it.
3. Add alternate mode values where the design requires them.
4. Test the project's actual mode trigger and inherited CSS variables.

The [Variables & modes reference](/reference/rules/modes) owns the resolution order, mode triggers and inline/static rules.

## Look up a rule

- <a id="overview"></a> [Overview](/reference/rules/modes#overview)
- <a id="variable-lifecycle"></a> [Variable lifecycle](/reference/rules/modes#variable-lifecycle)
- <a id="namespace-resolution"></a> [Namespace resolution](/reference/rules/modes#namespace-resolution)
- <a id="default-namespace-sources"></a> [Default namespace sources](/reference/rules/modes#default-namespace-sources)
- <a id="contextual-token-lookup"></a> [Contextual token lookup](/reference/rules/modes#contextual-token-lookup)
- <a id="inline-and-static-variables"></a> [Inline and static variables](/reference/rules/modes#inline-and-static-variables)
- <a id="mode-buckets"></a> [Mode buckets](/reference/rules/modes#mode-buckets)
- <a id="derived-mode-aware-values"></a> [Derived mode-aware values](/reference/rules/modes#derived-mode-aware-values)
- <a id="mode-triggers"></a> [Mode triggers](/reference/rules/modes#mode-triggers)
- <a id="project-settings"></a> [Project settings](/reference/rules/modes#project-settings)
- <a id="custom-spacing-example"></a> [Check a custom spacing token](/reference/rules/modes#custom-spacing-example)
