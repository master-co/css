import define from 'internal/utils/metadata'

const metadata = define({
    title: 'Base Styles',
    description: 'Set up Master CSS base styles, import the base layer, and add project-level base rules without fighting utilities.',
    category: 'Fundamentals',
    fileURL: import.meta.url,
})

export default metadata
