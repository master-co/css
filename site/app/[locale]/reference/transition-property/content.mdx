## Overview [sr-only]
<Overview/>

### Apply conditionally
Apply styles based on different states using [selectors](/guide/state-selectors) and [conditional queries](/guide/conditional-queries).
```html
<div class="transition-property:all:hover transition-property:all@sm transition-property:all@dark transition-property:all@print">…</div>
```

