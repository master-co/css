## Overview [sr-only] \{#overview\}
<Overview/>

## Examples \{#examples\}
### Align contents to the left \{#align-contents-to-the-left\}
Use `text-left` to align contents to the left.
<Basic className="text-left" />

### Align contents to the right \{#align-contents-to-the-right\}
Use `text-right` to align contents to the right.
<Basic className="text-right" />

### Center contents \{#center-contents\}
Use `text-center` to center contents.
<Basic className="text-center" />

```html
<!-- @MARK text-center -->
<p class="text-center">Centered text</p>
```

<details>
  <summary>Generated CSS</summary>
  <Class2CSS>
    {['text-center']}
  </Class2CSS>
</details>

### Align contents to the left and right \{#align-contents-to-the-left-and-right\}
Use `text-justify` to align contents to the left and right.
<Basic className="text-justify" />

### Use logical alignment \{#use-logical-alignment\}
Use `text-start` and `text-end` when alignment should follow the writing direction instead of always mapping to physical left or right.
```html
<!-- @MARK text-start --><!-- @MARK text-end -->
<p class="text-start">Start-aligned content</p>
<p class="text-end">End-aligned content</p>
```

Use physical `text-left` and `text-right` when the design explicitly requires those sides regardless of direction.

### Apply conditionally \{#apply-conditionally\}
Apply styles based on different states using [selectors](/guide/state-selectors) and [conditional queries](/guide/conditional-queries).
```html
<div class="text-start text-start@print text-center@sm text-end@lg">...</div>
```
