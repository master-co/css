## Overview [sr-only] \{#overview\}
<Overview/>

## Examples \{#examples\}
### Keep text on one line \{#keep-text-on-one-line\}
Use `white-space:nowrap` when inline text should not wrap. Pair it with overflow handling when the container can become narrow.
```html
<!-- @MARK white-space:nowrap --><!-- @MARK overflow:hidden --><!-- @MARK text-ellipsis -->
<span class="overflow:hidden text-ellipsis white-space:nowrap">
  A long single-line label
</span>
```

<details>
  <summary>Generated CSS</summary>
  <Class2CSS>
    {['white-space:nowrap', 'overflow:hidden', 'text-ellipsis']}
  </Class2CSS>
</details>

### Preserve authored line breaks \{#preserve-authored-line-breaks\}
Use `white-space:pre-line` to preserve line breaks while still collapsing runs of spaces.
```html
<!-- @MARK white-space:pre-line -->
<p class="white-space:pre-line">
  First line
  Second line
</p>
```

### Preserve spaces and wrapping \{#preserve-spaces-and-wrapping\}
Use `white-space:pre-wrap` or `break-spaces` when user-authored spacing should remain visible.
```html
<!-- @MARK break-spaces -->
<code class="break-spaces">token    value</code>
```

### Apply conditionally \{#apply-conditionally\}
Apply styles based on different states using [selectors](/reference/rules/selectors) and [conditional queries](/reference/rules/conditions).
```html
<div class="white-space:normal break-spaces@print white-space:nowrap@sm">...</div>
```

Use [text-wrap](/reference/text-wrap) when the problem is line balancing rather than preserving whitespace.
