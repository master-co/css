## Overview [sr-only]
<Overview/>

### Apply conditionally
Apply styles based on different states using [selectors](/guide/state-selectors) and [conditional queries](/guide/conditional-queries).
```html
<div class="transition-duration:fast:hover transition-duration:slow@sm transition-duration:slower@dark transition-duration:slowest@print">…</div>
```
