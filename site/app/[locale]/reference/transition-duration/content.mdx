## Overview [sr-only]
<Overview/>

## Examples
### Set transition speed
Use `transition-duration:*` to control how long a property takes to move from one value to another.
```html
<!-- @MARK transition-duration:150ms -->
<button class="bg:blue-60 transition-duration:150ms transition-property:background-color bg:blue-70:hover">
    Quick change
</button>
```

<details>
    <summary>Generated CSS</summary>
    <Class2CSS>
        {['transition-duration:150ms']}
    </Class2CSS>
</details>

### Match duration to distance
Short hover feedback usually feels best under a few hundred milliseconds. Larger position or panel changes can use a longer duration.
```html
<!-- @MARK transition-duration:300ms -->
<aside class="transform:translateX(0) transition-duration:300ms transition-property:transform">
    Panel
</aside>
```

### Remove motion for static contexts
Use `transition-duration:0ms` when a state should update immediately, such as print output or a compact fallback.
```html
<!-- @MARK transition-duration:0ms@print -->
<button class="transition-duration:150ms transition-duration:0ms@print">
    Print without motion
</button>
```

### Apply conditionally
Apply styles based on different states using [selectors](/guide/state-selectors) and [conditional queries](/guide/conditional-queries).
```html
<div class="transition-duration:150ms:hover transition-duration:0ms@print transition-duration:300ms@sm">...</div>
```
