## Overview [sr-only]
<Overview/>

## Conditionally apply
Apply styles based on different states using [selectors](/guide/state-selectors) and [applying conditionally](/guide/applying-conditionally).
```html
<div class="font-variant-numeric:normal:hover font-variant-numeric:normal@sm font-variant-numeric:normal@dark font-variant-numeric:normal@print">…</div>
```

