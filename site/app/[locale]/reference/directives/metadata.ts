import define from 'internal/utils/metadata'

const metadata = define({
    title: 'Directives',
    description: 'Reference for Master CSS stylesheet directives, entry markers, settings, reference context, and extraction policy.',
    category: 'Stylesheet',
    type: 'entity',
    fileURL: import.meta.url,
})

export default metadata
