## Overview [sr-only]
<Overview/>

---

## Conditionally apply
Apply styles based on different states using [selectors](/guide/state-selectors) and [conditional queries](/guide/conditional-queries).
```html
<div class="visibility:visible:hover visibility:visible@sm visibility:visible@dark visibility:visible@print">…</div>
```

