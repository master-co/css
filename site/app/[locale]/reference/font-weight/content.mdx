## Overview [sr-only] \{#overview\}
<Overview/>

## Examples \{#examples\}

### Basic usage \{#basic-usage\}
Compare the same phrase at a constant size and line height.

<DemoExample page="font-weight" section="basic-usage" />

<BasicUsage />

### Use explicit weights \{#use-explicit-weights\}
Use `font-weight:*` for a numeric value between named steps. A variable font can interpolate supported weights; a static family uses its available faces and may synthesize a heavier appearance.

<DemoExample page="font-weight" section="use-explicit-weights" />

```html
<!-- font-medium -->
<p id="target" class="m:0 font-medium font-xl leading-sm">Design with clarity.</p>
```

```html
<!-- font-weight:550 -->
<p id="target" class="m:0 font-weight:550 font-xl leading-sm">Design with clarity.</p>
```

```html
<!-- font-semibold -->
<p id="target" class="m:0 font-semibold font-xl leading-sm">Design with clarity.</p>
```

<details>
  <summary>Generated CSS</summary>
  <Class2CSS>
    {['font-weight:550']}
  </Class2CSS>
</details>

### Apply conditionally \{#apply-conditionally\}
Hover the button or focus it with the keyboard. Both states apply `font-semibold`; the default remains `font-regular`.

<DemoExample page="font-weight" section="apply-conditionally" />

```html
<!-- Pointer and keyboard states -->
<button type="button" id="target" class="font-md font-regular font-semibold:hover font-semibold:focus-visible">Focus or hover to compare</button>
```
