## Overview [sr-only]
<Overview/>

## Examples
### Body text
<BodyText />
If you don't want to include line height, use the [`font:<size>`](/reference/font-size).

### Headings
<Headings />

### Page titles, hero sections
<PageTitles />

### Use a precise size
Use `text:*` when the type scale utility should set font size, line height, and letter spacing together.
```html
<!-- @MARK text:16px -->
<p class="text:md">
    Body copy
</p>
```

<details>
    <summary>Generated CSS</summary>
    <Class2CSS>
        {['text:16px']}
    </Class2CSS>
</details>

### Apply conditionally
Apply styles based on different states using [selectors](/guide/state-selectors) and [conditional queries](/guide/conditional-queries).
```html
<div class="text:xl:hover text:md@print text:5xl@sm">...</div>
```
