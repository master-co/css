import { IconArrowUp } from '@tabler/icons-react';

Use the `round` token to create an icon button.
<Demo>
    <button className='btn btn-lg yellow touch-yellow round p:0'>
        <IconArrowUp className='size:1em' />
    </button>
</Demo>
```html
<!-- @MARK round -->
<button class="round …">
    <svg>…</svg>
</button>
```