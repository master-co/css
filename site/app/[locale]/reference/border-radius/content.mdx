## Overview [sr-only]
<Overview/>

## Examples
### Basic usage
<BasicUsage />

### Create a pill button
<CreatePillButton />

### Create a icon button
<CreateIconButton />

### Apply conditionally
Apply styles based on different states using [selectors](/guide/state-selectors) and [conditional queries](/guide/conditional-queries).
```html
<div class="r:md:hover r:md@sm r:md@dark r:md@print">…</div>
```

---

## Customization
### Use custom border radius
Customize your border radius tokens by defining [variables](/guide/variables).
```css name=index.css
@theme {
    --border-radius-xs: 2; /* [!code ++] */
    --border-radius-sm: 4; /* [!code ++] */
}
```
Apply the defined `border-radius` variables.
```html
<!-- @MARK r:sm -->
<div class="r:sm">…</div>
```
