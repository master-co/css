## Overview [sr-only]
<Overview/>

### Apply conditionally
Apply styles based on different states using [selectors](/guide/state-selectors) and [conditional queries](/guide/conditional-queries).
```html
<div class="animation-name:fade:hover animation-name:fade@sm animation-name:fade@dark animation-name:fade@print">…</div>
```

