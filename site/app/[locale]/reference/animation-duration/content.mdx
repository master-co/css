## Overview [sr-only]
<Overview/>

### Apply conditionally
Apply styles based on different states using [selectors](/guide/state-selectors) and [conditional queries](/guide/conditional-queries).
```html
<div class="animation-duration:fast:hover animation-duration:slow@sm animation-duration:slower@dark animation-duration:slowest@print">…</div>
```
