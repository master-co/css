## Installation [sr-only]
```bash action=install
@master/css
```

---

## API Reference
### Stylesheet entries
`@master/css` exports the default stylesheet and the split stylesheet entries used by integrations.

```css
@import '@master/css';
```

The default stylesheet contains:

```css
@master shake;
@import "./base.css";
@import "./theme.css";
@import "./utilities.css";
```

Use `@import '@master/css/base.css';` when a runtime-only page only needs the base layer declaration and normalization rules. Use the default `@master/css` import from app stylesheets that should receive generated Master CSS and native CSS shaking in extract mode.

### `MasterCSS`
The main body of this package is the class `MasterCSS`, which encapsulates the core business of Master CSS.
```js
import { MasterCSS, createCSS } from '@master/css'

const css = createCSS(config)
const customCSS = new MasterCSS(config)
```
<DocFn>
    {[
        { name: 'config?', type: 'Config', defaultValue: 'undefined' },
    ]}
</DocFn>

### `css.config`
The `css.config` property is the resolved config used by the engine. `new MasterCSS(config)` uses the provided config directly; `createCSS(config)` extends the default config with the provided config.
<DocProp types={['Config']} />

### `css.text`
This `css.text` getter returns the result of joining the text of all CSS rules.
<DocProp defaultValue="''" types={['string']} />

### `css.add()`
Add a new syntax class.
<DocFn>
    {[
        { name: '...classNames', type: 'string[]' },
        { name: 'return', type: 'boolean' }
    ]}
</DocFn>
```ts
css.add('text:center', ...)
```

### `css.delete()`
Remove an existing syntax class.
<DocFn>
    {[
        { name: '...classNames', type: 'string[]' },
        { name: 'return', type: 'boolean' }
    ]}
</DocFn>
```ts
css.delete('text:center', ...)
```

### `css.refresh()`
Refresh the Master CSS with the configuration.
<DocFn>
    {[
        { name: 'config?', type: 'Config', defaultValue: 'this.config' },
        { name: 'return', type: 'this' },
    ]}
</DocFn>
```ts
css.refresh(config)
```

### `css.reset()`
Clear all rules and reset to initial.
<DocFn>
    {[ { name: 'return', type: 'this' } ]}
</DocFn>
```ts
css.reset()
```

### `css.destroy()`
Destroy and remove the current instance.
<DocFn>
    {[ { name: 'return', type: 'this' } ]}
</DocFn>
```ts
css.destroy()
```

### `globalThis.MasterCSS`
<DocProp defaultValue="MasterCSS" types={['typeof MasterCSS']} />
