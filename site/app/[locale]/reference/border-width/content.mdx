## Overview [sr-only]
<Overview/>

## Examples
### Set border width
Use `b:*` with a size value to set `border-width`.
```html
<!-- @MARK b:px -->
<div class="b:px b-solid">
    ...
</div>
```

<details>
    <summary>Generated CSS</summary>
    <Class2CSS>
        {['b:px']}
    </Class2CSS>
</details>

### Set one side or axis
Use `bt:*`, `br:*`, `bb:*`, and `bl:*` for physical sides. Use `border-inline-width:*` and `border-block-width:*` for logical axes.
```html
<!-- @MARK bt:2px --><!-- @MARK border-block-width:1 -->
<div class="bt:2px border-block-width:1">...</div>
```

Use [border](/reference/border) when width, style, and color should be declared together.

### Apply conditionally
Apply styles based on different states using [selectors](/guide/state-selectors) and [conditional queries](/guide/conditional-queries).
```html
<div class="b:0 b:px@sm b:0@print">...</div>
```

Remember that a border is not visible unless its style is not `none`.
