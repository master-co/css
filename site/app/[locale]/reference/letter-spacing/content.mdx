## Overview [sr-only] \{#overview\}
<Overview/>

## Examples \{#examples\}

### Basic usage \{#basic-usage\}
Compare the same uppercase label across the tracking scale.

<DemoExample page="letter-spacing" section="basic-usage" />

<BasicUsage />

### Use a custom value \{#use-a-custom-value\}
An `em` value scales with the element’s font size. This explicit `0.03em` adds 0.6px at 20px type; the box readout measures the complete run.

<DemoExample page="letter-spacing" section="use-a-custom-value" />

```html
<!-- A measured custom adjustment -->
<p id="target" class="inline-block m:0 font-xl leading-md tracking:.03em">CLEAR TYPE</p>
```

<details>
  <summary>Generated CSS</summary>
  <Class2CSS>
    {['tracking:.03em']}
  </Class2CSS>
</details>

### Apply conditionally \{#apply-conditionally\}
Hover or keyboard-focus the button to increase tracking. Leave enough room for the text to grow without clipping.

<DemoExample page="letter-spacing" section="apply-conditionally" />

```html
<!-- Pointer and keyboard states -->
<button type="button" id="target" class="font-md tracking-normal tracking-wide:hover tracking-wide:focus-visible">Explore the canvas</button>
```
