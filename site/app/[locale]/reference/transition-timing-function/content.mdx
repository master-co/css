## Overview [sr-only]
<Overview/>

### Apply conditionally
Apply styles based on different states using [selectors](/guide/state-selectors) and [conditional queries](/guide/conditional-queries).
```html
<div class="~easing:smooth:hover ~easing:crisp@sm ~easing:overshoot@dark ~easing:ease-in-out@print">…</div>
```
