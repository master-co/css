import MASTER_CSS_RAW from '~/site/../packages/create/src/master-css-template'

## Usage [sr-only]
### Create a CSS entry
Initialize Master CSS in an existing project.
```bash action=create
@master/css@rc
```

### Create a new app
Or you can specify an argument to create a new project, which by default uses the [blank example](https://github.com/master-co/css/tree/rc/examples/blank).
```bash action=create
@master/css@rc my-app
```

---

## API Reference
### `create`
Create a project CSS entry in the current project.
<DocProp defaultValue="'index.css'" types={['string']} />
```bash action=create
@master/css@rc
```
<details>
    <summary>View the generated file</summary>
    <div>
        <Code lang="css" name="index.css">
            {MASTER_CSS_RAW}
        </Code>
    </div>
</details>

### `create --example`
Create a Master CSS App using [official examples](https://github.com/master-co/css/tree/rc/examples).
<DocProp defaultValue="'blank'" types={['string']} />
```bash action=create
@master/css@rc project --example react
```

### `create --pm`
Manually specify the package manager you are using.
<DocProp defaultValue="auto" types={['string']} />
```bash action=create
@master/css@rc --pm bun
```

### `create -o, --override`
Override the existing project CSS entry.
<DocProp defaultValue="false" types={['boolean']} />
```bash action=create
@master/css@rc --override
```
