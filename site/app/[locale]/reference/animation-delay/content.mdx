## Overview [sr-only]
<Overview/>

### Apply conditionally
Apply styles based on different states using [selectors](/guide/state-selectors) and [conditional queries](/guide/conditional-queries).
```html
<div class="animation-delay:300ms:hover animation-delay:300ms@sm animation-delay:300ms@dark animation-delay:300ms@print">…</div>
```

