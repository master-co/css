import categories from '~/site/.categories/reference.json'

## Syntax
<OverviewButtons>
    {categories.find(category => category.name === 'Syntax').pages}
</OverviewButtons>

## Package
<OverviewButtons>
    {categories.find(category => category.name === 'Package').pages}
</OverviewButtons>
