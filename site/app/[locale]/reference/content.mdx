import categories from '~/site/.categories/reference.json'

## Design Token
<OverviewButtons>
    {categories.find(category => category.name === 'Design Token').pages}
</OverviewButtons>

## Syntax
<OverviewButtons>
    {categories.find(category => category.name === 'Syntax').pages}
</OverviewButtons>

## Integration
<OverviewButtons>
    {categories.find(category => category.name === 'Integration').pages}
</OverviewButtons>
