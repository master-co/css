## Overview [sr-only] \{#overview\}
<Overview/>

## Examples \{#examples\}
### Enable a font feature \{#enable-a-font-feature\}
Use `font-feature-settings:*` to pass OpenType feature tags directly to `font-feature-settings`.
```html
<!-- @MARK font-feature-settings:'tnum' -->
<span class="font-feature-settings:'tnum'">1234567890</span>
```

<details>
  <summary>Generated CSS</summary>
  <Class2CSS>
    {["font-feature-settings:'tnum'"]}
  </Class2CSS>
</details>

### Reset features \{#reset-features\}
Use `font-feature-settings:normal` when a nested element should not inherit custom feature settings.
```html
<!-- @MARK font-feature-settings:normal -->
<span class="font-feature-settings:'tnum'">
  <span class="font-feature-settings:normal">Reset</span>
</span>
```

### Prefer semantic numeric utilities \{#prefer-semantic-numeric-utilities\}
For common numeric features, prefer [`tabular-nums`](/reference/font-variant-numeric) or related numeric utilities because they are easier to read than raw feature tags.
```html
<span class="tabular-nums">12:45</span>
```

### Use feature tokens \{#use-feature-tokens\}
Define `font-feature-*` tokens when a feature combination is shared across several typographic treatments.

```css name=index.css
@theme {
  --font-feature-tabular: "tnum";
}
```

```html
<span class="font-feature-settings:tabular">1234567890</span>
```

### Apply conditionally \{#apply-conditionally\}
Apply styles based on different states using [selectors](/reference/rules/selectors) and [conditional queries](/reference/rules/conditions).
```html
<span class="font-feature-settings:'tnum':hover font-feature-settings:normal@print font-feature-settings:normal@sm">...</span>
```
