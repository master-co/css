## Causes [sr-only]
### Why this error occurred

1. The server's compiled project plan does not match the client's.
2. The server's version does not match the client's.

---

## Solutions
### Import the same plan
```ts name=server.ts
import plan from 'virtual:master-css-plan.json'
```
```ts name=client.ts
import plan from 'virtual:master-css-plan.json'
```
