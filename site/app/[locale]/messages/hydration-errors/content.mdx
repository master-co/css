## Causes [sr-only]
### Why this error occurred

1. The server's [configuration](/guide/configuration) does not match the client's.
2. The server's version does not match the client's.

---

## Solutions
### Import the same configuration
```ts name=server.ts
import config from './master.css?master-css-config'
```
```ts name=client.ts
import config from './master.css?master-css-config'
```
