## Errors
<OverviewButtons>
    {props.pageCategories.find(category => category.name === 'Errors').pages}
</OverviewButtons>

## Warnings
<OverviewButtons>
    {props.pageCategories.find(category => category.name === 'Warnings').pages}
</OverviewButtons>
