import define from 'internal/utils/metadata'

const metadata = define({
  title: 'Missing Master CSS entry',
  description: 'The integration could not find a project CSS entry for the shared MasterCSSManifest, editor tooling, and build integration behavior.',
  category: 'Warnings',
  fileURL: import.meta.url
})

export default metadata
