import define from 'internal/utils/metadata'

const metadata = define({
    title: 'Missing master.css',
    description: 'The integration could not find a master.css workspace entry for Master CSS config, editor tooling, and build integration behavior.',
    category: 'Warnings',
    fileURL: import.meta.url
})

export default metadata
