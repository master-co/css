# Previous shared handoff - preserved verbatim

- 0183 已修正 managed manifest 啟動失敗後的開發恢復：伺服器保留 HTTP 錯誤與原診斷，讀到有效 manifest 後才重新載入；HTML 在 Vite 預載模組前檢查錯誤，正式建置仍遇錯失敗。compiler async／sync project API 新增可選 onDependency，在讀檔前逐一回報 imports／references（含缺檔），沿用既有 Rust／Node graph，沒有語意 fallback；Vite 以既有失敗相依核對處理尚無模組的初始化，pre-render 不使用失效 manifest 的舊 renderer。新 13Vite／6compiler 控制通過；完整 Vite592tests 為586PASS／6FAIL，原479全PASS，剩餘6例皆為 build-watch 啟動缺 reference。Compiler275／internal18、三套件 lint／types／build 與原 Vite 範例通過。最終600三瀏覽器觀測PASS、0非預期pageerrors：新72manifest（四模式、runtime開關、錯誤更新與恢復）加原528reference／resource／directory／diagnostic。初版漏報reference相依與HTML預載時序已修正，瀏覽器腳本錯讀manifest.components及外部暫存app缺套件解析路徑另列腳本錯誤；原始失敗／追蹤日誌保留。423來源已核對；12既有來源修改、5新來源，163原排除檔、foreign Site、5Wasm/runtime與3CLI產物不變。API census／package golden仍FAIL，新增可選callback為刻意的public型別變動，未覆寫golden。下一步補manifest進行中關閉／restart等所有權邊界，再接internal Modules proxy?direct HTTP500與其餘host；6build-watch待可用host能力，阻礙不計完成。47historical／45fixed／2unresolved、65checked／10blocked；BH-0004／BH-0029、完整graph／其他host／benchmark、4root gates／4原候選、native shutdown／WebKit及0038身分暫停維持。0183與目標active；HEADa19516d15，本輪未commit/push。[0183](batches/0183-reference-host-recovery.md)／[核對](evidence/0183-manifest-final-checks.json)；前次共同交接逐字見[歷史](progress-history-0183-manifest.md)。
