# 0181 Virtual source reference ownership

- Previous turn completed user-authorized commits d572f6bed/4d7274050 and verified 157 excluded files unchanged. This continuation has no new commit/push authorization. All 389 source hashes match 0180; 0181-before-hashes.json remains the original baseline.
- Scope BH-0004: relative CSS `@reference` in supplied virtual/preprocessed graph entries and children, including original reference resource owners, registration/composition variants, dependencies and recovery. Next verify actual retained Sass/Vite dev/build behavior. Relative Sass reference targets and arbitrary virtual reference aliases are not promised by the existing CSS/package reference contract.
- Read compiler package/AI, data flows/testing policy, public stylesheet APIs, graph delivery and reference resolver. The resource/source-pattern paths use graph.baseFiles; reference extraction currently uses only graph identity/owners. This is a hypothesis until a focused reproduction fails.
- 47 historical / 45 fixed / 2 unresolved (BH-0004/BH-0029), 65 checked / 10 blocked remain. Full graph/public-path/host migration, four root gates/four original candidates, benchmark matrices/history and host/browser limitations remain unfinished. Batch0038 additional verification is still paused pending explicit identity confirmation.

## Reproduction

- Six public API cases: four virtual entry/child transform/collection cases FAIL, two physical controls PASS. Encoded/query/fragment reference URLs incorrectly resolve the root-directory decoy instead of two distinct owner directories. The dependency assertions expose actual wrong-file selection, not a missing fixture or unsupported Sass reference target.
- Host correction preserves compileCSS diagnostic source identity, maps only extracted reference filesystem origins through graph.baseFiles, uses the real entry owner for cycle detection, and reports attempted reference paths before realpath/read errors so missing dependencies can be watched. Focused and lifecycle validation pending; no completion claim.

## Intermediate verification

- Six original public cases now PASS; expanded ten include failed re-registration preserving prior state, attempted missing dependency notification, recovery with changed definitions/removal of stale resource dependencies, self/indirect reference cycles and original virtual diagnostic identity. Compiler lint/types PASS.
- Initial Vite log named `0181-vite-reference-before.log` actually ran after the compiler source correction (Vitest resolves workspace source paths); it is not a before-product-fix baseline. Both build cases PASS. Dev probe first read the Modules JavaScript facade as CSS; normal client-request CSS was then decoded from Vite's emitted JavaScript wrapper. Dev layers are emitted as blocks, unlike the retained build imports; the original literal import-layer assertion was a harness error.
- Separate explicit `?direct` requests to the emitted internal `.master-css-sass.css` proxy return HTTP500 and attempt `/style.module.css` at the filesystem root. Both SSR-first and client-first traces reproduce this. Normal emitted client imports succeed. This boundary remains uncompleted under BH-0004; preserve the logs and reproduce via an actual browser/client contract before deciding the owning repair.
- Expanded Vite four-mode SCSS/indented-Sass build/dev and reference/resource HMR controls are running. Full compiler/downstream checks and final ledger synchronization remain pending.

## Final verified checkpoint

- Compiler253 tests/34files, Vite457 tests/60files and CLI71 tests/16files PASS. Compiler/Vite lint and types, both package builds and original Vite example PASS. Site prepare and lint PASS with 0errors/75existingwarnings. No Rust/compiler semantic change, existing test/fixture/snapshot edit, dependency or build-flow change.
- Sixteen Vite source tests cover two Sass syntaxes, four modes, real builds and development HTTP/SSR loads. The eight initial HMR assertions had omitted the client entry's module graph and accept boundary; Vite correctly requested full reload. Loading the actual entry with its explicit accept contract makes all16PASS, including reference/resource edits. This is a harness omission, not a repaired product HMR defect.
- Built-plugin browser validation independently exercises default runtime behavior, both Sass syntaxes, four modes and Chromium/Firefox/WebKit. All96observations PASS:72development initial/reference-update/resource-update and24production initial. Exact SVG bytes/MIME/query/fragment/pixels, expected padding, absence of reference-native leakage and unchanged boot IDs verify ownership and live delivery. No retained failure was relabeled PASS.
- Compiler test and Vite integration behavior use workspace source resolution; browser tests import built Vite dist. Compiler build briefly overlapped the source-resolved focused Vite test, with no failure; all built consumers started after both package builds completed. No rebuilt native/Wasm artifacts were read during mutation.
- The new Vite test's indentation was normalized after the full run had started; executable content did not change. Final lint/types pass on the recorded source. The initial ledger-sync script assumed the README batch index was a table and stopped before writing; corrected to the actual bullet index. Both are bookkeeping details, not product failures.
-392source hashes reconcile against389:4existing changes (compiler delivery/import-source comment, compiler README and directive guide),2newtests/1repro. Of157previous exclusions,153byte-identical and4intended changes. Three foreign Site files,fiveWasm/runtime artifacts andthreeCLI built artifacts unchanged; indexempty. All process handles terminal; no commit/push.
- This closes only explicit-baseFile reference ownership for prepared graphs and the verified direct retained Sass cases. Cross-directory Sass partial reference origins, actual host missing-reference/resource recovery, missing-base virtual references and the internal proxy `?direct`500 boundary remain incomplete. Full BH-0004 graph/public-path migration and BH-0029 still open; all10blockedcoverage,4rootgates,4originalcandidates,benchmarkmatrices/history,native shutdown/WebKit namespace andpaused0038 remain.
- Next0182 starts with a retained Sass child importing a partial from a different directory, where the partial's relative CSS reference and resource must retain the right origin. Compare pure Vite/Sass output and available maps before choosing the host boundary. Then verify missing reference deletion/restoration through actual dev/build-watch, and classify the recorded internal proxy direct-request boundary. Goalactive; obstacles are not completion.
- This host-only correction adds a small per-reference owner mapping and attempted-path notification. No engine/runtime hot-path or payload change; no performance improvement claimed or hot-path benchmark required.

## Evidence

- [Original4FAIL/2PASS](../evidence/0181-reference-before.log), [ten ownership/cycle/recovery/diagnostic cases](../evidence/0181-reference-expanded.log), [253compiler](../evidence/0181-compiler-tests.log), [457Vite](../evidence/0181-vite-tests.log), [71CLI](../evidence/0181-cli-tests.log).
- [16Vite host cases](../evidence/0181-vite-reference-client-graph.log), [omitted-client-graph failures](../evidence/0181-vite-reference-modes.log), [internal direct proxy failure](../evidence/0181-vite-reference-client-first.log), [96browser summary](../evidence/0181-browser-summary.json), [browser driver](../repros/vite-retained-sass-references.mjs).
- [Preservation](../evidence/0181-handoff-preservation.json), [final checks](../evidence/0181-final-checks.json), [source hashes](../evidence/0181-source-hashes.json), [verification summary](../evidence/0181-verification-summary.json), [inventory](../evidence/0181-file-inventory.json).
