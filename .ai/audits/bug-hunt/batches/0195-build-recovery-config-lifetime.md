# Batch 0195: Build recovery configuration lifetime

- In progress. Continue0190original3failures;530sources match0194. Vite8.2.2 public build.createEnvironment runs after configResolved, before native watch creation. Its environment.init must finish all applyToEnvironment hooks before allocating owned recovery material. A factory/init wrapper may avoid config and environment-selection failures leaking private files while preserving custom environments; verify actual watch startup to detect the previously rejected spurious-create rebuilds. Rolldown public watcher still has on/off/clear/close only; filtered invalidation remains unresolved. All graph/host/maps/gates/benchmarks/Site,2original candidates and0038identitypause remain. No commit/push.

- First after-init allocation made16setup-failure controls and4custom-environment controls pass, but the61-test recovery run retained2filters and observed1extra stable-error rebuild. A matched trace rerun was59PASS/2filtersFAIL; do not erase the first counterexample. Allocation now begins in the environment factory, after all config hooks but before factory/init work, with immediate release of unstarted material on either failure. Existing started owners prevent cleanup from removing live recovery. Verify this revision separately; no arbitrary timing delay or assertion relaxation.
- Pure-host missing JS import controls also timed out on Vite8.2.2 for default/include/exclude, including a500ms settle control; no Master plugin. Installed Vite6.4.3 yielded mixed outcomes (one BUNDLE_END, otherwise TIMEOUT or a repeated unresolved error), so no general Vite6 compatibility/host root-cause claim. These are4-second bounded observations, not proof a later event cannot occur; no dependency changed.

## Rejected allocation revision and stronger controls

- Previous goal turn made progress: standalone BH-0059/0060 committed as a80ba1b50; completed audit records/checkpoints committed c77756b13. This continuation does not authorize another commit/push.
- Normal250ms lifecycle assertions are unchanged. Optional BH_WATCH_TRACE_FILE now writes per-root/mode JSONL watch events, terminal events and authored writes without changing timing. Normal63controls:58PASS/5FAIL; three extra lifecycle rebuilds (local static, managed pre-render/progressive) plus two known filters. Managed traces contain private-cache create events before the first authored dependency write, excluding reconciliation responding to that write as the sole cause.
- Reinstating the exact0194 configResolved plugin (SHA-256 matches inventory) for a bounded control made all8original lifecycle cases PASS, with no pre-write watch events. It was temporarily restored to the candidate afterward. This does not prove all old allocation timings immune; it shows the candidate cannot inherit0194's prior regression results.
- New pure Vite8.2.2 driver, no Master CSS plugin and no writes after watcher construction: native watcher receives create events for both configResolved-created and environment-created files, causing two ERROR builds. Pre-existing AGENTS.md control is quiet. All3polling controls are quiet. Six600ms observations total4PASS/2FAIL. This supports a native-host initial-event limitation; it does not justify forcing polling for the entire user's build or treating custom filters as repaired. Driver/log: vite-watch-initial-events.mjs /0195-pure-initial-events.log.
- Expanded setup tests:22PASS/2FAIL; nonshared builder peer factory/init failure leaves the successful peer's unstarted private file. The first nonshared test incorrectly supplied the custom factory only at top-level; Vite's per-environment build config selected its default factory. Corrected only the new test to provide each environment's factory. Initial setup mistake and corrected cleanup failures are retained separately in0195-peer-config-{lifetime,corrected}.log.
- Reject the factory/init allocation attempt. Preserve its exact sources/hashes in0195-rejected-source.json, restore the0194plugin exactly, and remove the unused cleanup helper. The utility's comment now accurately names config resolution; behavior is restored. No unrelated source or artifact changes.24newsetup tests retain their required cleanup assertions. Configuration failure cleanup, private trigger/filter architecture, host initial-event behavior and full goal remain unfinished.
- Next investigate an invalidation/lifecycle design that observes accepted failed dependencies without exposing newly created private files to native watch history, and releases every unstarted environment when setup aborts. Do not use arbitrary waits, mutate authored/dependency files, force global polling, discard filter rules or weaken tests. The pure-host6controls and new shared/nonshared24setup cases are required controls.0038 remains paused; no identity confirmation.

- Restored-state65controls:42PASS/23FAIL (20expanded setup cleanup failures plus original configuration cleanup and2filters); no extra lifecycle rebuild in this run. Vite lint/types PASS. No full-suite/build/browser rerun is claimed because candidate source changes were rejected;358existing artifacts unchanged.528of530baseline sources match, with only utility comment and opt-in trace test differences;3new evidence sources give533. Full pending scope, log hashes and preservation are in[progress checks](../evidence/0195-progress-checks.json). All commands terminal; no commit/push.0195 remains in progress.
