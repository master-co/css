# 0097 CLI scanner binding selection

- BH-0026 originally confirmed0048; current pre-fix source execution again instantiates native ScannerSession under --binding wasm ([before](../evidence/0097-binding-before.log)). CLI discarded the option, scanner options lacked load settings, and its factory always requested default loading.
- CLI forwards binding choice to scanner; scanner options accept binding/native/wasm loading options and forward them to the existing tooling binding factory. Scanner and validator use the selected binding. Commander rejects values outside auto/native/wasm. Compiler/project Node orchestration retains its separate native contract: this is not a claim that --binding wasm avoids all native code in the CLI.
- Source test aliases resolve the browser Wasm provider, so direct source controls supply actual Wasm bytes. Built CLI tests use normal Node package export conditions and its filesystem-capable Wasm provider. No root tsconfig, binding loader, generated artifact source or dependency workaround.
- Scanner14files/83tests PASS, including real Wasm reset and deliberate native switch, explicit Wasm load failure, and native failure without fallback ([scanner](../evidence/0097-scanner-tests.log)). Full CLI9files/34tests PASS, including invalid option rejection and0096watch controls ([CLI](../evidence/0097-cli-tests-final.log)).
- `BH_CLI_ARTIFACT=dist node scripts/with-typescript-tooling-compat.mjs node .ai/audits/bug-hunt/repros/BH-0026-binding.mjs` passes with actual built CLI; native scanner1, Wasm scanner0, both CSS outputs correct ([built](../evidence/0097-built-binding.log)). Observer delegates process.dlopen and wraps constructors; artifacts are not replaced.
- `node .ai/audits/bug-hunt/repros/cli-binding-watch.mjs` passes both built CLI bindings: initial CSS, atomic CSS configuration replacement, scanner reset, and newly created HTML after reset. Native creates2scanners/2validators; Wasm creates0native scanners/validators ([watch](../evidence/0097-built-binding-watch.log)). Temporary directories and own child processes cleaned.
- Tooling/CLI lint, type-check and builds PASS; CLI final build/types/lint include0096helper correction. No new source changes to tooling since its successful build/scanner tests.
- BH-0026 fixed for CLI scanner/validator selection. Combined0096/0097 progress:44historical findings,20fixed/24unresolved;65checked/10blocked coverage unchanged. BH-0004 partial and Integration lab paused remain unfinished. No commit this goal continuation; all jobs terminal.
