# 0072 Vite benchmark source counts

- Scope classify source-file-count6 from0070/0067. Current build/startup helper, Vite usage-graph/scanner/core and tooling scanModule/isModuleAllowed source inspected. Original scanner skips style/virtual/non-source IDs; benchmark increments before calling plugin. Actual trace below confirms BH-0041.
- [Driver](../repros/vite-scan-count-control.mjs), [source hashes](../evidence/0072-source-hashes.json). Original minimal fixture prepared under disposable benchmarks/.results; external observer wraps original usageGraph hooks and scanner scanModule/scan methods in memory, invokes original implementations and restores prototypes. No product file edit. Instrumented CSS must SHA256-match original0070 output before counts are accepted.

## BH-0041 · P3 · 已確認：Vite benchmark 把所有 hook callback 當成已掃描檔案

- Command: `python3 .ai/audits/bug-hunt/repros/isolated-package.py benchmarks node /Users/aron/master/css/scripts/with-typescript-tooling-compat.mjs node --import tsx /Users/aron/master/css/.ai/audits/bug-hunt/repros/vite-scan-count-control.mjs`. [Log](../evidence/0072-scan-counts.log), [trace](../evidence/0072-scan-counts.json). Six observed hooks exactly match0070reported source-file-count6, but actual scan called3times for2unique files (index.html twice, src/main.js once).
- Two virtual IDs (rolldown runtime and Vite modulepreload polyfill) are returned early by usageGraph; input.css reaches scanModule but isModuleAllowed=false, so it never calls scan. Both validHTML/JS controls do reach scanner. CSS5179bytes/SHA256d4a478716d50513c2944406c37108650cd333dd5d5404e34ab9b20360c258bcc exactly matches uninstrumented0070artifact; observation did not change output.
- Root `benchmarks/shared/build-diagnostics.ts:242` and `benchmarks/shared/startup-diagnostics.ts:482` sum transform+HTMLhook invocation counts. Build metric metadata:50–53 explicitly describes fixture source files scanned. Source-file-count6 is neither unique scanned files2 nor scan operations3, so label is incorrect regardless of deduplication convention.0070and0067original reports both show6 for eachVite fixture; this trace validates minimal case, not every arbitrary plugin graph.
- Classification benchmark tooling; actual scanner skip behavior and CSS remain correct in the control. Same count algorithm affects build and startup diagnostics, one stable ID. Fix direction (not implemented): count actual eligible scanned sources with a defined deduplication contract, or expose hook-invocation counts under accurate labels rather than as source files.
- Original build/startup Vite execution controls completed; accurate source-file metrics remain unfinished underBH-0041. Observer restored prototypes, browser-free build process exited0 and isolated directory removed. Benchmarks has no local lint script; no product edits, fixtures/snapshots, installs or commit.

- Final reconciliation: [source/ownership/link/budget checks](../evidence/0072-final-checks.json), [inventory](../evidence/0072-file-inventory.json), [AI context check](../evidence/0072-ai-context.log).
