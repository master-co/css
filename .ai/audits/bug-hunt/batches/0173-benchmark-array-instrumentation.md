# 0173 Benchmark runtime array instrumentation

- Previous goal turn made progress:BH-0034 native CSSOM traversal fixed and verified.359 baseline source hashes match;HEADca2c8d00d. No new commit/push authorization.
- Scope BH-0042: preserve the current readonly-array method contract, count actual supplied array entries, batch delayed deletion per runtime, and verify actual public snapshot/native CSSOM removal. Empty arrays, repeated names, capture windows and repeated flushes are part of the focused controls.
- Original0078 established one-array-argument counted as one class and variadic apply on delayed flush leaving all requested rules in place.0171 fixed both preseed callers to use arrays; this batch completes method instrumentation and strategy behavior.
- No runtime product, fixture/snapshot, dependency, lockfile, CI/release or foreign-work changes. All9 unresolved findings/10 blocked coverage/4 gates/4 candidates/host shutdown and0038 identity pause remain until evidence supports closure.

## Reproduction and implementation

-36 before-change direct controls fail across Chromium/Firefox/WebKit, runtime/progressive and baseline/defer/suppress strategies. Baseline reports1 array argument instead of3 entries; delayed flush claims3 removals while all3 rules remain in both public snapshot and native CSSOM. Repeated/empty calls show the same count defect. No product deletion failure is inferred from these wrappers.
- Wrappers now accept the current single readonly class-name array and forward that exact array with call(this,classNames). Counts use supplied array length:repeats count separately, empty arrays add zero while still counting a method call. Non-array legacy input throws a benchmark TypeError before reaching the product method. Baseline results remain the original transition object.
- Queues copy the submitted array so later caller mutation cannot change pending work. Flush keeps existing per-runtime deduplication and passes one array to the original delete method. It separately records raw queued entries, unique names per runtime, native method calls and duration. Repeated empty flush is a no-op. Requested-entry counts are explicitly distinguished from successfully deleted rules in metric descriptions.
-4 focused instrumentation tests plus15 existing benchmark tests PASS (19total); types PASS. Tests execute the original generated harness with controlled method receivers/results, cover frozen/empty/repeated arrays, separate runtime batches, caller mutation, outside-window calls and invalid legacy input. There is no package-local lint script.

## Browser evidence and measurement limits

-36 final direct browser controls PASS with independent public snapshot and native CSSOM presence before ensure, after ensure, after delete and after explicit flush. Both delayed strategies remove the actual3 requested rules. In repeated/empty controls,4 submitted entries become3 unique flushed names;2 ensure calls and3 delete calls retain exact counts. Calls outside collection execute immediately and leave diagnostic counters unchanged.
-18 actual generated cleanup-cycle controls PASS across the same3 engines/2 modes/3 strategies. A separate outer observer of method requests records each array and compares request counts and entry totals with benchmark metrics. Each observed dynamic cycle makes4 ensure calls with2 names each:8 class entries. Scratch DOM is empty and actual configured temporary class usages are zero after the cycle.
- Current product retention leaves these2 temporary classes retained during the short measurement window; no deleteClassRules request occurs in these18 scenarios and delayed queues remain empty. This is a real absence of work, not a successful deletion optimization. Explicit direct controls prove deletion behavior separately. No timing/speed claim or fixture redesign follows from the zero deletion counts.
- Both preseed sites were already repaired/verified in0171. Search of remaining tracked benchmark call sites finds no obsolete spread invocation or delayed apply(runtime,names). Lifecycle apply(this,args) correctly forwards the argument list containing the array; ellipses in existing method descriptions are notation, not executable variadic calls.
- Original mutation16, invalidation32, interaction54 and report-smoke now PASS. All48 diagnostic variants retain0171 preparation/cleanup checks and additionally compare declared call/entry/flush samples and summaries with actual diagnostic fields. BH-0043 original hardcoded class/computed-style validity remains independent.

## Final real-retention controls and classification

- Expanded actual cleanup-cycle corpus:36 PASS across three engines, two runtime modes, three strategies and retained volumes0/512. An outer method observer records the actual arrays and independently compares both method-call counts and entry counts. The512 corpus triggers one386-entry deletion; baseline and defer remove requested rules before explicit after-trace flush, while suppress retains all386 until that flush. Public snapshot and native CSSOM independently agree at both points. Final explicit retained cleanup leaves zero retained classes.
- First expanded probe passed25 cases then failed a WebKit assertion that a deletion must occur during the short original scenario. This was an observation-window assumption, not a new product bug. WebKit exposes no requestIdleCallback; its existing runtime fallback waits5000ms. The corrected probe separately observes the real callback for at most8000ms after the original window, without changing benchmark measurement duration. All six WebKit512 cases required4652–4683ms additional observation, with zero original-window delete calls and one386-entry supplemental deletion. Chromium/Firefox completed deletion in their original windows.
- The original18 short-cycle controls and first expanded25 passing controls are historical subsets, not added to the final total. Final browser evidence is36 direct controls plus36 actual scenario controls (72total). No deletion-speed benefit follows from zero original-window deletion counts; no performance comparison or fixture redesign is claimed.
- Original invalidation512 diagnostic variants also record one386-entry deletion. Their product settle stage retains128 classes; separately forced cleanup removes128, preserving0171 preparation contracts. Dynamic scenarios ensure8 entries over4 calls; stress scenarios ensure6 entries over3 calls. Samples and summaries match diagnostic fields for every declared count metric.

## Final validation and handoff

-19 tests/types PASS; no package-local lint script. Three repro scripts pass syntax checks. Original mutation16/invalidation32 plus48 preparation checks and48 array report checks, interaction54 and report-smoke PASS. Tests execute the generated harness with independent method receivers; actual browser probes additionally verify real runtime rules and native CSSOM. All owned subprocesses terminated and isolated trees were removed.
- Commands use isolated-package.py benchmarks, the mandatory with-typescript-tooling-compat wrapper, and node --import tsx with benchmark-runtime-arrays.mjs, benchmark-runtime-array-scenarios.mjs, or benchmark-runtime-array-report.mjs <diagnostic-suite>. Unit/types use the scoped package commands recorded in0172. Local macOS/Node24.20.0/Chromium153.0.8010.12/Firefox155.0/WebKit26.6 correctness evidence carries no timing claim. Existing fixture/snapshot/history is preserved.
-363 final source hashes:359 baseline sources with two benchmark-only edits and four new test/repro sources. The first hash snapshot is preserved separately because the scenario probe was extended; only that new repro changed between first and final snapshots.151 prior excluded product/test/foreign files and five native/Wasm/runtime artifacts remain byte-identical; no production runtime or semantic CSS changes.
- Evidence: [before36FAIL](../evidence/0173-arrays-before.log), [direct36PASS](../evidence/0173-arrays-fixed.log), [actual36PASS](../evidence/0173-scenario-arrays-verified.log), [first timing assumption failure](../evidence/0173-scenario-arrays-final.log), [unit19](../evidence/0173-unit-tests.log), [types](../evidence/0173-types.log), [mutation16](../evidence/0173-mutation-report.log), [invalidation32](../evidence/0173-invalidation-report.log), [interaction54](../evidence/0173-interaction-report.log), [smoke](../evidence/0173-report-smoke.log), [summary](../evidence/0173-verification-summary.json), [final checks](../evidence/0173-final-checks.json).
- BH-0042 fixed.47 historical/39 fixed/8 unresolved;65 checked/10 blocked. AllBH-0004 graph/host/Sass maps/virtual/watch/base/SSR/lifecycle/Nuxt/Webpack requirements, four root gates/four original candidates/native shutdown limitation and0038 identity pause remain. Next0174:BH-0043 actual temporary classes and computed-style/cleanup validity. Prior goal turn committed completed0171/0172 audit materials asbdd9f25b8; no further commit/push authorization and no new commit. Goal remains active.
