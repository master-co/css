# 0171 Benchmark runtime preparation

- Previous turn made progress:0170 public snapshot readers/guards verified with9 tests/36 browser controls; BH-0033 remains open.349 source hashes match, HEADac24c0f96. No commit/push.
- Repair actual forced cleanup, observer pause and retained-volume preparation without product/private facade access; explicitly handle inaccessible private operation metrics. Keep product settle distinct from benchmark forced cleanup. Original BH-0034/0042/0043 and every other requirement remain.
- Counts47 historical/36 fixed/11 unresolved,65 checked/10 blocked. Batch0038 still awaits explicit identity confirmation.

## Implemented preparation and measurement contract

- Existing benchmark instrumentation wraps Runtime.prototype.observe with the actual runtime instance. A read-only browser capability probe confirms the instance owns the retained Set and MutationObserver while the readonly global facade does not expose them. The benchmark now captures controls in that existing hook; no runtime product API or bundle changed. Missing capabilities throw instead of silently recording zero.
- Retained Set add/delete/clear wrappers count actual calls only during the measured interval and preserve original Set return values, including duplicate adds and unsuccessful deletes. Observer pause disconnects only the captured instance's current observer; snapshot/CSS remain unchanged and an independent DOM observer continues receiving mutations. Public disconnect is not used for pause because it also clears runtime state.
- Temporary-rule preseed uses the public readonly-array ensureClassRules API and verifies every configured class generated rules. The returned rule delta counts actual output-layer entries, not class names. Two temporary classes can introduce three output entries.
- Retained-volume preparation appends and removes real DOM classes and verifies requested volumes0/2/128/512 with zero usage and retained rules. CSS bytes and shared rule identities come from public snapshots. These are newly retained rules, unlike the old fabricated/private state backdated by2000ms; historical timing values are not comparable. Natural retention policy remains active during the measured action.
- Forced cleanup happens after trace collection and the product settle window. Public snapshot selects inactive retained names; public deleteClassRules receives one array; post-snapshot verifies deletion. Before/removed/after counts are explicit. Mutation diagnostics now preserve the after-forced-cleanup state in their existing disposable diagnostic JSON. Metric descriptions identify public forced deletion and exclude the product settle window from its duration.
- Two additional independent HTTP servers (mutation and invalidation) now return application/wasm.0169 verified the lifecycle/delivery/interaction servers; these extra endpoints were not part of its three-server MIME claim. Both exact server implementations passed HTTP200/MIME/binary equality and missing-file404 controls through a disposable appended export; no product export was added.

## Evidence obtained before final report checks

-12 focused tests PASS (3 preparation,4 snapshot,5 payload); benchmarks TypeScript noEmit PASS. No package-local lint script exists.
-36 actual preparation controls PASS across Chromium, Firefox and WebKit, each runtime/progressive: preseed/repeated ensure, four retained volumes, observer pause with CSS preserved/independent observer active, and one actual Set clear on teardown.2 diagnostic server MIME/binary controls PASS.
- Original interaction-cost54 variants and report-smoke PASS on the new helper. Original mutation16/invalidation32 reports completed, but added artifact validation initially failed on script assumptions described below; corrected full artifact/sample/summary verification subsequently passed. No broad performance claim follows from successful report generation.

## Error classification and remaining work

- Initial TypeScript preparation config access lacked a narrow type; corrected implementation typing, then types PASS.
- First browser preseed oracle assumed two classes must produce exactly two output rules. Independent layer counts show three; corrected to verify configured generated names, exact output delta and repeated ensure delta0. This was an audit script error.
- First artifact oracle expected runtimeStyleTextBytes in mutation JSON, but that field exists only in invalidation JSON. Corrected the suite-specific check without changing product behavior.
- The oracle then reused original helpers' hardcoded outline/shadow names. Current generated pages configure fg:green-60 and w:1px, so the oracle rejected valid preseed. The verification now reads each original generated page's actual interaction config. This confirms the already-recorded BH-0043 stale-name risk; the original benchmark validity checks remain separately unfinished.
- Final report validator checks actual configured temporary classes, preparation volumes/bytes/pause, actual retained Set operations, before/after forced cleanup and matching report samples/summary. Final48 original diagnostic artifact checks passed; BH-0033 is now fixed together with0170 reader/guard evidence. BH-0034 native CSSOM counting andBH-0042 method argument counts/deferred flush still remain independently unresolved.
- HEADca2c8d00d records the authorized0169/0170 audit commit. No new commit/push in this continuation. All BH-0004/gates/candidates/host-shutdown and0038 identity-confirmation requirements remain.

## Final validation and handoff

- Original mutation16 and invalidation32 variants PASS with actual configured temporary names, before/after retained counts, exact requested seeded classes/rules/bytes and paused-observer controls. Every preparation/cleanup sample and summary agrees with the diagnostic artifact. Mutation preseed reports3 output entries for2 configured classes; each cleanup removes2 inactive classes. Invalidation512-volume cases start with512 verified retained classes, product policy reduces the post-settle set to128 after temporary activity, then explicit public cleanup removes128. The report preserves both stages.
- Actual baseline invalidation records2 retained Set additions; paused/idle controls record0 with the captured observer/preseed controls proven separately. Volumes2/128/512 contain48/3072/12288 retained CSS bytes. These are correctness observations on existing fixtures, not performance comparisons.
-12tests/types and report-smoke PASS; interaction54 PASS. No package lint script. Final355 source hashes record349 baseline sources with6 benchmark-only changes and6 new helper/test/repro sources.151 previously excluded product/test/foreign files and all5 native/Wasm/runtime artifacts are byte-identical. No runtime bundle, semantic CSS, dependency, fixture/snapshot, CI or release change.
- Report commands run in disposable package copies under the mandatory with-typescript-tooling-compat wrapper on macOS/Node24.20.0. Browser controls use Chromium153.0.8010.12, Firefox155.0 andWebKit26.6. Concurrent local timings remain advisory; no benchmark history reports/traces are retained in the repository. The two description-only metric edits were validated by report-smoke and the final diagnostic suites.
- Evidence: [unit](../evidence/0171-unit-tests-final.log), [types](../evidence/0171-types-final.log), [36browser controls](../evidence/0171-preparation-browser-corrected.log), [HTTP MIME](../evidence/0171-diagnostic-mime.log), [mutation16](../evidence/0171-mutation-verified.log), [invalidation32](../evidence/0171-invalidation-verified.log), [interaction54](../evidence/0171-interaction-final.log), [smoke](../evidence/0171-report-smoke.log), [hash preservation](../evidence/0171-handoff-preservation.json). Original failed script logs remain separate and are not counted as product failures.
- Reproduction: isolated-package.py benchmarks + with-typescript-tooling-compat + node --import tsx + benchmark-runtime-preparation.mjs (browser), benchmark-diagnostic-mime.mjs (HTTP), or benchmark-runtime-preparation-report.mjs <runtime-mutation-diagnostics|runtime-style-invalidation-diagnostics> (original reports and artifact/sample checks). Repros stay within disposable package trees.
- BH-0033 fixed;47 historical/37 fixed/10 unresolved,65 checked/10 blocked. All other original requirements remain. Next0172: repair BH-0034 native CSSOM traversal with real leaf/nested style, grouping and keyframe controls; then continueBH-0042/BH-0043 and other benchmark/host/gate items. No new commit/push; active goal unchanged.
