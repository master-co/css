# 0100 HTML class character references

- Starting HEAD9e1c87794; previous goal turn fixed BH-0011 with evidence, classified progress. All previous jobs terminal; existing work and other-task Site files preserved. BH-0010 original current-source failure recorded in0099-source-before.log remains the pre-fix baseline.
- Read WHATWG attribute character-reference states and canonical https://html.spec.whatwg.org/entities.json (2231entries, SHA256 d741d877ac77c4194c4ad526b5b4a19aef8dfe411ab840a466891cdbb9f362e6). Existing htmlparser/XML and Oxc XML decoders do not implement the complete HTML attribute contract.
- Rust HTML decoder handles longest named matches, optional semicolons, attribute ambiguous-ampersand exceptions, decimal/hex/uppercase X numerics, invalid/null/surrogate/overflow replacement and C1 remapping. No recursive decoding. Named lookup is a sorted static Rust table with binary search; package-local stdlib-only generator checks the pinned upstream digest before writing. Table stays below800lines/64KiB; no dependency/lockfile/config or existing generated source changed.
- HTML class values now split only at HTML ASCII whitespace and preserve quotes/punctuation/non-ASCII whitespace within a token. Unlike arbitrary-source heuristics, parsed class values represent complete DOM class tokens; this intentionally changes candidate output and resulting CSS for encoded values. JavaScript/script bodies and raw source retain the existing Oxc/raw extraction behavior. No managed directive syntax or extraction-policy changes.
- An expanded unquoted-attribute test exposed the existing XML tokenizer's consume_quote failure, which aborts subsequent extraction ([initial Rust](../evidence/0100-rust-source.log)). Replaced this HTML-only use with a bounded Rust start-tag/attribute reader supporting quoted/unquoted values, first duplicate attribute, comments, incomplete tags, raw text and script body delegation. Tokenizing once also avoids treating tag-looking attribute/comment text as scripts. This is source extraction, not a complete DOM tree builder.
- Broader browser markup control found a new implementation regression for svg:title children ([intermediate browser](../evidence/0100-html-markup-browser.log)). Added namespace/integration-point tracking, and foreign self-closing script handling. HTML/SVG/MathML representative controls now agree with browsers. No claim of exhaustive malformed HTML, foreign-content recovery, or script escaped-state tree construction.
- Initial Clippy flagged invisible data characters and a manual char predicate; generator now emits escapes for control/format characters and class splitting uses a char array. Semantic table data unchanged; final Clippy/rustfmt pass ([Clippy](../evidence/0100-clippy-complete.log)).
- Final Rust8baseline+6new tests PASS, including references, malformed numerics, nonbreaking space, unquoted/duplicate/empty class, comments/raw text, script/non-script distinction and namespace controls ([Rust](../evidence/0100-rust-tests-complete.log)). Native/tooling-Wasm artifacts rebuilt from final source; no ABI/protocol changes.
- `node scripts/with-typescript-tooling-compat.mjs node --import tsx .ai/audits/bug-hunt/repros/html-reference-browser.mjs`:23312cases, each compared with Chromium/Firefox/WebKit DOM classList and native/Wasm source extraction. Covers all2231names with semicolon/following-letter/equals variants, every numeric0..255 plus boundary/overflow values, three attribute quoting modes and representative markup controls. AllPASS ([final corpus](../evidence/0100-html-browser-complete.log)); corpus derived from separately retained canonical table, not from decoder output.
- New permanent native/Wasm scanner tests prove encoded whitespace creates block/hidden CSS, unquoted numeric produces inline-flex, and NBSP does not create false flex/grid rules. Full unfiltered tooling34files/212tests PASS, including prior BH-0010 regression ([tooling](../evidence/0100-tooling-tests-complete.log)); lint/type-check/build PASS.
- Actual Master CSS Vite static build + three-browser computed CSS checks PASS for encoded whitespace, unquoted attributes, NBSP exclusion and quoted A&B pseudo-element content ([static](../evidence/0100-static-browser-complete.log), [repro](../repros/html-reference-vite.mjs)). CLI34tests, compiler inspection4tests, isolated Vite16files/99tests PASS; known Vite relative hydration BH-0016 remains excluded/unresolved.
- Tooling Wasm size increase and unchanged runtime engine Wasm digest are recorded in0100-wasm-before/after.json. Decoder/table are tooling-only; runtime bundle/source unchanged. Plain attribute values borrow existing text, named lookup has bounded32-byte probes, and markup reader uses transient source/tag collections. No engine/runtime hot-path benchmark required or performance improvement claimed.
- BH-0010 fixed at these source/scanner/static build boundaries.44historical findings:23fixed/21unresolved;65checked/10blocked coverage unchanged. No new commit or ledger cleanup. All commands/browser processes terminal and temporary browser/build projects cleaned. Keep BH-0004 partial, root baseline failures, mjs inspection candidate and all remaining findings in scope. Integration lab0038追加驗證 remains paused pending explicit user identity confirmation.
