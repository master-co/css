# 0078 Interaction instrumentation array contract

- Scope current public ensureClassRules/deleteClassRules(readonly string[]) through original benchmark wrappers, three unique classes. Full original harness and relevant runtime methods inspected. No mutation of product code: generated page and existing Wasm sidecar only in disposable output, baseline/defer/suppress strategy options are original APIs.
- [Driver](../repros/interaction-array-controls.mjs), [source hashes](../evidence/0078-source-hashes.json). Baseline array call should count3classes/1call; deferred flush should remove same3classes using current API. Observe actual snapshot before/after and original counters; classification completed.

- **BH-0042 P3 confirmed — benchmark uses obsolete variadic class API**. [Original wrapper controls](../evidence/0078-array-controls.json), [log](../evidence/0078-array-controls.log). Three unique valid classes startabsent, ensurearray makes allpresent. Baseline deletearray removesall; counters reportadd1/remove1/calls1 instead of3classes. benchmarks/shared/interaction-cost-harness.ts:129/:157 countargs.length (onearrayargument), not arrayelements. Correct runtime public contract at packages/runtime/src/core.ts:624/:647.
- Original defer-remove and suppress-remove-during-trace wrappers each queue3classes. Explicitflush returnsflushedtrue/classCount3 withoutthrowing, but all3remain inpublicsnapshot. At harness:219, originalDeleteClassRules.apply(runtime,names) passes firststring as the readonly-arrayparameter rather than passing thewholearray. Baseline control with sameclassarray cleanscorrectly. This is benchmark mutation-strategy/instrumentation behavior, not normal runtime deletion failure.
- Fix later in owning benchmark: normalize arrayargument forcounts and pass onearray tooriginalmethod onflush, then rerun original diagnostics after asset/facade issues are also fixed. No production fix made. Other call sites using spread require separately bounded evidence; no assertion that all metrics/strategies were exercised by thiscontrol.
- Three browsercontrols PASS, tool79359 exit0, isolatedbej_1art removed; no lingering ownedbrowser. Existing sidecar copied only into temporarygeneratedoutput to reach method control. No newfixture/snapshot/dependency/commit. Benchmark has no local lint script.

- Final0077/0078 checks: [source/ownership/links](../evidence/0078-final-checks.json), [inventory](../evidence/0078-file-inventory.json), [AI context](../evidence/0078-ai-context.log).42confirmed/62checked/13blocked; goal remains active.
