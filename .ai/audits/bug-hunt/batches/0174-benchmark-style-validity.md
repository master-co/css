# 0174 Benchmark actual style and cleanup validity

- Previous goal turn made progress:0173 BH-0042 array instrumentation completed with19tests/72browser controls and original diagnostic checks. HEADbdd9f25b8; no new commit/push authorization.363 baseline source hashes match.
- Scope BH-0043: cleanup scenario computed-style assertions and actual temporary class cleanup gates. Preserve fixture/snapshot/history and product semantics; benchmark measurements with added correctness reads are not comparable to invalid old measurements.
- Current source confirms runCleanupCycle hardcodes computedStyleValid:true. The invalidation assertion still names obsolete outline/shadow classes. The mutation sample helper already uses actual Master fg:green-60/w:1px names; do not call it stale. Original0084 disabled-CSS evidence is historical; reproduce against current source before repair.
- All8 unresolved findings,10 blocked coverage,four root gates/four original candidates/native host shutdown and0038 explicit identity pause remain. Do not markBH-0043 complete from an initial readiness check or DOM-only assertion.

## Reproduction and bounded repair

- Current cleanup corpus before repair:24 controls across Chromium/Firefox/WebKit and Master static/runtime/progressive plus Tailwind static. All12 normal controls pass; all12 disabled-CSS controls incorrectly report computedStyleValid1. Initial readiness was center before disabling; the probe then resolves to start and temporary elements expand to1264px while cleanup remains legitimately successful. This reproducesBH-0043 on current source.
- Each cleanup cycle now measures alignment on the existing probe and the first appended temporary element's computed width before removal. The current fixture's width class requests1px. Master uses content-box and resolves to1px; Tailwind uses border-box and resolves to26px because padding/borders impose a minimum. Expected width includes that native minimum. The metric explicitly identifies sampled per-cycle assertions; it is not an assertion about every class/property/element. Existing competing foreground classes retain their original cascade and are not changed.
- Actual style details are preserved per cycle and aggregated with every-check success. New correctness reads are inside the measured workload, so old timings are not directly comparable. No fixture, snapshot, history or product behavior change is claimed.
- Expanded48 controls PASS:three engines/four modes, normal, all CSS disabled, width-only override with alignment still center, and width overridden after the first successful cycle. The late-failure control demonstrates that later invalid cycles cannot be masked by initial readiness or first-cycle success. Every page still restores scratch DOM.
- Invalidation now captures the actual generated page's configured temporary classes before tracing, rejects absent/malformed configuration, writes those names to diagnostic artifacts and uses them for retained-cleanup assertions. The obsolete outline/shadow literals were removed. Mutation's currently correct names remain unchanged. Idle cleanup now observes empty scratch DOM instead of assigning a constant success; idle style validation retains its actual alignment probe.

## Original diagnostics and limits

-19 existing benchmark tests/types PASS; no package-local lint script. Original mutation16/invalidation32 variants PASS with all48 preparation checks and48 array counter checks preserved. The new validator checks per-cycle details and style sample/summary equality, accepts valid results, rejects computedStyleValid0, and rejects either actual configured class being reintroduced into retained state after forced cleanup. This is48 positive gates plus144 targeted negative gate assertions; synthetic gate corruption tests the validator, independently from actual browser style-failure controls.
- Original interaction54 and report-smoke PASS. This establishes continued execution and report consistency, not universal reliability of every metric. Source inspection also finds dom-append-remove deriving computedStyleValid from appended DOM count and viewport-resize deriving it only from changed grid width. The supplementary48 controls confirmed24 false positives (12 per scenario) with CSS disabled;24 normal controls passed. The additional repairs and final controls are described below.
- Commands: isolated-package.py benchmarks + with-typescript-tooling-compat + node --import tsx with benchmark-style-validity.mjs, benchmark-style-validity-report.mjs <runtime-mutation-diagnostics|runtime-style-invalidation-diagnostics>, benchmark-runtime-report.mjs <interaction-cost|report-smoke>, and benchmark-style-other-scenarios.mjs. Unit/types use scoped benchmark commands. Local concurrent macOS/Node24.20.0 and the existing three browser binaries provide correctness evidence only.

## Other scenario repairs

- The exact existing viewport measurement function is exported only from a disposable copy for independent browser controls. Without CSS, its grid container still changes width when the viewport changes, so the former predicate reports style success. The append/remove function similarly reports DOM insertion success as computed-style success.
- Append/remove now checks the first appended element's computed min-height72px and the existing center-alignment probe before removal. Viewport measurement records actual grid display, four resolved columns and center alignment before/after resizing, and also requires the existing width change. These are explicit sampled fixture assertions, not comprehensive visual equivalence tests. No changes to viewport dimensions, fixture classes or scenario counts.
- Expanded72 controls PASS across three engines, four modes and both scenarios: normal, all CSS disabled, and selective min-height/grid-column overrides with alignment still center. Selective failures are rejected despite successful DOM insertion or changed viewport width. Original48/24 before/fixed runs are historical subsets, not added to the final total. The complete final actual-browser corpus is48 cleanup plus72 append/viewport controls (120total).
- Final original interaction54 variants PASS after both repairs.19tests and final TypeScript validation PASS. Final cleanup48 controls and report-smoke also rerun successfully on the same final benchmark sources. Final mutation16/invalidation32 repeats also PASS, including all48 preparation/array/style artifact checks and144 negative gate assertions. All commands are terminal with isolated trees removed; report generation alone is not the validity oracle.

## Evidence and preservation

- [Cleanup before24/12FAIL](../evidence/0174-style-before.log), [cleanup final48PASS](../evidence/0174-style-verified.log), [other before48/24FAIL](../evidence/0174-other-scenarios.log), [other final72PASS](../evidence/0174-other-scenarios-final.log), [unit19](../evidence/0174-unit-tests-final.log), [types](../evidence/0174-types-final.log), [interaction54](../evidence/0174-interaction-final.log), [mutation final](../evidence/0174-mutation-final.log), [invalidation final](../evidence/0174-invalidation-final.log), [smoke](../evidence/0174-smoke-final.log).
-366 final source hashes:363 baseline sources,six benchmark-only edited files and three new repro scripts. The first snapshot is retained separately because append/viewport repairs followed cleanup/gate repair.151 excluded product/test/foreign files and five native/Wasm/runtime artifact byte streams remain unchanged. No product CSS, fixture/snapshot, dependencies, lockfile, CI/release, commit or push changes.

## Final handoff

- BH-0043 fixed;47 historical/40 fixed/7 unresolved,65 checked/10 blocked. Remaining BH-0004/BH-0029/BH-0037–0041 and all original graph/host/gate/candidate requirements are preserved. Paused batch0038 still has no explicit identity confirmation.
- Next0175:BH-0038 startup benchmark passive-import versus executable-bin contract (distinct from paused batch0038). All0174 sessions terminal; HEADbdd9f25b8; no commit/push. Goal active. [Final checks](../evidence/0174-final-checks.json), [verification summary](../evidence/0174-verification-summary.json), [source preservation](../evidence/0174-handoff-preservation.json).
