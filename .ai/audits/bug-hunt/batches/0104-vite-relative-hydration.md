# 0104 Vite relative hydration URLs — BH-0016 fixed

- Starting HEAD5dc6f2ad1. Previous goal turn made progress by committing completed0101–0103 repairs. Current worktree contained only0092 compiler partial work and foreign Site files; all preserved. No commit in this continuation. Integration lab0038追加驗證 remains paused until explicit identity confirmation.
- Read current Vite manifest/AI, pre-render hooks, shared asset href helper, runtime/manifest preload consumers, server external-manifest callback, runtime URL resolution, nearby tests and deep data-flow/testing policy. Existing toAssetHref already owns page-relative asset resolution, so no new URL abstraction or runtime behavior is required.
- Original0020 regression reproduced on current source: root control resolves, pages/nested.html points to nonexistent pages/assets hydration JSON ([before](../evidence/0104-relative-before.log), exit1). This is the original BH-0016, not a new issue or test setup failure.
- Repair in packages/vite/src/plugins/pre-render.ts: use existing toAssetHref with the current HTML path. transformIndexHtml passes its path context; transform derives a normalized root-relative path from its input ID. The renderer callback closes over that call's page path, so shared manifest bytes retain distinct per-page URLs. Asset emission also honors an explicitly empty assetsDir instead of silently using assets; filename joining omits the empty directory segment.
- Default/absolute/CDN bases continue to use the shared helper's existing behavior. Development uses the existing root hydration middleware URL. No new public API, Rust semantics, manifest bytes, runtime module or CSS rule change. URL spelling for root relative output may omit an unnecessary ./ prefix.

## Verification

| Check | Evidence |
|---|---|
| Original regression plus new matrix and pre-render tests | 3files/12tests PASS; [focused](../evidence/0104-focused.log) |
| Full Vite suite | 18files/106tests PASS, including existing actual Chromium dev HMR/runtime-order checks; [full](../evidence/0104-vite-full.log) |
| Package lint/types/build | All exit0; [lint](../evidence/0104-lint.log), [types](../evidence/0104-types.log), [build](../evidence/0104-build.log) |
| Actual built package / three browsers | 36controls PASS:3bases ×4page paths ×Chromium/Firefox/WebKit; native JSON import requests resolve and two newly introduced classes trigger observed runtime updates on every page. One runtime style, no page/console errors or missing assets; [driver](../repros/vite-relative-hydration.mjs), [results](../evidence/0104-built-browser.log) |
| Existing Vite example | TypeScript and production build PASS, output /tmp/mastercss-0104-vite-example; [build](../evidence/0104-example-build.log). Example is a static-mode control; progressive behavior is proven by the dedicated built-browser matrix |

- New packages/vite/tests/bug-hunt-hydration-url-matrix.test.ts performs six actual multi-page builds: ./, empty, root-absolute and CDN base, default/custom/empty assetsDir, progressive and pre-render modes. Four pages include root, nested, deep index and a page sharing the asset directory prefix. Each resulting URL resolves to emitted JSON containing block; all pages share one identical asset. Existing regression and fixtures/snapshots were not changed.
- Built-browser driver tests deployed /mount/ paths with ./, empty and /mount/ base, four HTML paths and custom/files assetsDir. It imports packages/vite/dist/index.js and uses real browser module loading through local route fulfillment. It does not claim real CDN infrastructure or arbitrary deployment rewrite behavior. All pages/browsers close and its temporary project is removed in finally.
- No runtime benchmark: change is build-time URL formation, not hydration execution, observation, CSSOM or runtime bundle code. Runtime/engine source and engine Wasm hash remain unchanged. No dependencies, lockfiles, generated contracts, CI/release or existing fixtures/snapshots changed. No package without a lint script was modified.
- Final [hashes](../evidence/0104-source-hashes.json), [inventory](../evidence/0104-file-inventory.json), [checks](../evidence/0104-final-checks.json), [AI context](../evidence/0104-ai-context.log). All command handles are terminal; compiler0092/Site hashes preserved, index empty, no new commit/push.
- BH-0016 fixed:44historical findings,26fixed/18unresolved;65checked/10blocked coverage unchanged. Four existing root check failures, benchmark/nested-host/platform blockers, BH-0004 partial repair and the mjs inspection candidate remain unfinished. Next bounded batch: BH-0017 Webpack relative runtime asset URLs on nested pages, using its original0021 evidence; do not resume the separately paused0038 lab validation.
