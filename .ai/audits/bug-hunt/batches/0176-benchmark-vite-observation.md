# 0176 Benchmark Vite source observations

- Previous goal turn made progress:BH-0038 fixed with8startup variants/292samples andfour actual CLI entry controls.368 baseline source hashes match;HEADbdd9f25b8;no commit/push authorization.
- Scope BH-0041: count unique actual nonempty scanned source paths, not hook callbacks; preserve scanner semantics and query/repeated-call behavior. Also classify/fix the four unsampled startup metrics recorded0175 with actual environment-bound hook evidence.
- Current usageGraph skips virtual IDs and scanner.scanModule applies the real eligibility/exclusion rules. scan returns whether classes changed, not whether scanning occurred. Counting only true returns would therefore also be wrong. Observe actual completed nonempty scan calls and deduplicate normalized source paths.
- Current benchmark instrumentation decorates shallow copies; scopePlugins.applyToEnvironment closes over the original plugin. Verify lost transform/load/generateBundle observations separately from scanner operation. Product source remains unchanged.
- All6 unresolved findings/10blocked/four root gates/four original candidates/host shutdown andpaused batch0038 remain. Missing timing fields are not verified zero; source-count definitions must remain explicit.

## Reproduction and repair

- Current four-fixture reproduction fails all four: reported source-file-count1 versus two independently observed unique source paths (index.html and src/main.js), plus three missing hook timings. Each fixture actually completes three scans: repeated HTML plus JS; false changed results still represent scan work. Historical0072 had six callback counts for the same two files/three scans. This remains BH-0041, not a new product finding.
- The shared benchmark counter observes the actual scanner scan method after successful completion, retaining nonempty unchanged work, matching the diagnostic scanner root and deduplicating query-stripped normalized paths. Eligibility and exclusion remain entirely product-owned through scanModule. The exact method descriptor is restored in finally; concurrent nested counters are rejected because diagnostics run in isolated processes. These are normalized logical paths, not symlink-realpath or disk-inode counts.
- The common instrumentation now decorates the owned plugin factory objects in place. Environment closures therefore retain transform/load/generateBundle observation; receiver, arguments, results, error identity and hook object metadata are preserved. Timing descriptions explicitly include declined hook requests; generateBundle time does not claim to contain every output hook. No Vite/scanner product source was modified.
- The startup declaration vite-css-project-entries-import-ms had no scheduled probe; it is removed with an explicit report limitation. The existing vite-css-project-manifest-import-ms still measures the actual combined project module import. Three missing hook timing metrics are now sampled. No missing value is invented as zero and no synthetic duplicate import is added.
- Both build and startup publish scanned-sources.json with the actual sorted relative path list, alongside CSS artifacts. CLI source counts remain their explicit single fixture input; they do not inherit Vite counting semantics.

## Verification and classified intermediate failures

- Final four-fixture independent observations PASS: source counts2, source artifact lists match actual scans, all three missing hook fields present. Instrumented and uninstrumented actual Vite builds in each identical workspace produce byte-identical CSS.
- Twelve real scanner controls PASS: repeated content, absolute/query variants and unchanged scans count once per file; empty content, relative/absolute excluded paths, CSS, non-source extensions, style queries and NUL virtual requests do not count. Independent outer observation confirms which calls actually scan. Removing the counter restores the exact method descriptor, and rerunning all controls without observation produces identical CSS.
- Original build suite completes8variants/100finite samples/100matching summaries; original startup suite completes8variants/304finite samples/304matching summaries. Every current declared metric has samples. All16variant CSS marker/raw/gzip/brotli checks PASS; all eight Vite variants list exactly index.html and src/main.js and have the three hook timings. No claim of universal CLI/Vite CSS equality or cross-workspace hash equality is made.
-22benchmark tests PASS, including three new focused regressions for counter boundaries, environment-closure identity/hook semantics, and actual plain-Node helper imports. TypeScript noEmit and report-smoke PASS; no package-local lint script. Five new JavaScript modules/repros pass syntax checks.
- Initial TypeScript unknown-handler error was fixed by narrowing; switching startup helper loading exposed a missing benchmarkRoot import, also fixed. Original logs remain separate. These are intermediate implementation failures, not existing product/test defects.
- First attempted startup implementation serialized functions using toString; the real tsx-launched child injected an unavailable __name helper, although the Vitest VM test passed. That implementation was replaced with directly imported plain .mjs modules shared with build diagnostics; the regression now invokes plain Node. Final original startup suite proves the actual child path. No fragile transpiler-helper shim is retained.
- First real scanner control script completed all12behavior checks but called nonexistent scanner.destroy during cleanup. Corrected script uses existing observer restoration/listener cleanup and all final controls PASS. This was a repro script error, not a scanner lifecycle defect.

## Limits, preservation and handoff

- Cold imports, extra benchmark observer module loading, path normalization and timing wrappers add diagnostic overhead. Hook timings may overlap and are not an additive performance profile. These local concurrent macOS/Node24.20.0 runs establish measurement correctness only; historical timings/counts are not comparable performance baselines. The four-fixture source corpus does not prove full product scan coverage.
-374final source hashes:368baseline, four existing benchmark-only files changed, two new common JS modules, one new regression test and three new repro scripts.151 excluded product/test/foreign files, five existing Wasm/runtime artifacts and three existing CLI built files remain byte-identical. No fixtures/snapshots/dependencies/lockfile/CI/release changes; no generated reports, traces, pages or workspaces retained. Prior0175 handoff preserved verbatim in progress-history-0176.md.
- BH-0041 fixed;47 historical/42fixed/5unresolved (BH-0004/BH-0029/BH-0037/BH-0039/BH-0040),65checked/10blocked. Four root gates, four original candidates, Vite native immediate-close shutdown limitation and all original graph/host/Sass maps/virtual/reference/watch/recovery/base/assets/renderBuiltUrl/SSR/lifecycle/Nuxt/Webpack requirements remain unfinished. Paused batch0038 additional verification still requires explicit identity confirmation; none received. Recording an obstacle does not complete it.
- Next0177: reproduce BH-0039 specificity calculation against current benchmark source, focused universal/:is/:not/:has and selector-list controls, then original structure report validation. Preserve fixed issue IDs and remaining BH-0037/BH-0040 and full host migration requirements.
- All commands terminal;HEADbdd9f25b87305ab47437c30295d59ca6bb88f317,index empty,no commit/push;goal active. [Final checks](../evidence/0176-final-checks.json), [source preservation](../evidence/0176-handoff-preservation.json), [verification summary](../evidence/0176-verification-summary.json).

## Evidence

- [Before four failures](../evidence/0176-vite-before.log), [final four CSS comparisons](../evidence/0176-vite-final.log), [12real scanner controls](../evidence/0176-source-controls.log).
- [Build8/100](../evidence/0176-build-final.log), [startup8/304](../evidence/0176-startup-final.log), [tests22](../evidence/0176-unit-tests-final.log), [types](../evidence/0176-types-final.log), [report smoke](../evidence/0176-report-smoke.log).
- [Intermediate startup serialization failure](../evidence/0176-startup-report.log), [repro cleanup error](../evidence/0176-source-controls-first.log), [initial types](../evidence/0176-types-first.log), [missing import](../evidence/0176-types-final-first.log). Earlier successful runs remain historical; final evidence uses the plain-JS modules.
