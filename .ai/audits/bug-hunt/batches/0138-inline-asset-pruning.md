# 0138 Inline asset reachability

- Previous goal turn made progress: completed0137 validation/ledger and preserved130prior source files. This batch investigates actual inline output reachability; BH-0004 and the full goal remain incomplete. HEAD087655705.0038identity pause unchanged.

## Evidence before the fix

- [Initial six-build probe](../evidence/0138-pruning-before.log): an unused managed inline import emitted two CSS descendants, the35byte URL-base anchor and a62byte SVG; a used leaf emitted two already-inlined CSS descendants plus the anchor. The CSS text itself had already been tree-shaken from the unused JavaScript; the defect was unconditional publication, not a missing JavaScript tree-shaking annotation.
- Pure Vite also left the62byte SVG from an unused ordinary inline import. That is an existing host baseline, not a new Master CSS finding. The initial probe's blanket zero-CSS-file assertion additionally counted the deliberately retained URL-base anchor. Final classification distinguishes the anchor overhead, pure-host resource retention and the genuinely unreachable managed descendants.
- Two focused regressions [failed before the change](../evidence/0138-pruning-tests-before.log): fully inlined descendants were still published, and an unused inline import retained plugin-owned assets. Four prior inline tests passed. A third regression protects an active string's shared anchor while removing a different unused string and its resource.

## Change and validation

1. The Vite host reuses the Rust compiler's public source import/resource analysis, already used for URL rewriting, to traverse references from the final inline root. It queues only owned CSS assets still referenced transitively and resource files referenced by that reachable output. It does not parse CSS or infer stylesheet semantics in TypeScript.
2. Publication waits until generateBundle. A root is used only if its controlled CSS token appeared in an actual rendered chunk. Unused roots publish no pending assets; their anchors are removed only when no used root shares the same emitted filename. This preserves active shared anchors, retained import files and lazy chunks.
3. Final [Vite121tests](../evidence/0138-vite-tests-final.log), [lint](../evidence/0138-vite-lint-final.log), [types](../evidence/0138-vite-types-final.log), [build](../evidence/0138-vite-build.log) and [original example build](../evidence/0138-example-build.log) PASS. Three new tests; no fixtures/snapshots, dependencies or public exports changed.
4. [Expanded actual build/browser probe](../evidence/0138-pruning-final.log):10builds/30Chromium-Firefox-WebKit browser comparisons PASS. Five scenarios each compare pure Vite and managed output: unused, used leaf, used resource, used plus unused strings, and dynamically imported string/resource. Eight asset checks pass and two pure-Vite resource-retention observations remain explicitly BASELINE. Managed unused emits zero assets; used leaf/shared emit only the35byte anchor; resource/lazy emit anchor plus required SVG. All browser colors/image decodes pass with no missing assets/page errors; no automatic style is applied before insertion.
5. [Original inline delivery matrix](../evidence/0138-inline-control.log) rechecks CSS/Sass, import qualifiers/external boundaries, opaque author strings, virtual sources, resource bases, IIFE/UMD and independent/mixed insertion contexts. All14builds/84browser comparisons PASS after the publication change.
6. [JS-only constructable watch](../evidence/0138-watch-control.log):3builds/18browser comparisons PASS, preserving cached CSS re-registration and final resource publication lifecycle. This is build watch, not development HMR.

## Scope and remaining work

- The35byte anchor remains for used strings, including an import-free leaf; this is explicit overhead, not claimed eliminated. Root compilation/source analysis still happens before knowing whether a string survives tree-shaking, so no build-time CPU/memory improvement is claimed. This batch removes redundant output publication only.
- All five compiler/engine/runtime artifacts must retain0137 hashes; no Rust/ABI/runtime change or benchmark rerun. Root API census/package-golden failures retain their0137 evidence; no public export surface changed and no golden refresh. Other root gates remain open. No public Site code/docs changed in this batch.
- Continue CJS/SSR/workers and multi-output formats, cycles/root back-references, early WebKit automatic CSS timing and finer anchor/unused-work optimization. Then original Sass source-map attribution, broader Modules/preprocessors/PostCSS, virtual resources/reference, multiple roots/local-compose/HMR and Webpack publication.
- Original actual corpus still has3Webpack build/12browser failures (0133); legacy corpus21PASS/39FAIL (0125).45historical findings33fixed/12unresolved,75coverage65checked/10blocked,4root gates/4unclassified candidates and0038identity pause remain. All unverified requirements stay unfinished. No commit/push.
