# Batch 0193: Compiler and MCP ESM discovery

- In progress; inspect the original compiler diagnostics and MCP source discovery candidate. Default glob lacks `.mjs` in both owning host files, but product classification requires actual inspection/extraction/trace controls. No source changes yet. Original BH-0012/BH-0018 concern tooling/CLI; do not silently reuse their completed scope. 0038 identity pause and all remaining0192 requirements persist.

- BH-0059 confirmed P2: default compiler inspection and MCP scan/extract/trace omit `.mjs` and its generated CSS/occurrences. Explicit `.mjs` and `.js` succeed. New source tests: compiler1FAIL/2PASS, MCP3FAIL/6PASS. Real built compiler and MCP SDK stdio:4FAIL/8PASS. See `0193-compiler-before.log`, `0193-mcp-before.log`, `0193-built-before.log`; no mocks. This is independent of the already fixed CLI/tooling discovery constants.

## Repair and retained attribution failure

- Completed `.mjs` discovery fix in `packages/compiler/src/diagnostics/index.ts` and `packages/mcp/src/scan.ts`; only insert the missing extension into the existing default glob. Preserve explicit paths and node_modules exclusion. Source-file selection only; no Rust extraction/parser semantics, fixtures, snapshots, dependencies, public API, CI/release, browser runtime or Site edits.
- New source tests cover default / explicit `.mjs` / explicit `.js` for compiler inspection and MCP scan/extract/trace; ignored node_modules class never emits. Before:4FAIL/8PASS. After:11PASS/1FAIL; remaining failure is exact trace ownership, not file discovery or missing CSS.
- Full compiler330PASS, tooling215PASS; MCP42PASS/1FAIL. Both modified packages' lint, types and build PASS. Commands use `node scripts/with-typescript-tooling-compat.mjs pnpm --filter @master/css-{compiler,mcp,tooling} exec vitest run`; lint/type-check/build run for compiler and MCP. No package test script invoked because those rebuild native/Wasm; existing artifacts were hashed and reused. Builds ran only after consuming suites ended.
- Actual built compiler plus real SDK stdio MCP: original4FAIL/8PASS becomes11PASS/1FAIL. Default extraction now returns entry.mjs; default CSS contains display:block. Remaining trace includes unrelated control.js. [Before](../evidence/0193-built-before.log), [after](../evidence/0193-built-after.log), [driver](../repros/inspection-mjs-discovery.mjs). No tool writes or previews invoked.
- BH-0060 confirmed P2: separate built compiler [two plain .js control](../repros/inspection-source-attribution.mjs) reports b.js's `block` and invalid `text-decoration:bad()` under a.js, and warning filePath is a.js. [Evidence](../evidence/0193-js-attribution.log). This excludes `.mjs` extension handling as its cause. Retain exact failing assertions; do not weaken them to make the suite green.
- Current code runs `scanSourceFile` concurrently against one scanner and computes per-file discoveries from global before/after sets. This explains contamination; simply serializing may still lose repeated occurrences or safelisted/native ownership. Next batch must establish complete per-file attribution requirements and use existing Rust/tooling extraction/classification rather than create a TS semantic fallback.
- 60 historical / 55 fixed / 5 unresolved; BH-0059 fixed, BH-0060 unresolved. Original unclassified candidates remaining: parallel Webpack shared-dist and legacy native compose. All0192 remaining scope, four root gates,0190three failures, full graph/hosts/maps/benchmarks/Site and0038 identity pause remain. No commit/push. Final current source/artifact preservation and full remaining list: [checks](../evidence/0193-final-checks.json).
