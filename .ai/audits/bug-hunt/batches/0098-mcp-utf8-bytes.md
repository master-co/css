# 0098 MCP and inspection UTF-8 byte metadata

- Starting HEAD9e1c87794;0092partial/0096–0097 work retained. Original BH-0020 reproduces Unicode preview before/after sizes1/2 instead of2/4 while ASCII passes ([before](../evidence/0098-mcp-bytes-before.log)).
- MCP context now uses UTF-8 byte lengths for stored changes and aggregate summaries. Actual temp-file apply tests verify advertised counts against fs.stat sizes, covering ASCII, accented text, emoji, Chinese, combining characters, CRLF, empty output, new files, multiple changes and unchanged files omitted.
- Same bug exists in MCP directive-format content/file metadata and rendered CSS size. Added regressions both FAIL before fixes (38vs44 and52vs66), then pass with UTF-8 byte lengths ([surface before](../evidence/0098-mcp-surfaces-before.log)). Fixed in existing owning functions; no formatting, extraction or generated CSS semantic changes.
- Actual built MCP stdio exposed upstream createInspectionReport size52 while preview correctly reported66 ([first stdio](../evidence/0098-mcp-stdio-bytes.log)). Traced to mastercss-diagnostics encode_utf16().count(); corrected Rust owner to String::len(), preserving the single report implementation and avoiding MCP-specific result overrides.
- New Rust test fails before5vs12, then passes for ASCII/empty/Unicode with CSS text included or omitted ([Rust before](../evidence/0098-diagnostics-before.log)). One existing test explicitly asserted UTF-16 size2 for emoji; intentionally corrected its name/assertion to UTF-8 size4. Initial full Rust failure reflects this obsolete unit expectation ([initial full](../evidence/0098-diagnostics-tests.log)); no fixtures/snapshots changed.
- Final Rust5tests/Clippy/rustfmt PASS. Native Node inspection2files/4tests PASS, including 6 byte/text-inclusion cases ([compiler](../evidence/0098-compiler-tests-final.log)). Initial new test wrongly assumed Wasm inspection support; existing binding adapter explicitly requires native Node for inspection. Removed unsupported test variant, not a product workaround; Wasm compiler artifact rebuild succeeds but no Wasm inspection claim ([initial test](../evidence/0098-compiler-tests.log)). ABI/generated protocol unchanged.
- MCP3files/24tests PASS after native rebuild, compiler/MCP lint and types PASS; MCP build and native/compiler-Wasm artifacts rebuilt successfully. Existing SDK lifecycle/schema/preview/stale/error controls included. BH-0031 concurrency remains unresolved; this batch does not claim its fix.
- `node .ai/audits/bug-hunt/repros/mcp-utf8-bytes.mjs` exercises actual built child stdio, render, in-memory/file directive format, generated-CSS preview and token apply in disposable files. Render/generated CSS both66bytes; format44→39; existing generated output13→66; preview non-write and actual resulting stat sizes PASS ([final stdio](../evidence/0098-mcp-stdio-bytes-final.log)). Child/client/transport and temp directory cleaned.
- BH-0020 fixed across these byte metadata surfaces, including Rust inspection consumed by MCP.44historical confirmed findings:21fixed/23unresolved;65checked/10blocked coverage unchanged. Other metric problems, BH-0004partial and Integration lab追加驗證 remain unfinished. No new commit; final source hashes and inventory retained with this batch.
