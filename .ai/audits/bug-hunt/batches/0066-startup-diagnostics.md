# 0066 Startup benchmark diagnostics

- Scope: original startup suite, BENCHMARK_ROUNDS=1, four static fixtures × CLI/Vite variants; report success only for actual completed variants. Shared helper/entry/suite read, probes/imports/commands and reset/write destinations inspected. All writes live in own isolated benchmarks/.results; published package entries read through existing symlinks. No product edits/builds, snapshots/history or install.
- [Driver](../repros/startup-diagnostic-report.mjs), [source hashes](../evidence/0066-source-hashes.json). Existing output marker gates preserved. Original suite failed before first variant result; cause classified below. Correctness baselines remain current by earlier source hashes; this is report availability/contract audit, not performance comparison.

## BH-0038 · P2 · 已確認：startup benchmark 將 CLI bin 當被動模組匯入

- Original command: `python3 .ai/audits/bug-hunt/repros/isolated-package.py benchmarks node /Users/aron/master/css/scripts/with-typescript-tooling-compat.mjs node /Users/aron/master/css/.ai/audits/bug-hunt/repros/startup-diagnostic-report.mjs`. [Log](../evidence/0066-startup-diagnostics.log): suite1FAIL, first minimal/CLI variant at `import-cli-bin-module-import-ms.mjs` prints help/exits1; no report or completed variant, remaining7variants not reached. Parent and child completed; disposable directory removed.
- Root: `benchmarks/shared/startup-diagnostics.ts:218` schedules an import-only probe of `dist/bin/index.js`; probe generator:383–406 invokes import without CLI arguments and expects execution to return before printing the metrics marker. Current `packages/cli/src/bin/index.ts:5` intentionally awaits runProgram; core uses process.argv and Commander requires a subcommand. Existing built bin/core inspected and implement the same contract.
- [Minimal control driver](../repros/startup-bin-control.mjs), [control log](../evidence/0066-bin-controls.log), [structured results](../evidence/0066-bin-controls.json): same published bin with valid generate args exits0 and produces297bytes CSS with expected selector; bin import without arguments exits1 and never prints audit marker; passive core import exits0 and prints marker. All3assertions PASS. Commands run only in disposable benchmark workspace; generated CSS is a minimal control, not substitution for original four fixtures.
- Classification: benchmark tooling bug, not CLI generation failure, audit-script error, missing dependency, or environment denial. Merely passing --help would still terminate before marker and would not measure passive import; no product change was made.
- Fix direction (not implemented): use an entry that can be imported without executing the CLI for import-only timing, and keep executable-bin timing as a command measurement. Existing core-import and full-command controls demonstrate both contracts.
- Original startup report still unfinished underBH-0038; not counted as complete because the cause is documented. Vite startup variant can be tested independently next through existing run-diagnostic.ts without mutating this failure. Benchmarks has no package-local lint script; no workspace package source/test changed.

- Final ownership/hash/link/budget reconciliation: [0066 checks](../evidence/0066-final-checks.json), [inventory](../evidence/0066-file-inventory.json), [AI context check](../evidence/0066-ai-context.log).

- Final checks PASS:5867source snapshot entries,38findings,62checked/13blocked; only superseded0044/0047Site hashes differ; AI context2392files PASS. First ownership-check script omitted the23already documented tests because prior inventory listed only ledger files; corrected inventory from changes.md list, no new package tests or product finding. All isolated processes/directories cleaned.
