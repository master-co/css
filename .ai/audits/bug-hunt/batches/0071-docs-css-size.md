# 0071 Docs page CSS size

- Bounded independent HTTP/report checks while0070 build diagnostics runs. Existing suite/shared/input read. Original8public page URLs fetched read-only; `UPDATE_BENCHMARK_SNAPSHOT=false` explicitly keeps writes in disposable benchmarks/.results, no existing snapshot or input change. No external writes/login or deployment. Public-site availability and collector defects classified separately.
- [Driver](../repros/docs-css-report.mjs), [source hashes](../evidence/0071-source-hashes.json). Original public suite1PASS,8pages/45assets collected; Source fetchResource intentionally allows404 then counts response bytes; local HTTP controls will determine observed effect separately from current public-site network outcome.

- First local-control run had a harness assertion error: Chromium retains a stylesheet object for the404load, so document.styleSheets.length is not a reliable success check. Expected black color already passed. Corrected observer to actual cssRules count and computed style; original [failed harness log](../evidence/0071-http-controls-initial.log) preserved. No finding inferred from that failed assertion.

## BH-0040 · P3 · 已確認：404 HTML 錯誤頁被報為 CSS bytes

- Original command: `python3 .ai/audits/bug-hunt/repros/isolated-package.py benchmarks node /Users/aron/master/css/scripts/with-typescript-tooling-compat.mjs node /Users/aron/master/css/.ai/audits/bug-hunt/repros/docs-css-report.mjs`; [log](../evidence/0071-docs-css-size.log), [live collection](../evidence/0071-docs-css-snapshot.json). All8public pages collected45assets. These are point-in-time fetch results, not proof that each resource is valid CSS or equivalent page/UI work; no comparative size ranking published.
- [Local HTTP driver](../repros/docs-css-http-controls.mjs), [log](../evidence/0071-http-controls.log), [evidence](../evidence/0071-http-controls.json). Same original collector, unmodified8page input mapped at fetch boundary to real loopback HTTP server. Valid200text/css26bytes → CSS26bytes, Chromiumred/1rule.404text/html1079bytes → collector still resolves and reports CSS1079bytes without status/error marker; Chromiumblack/0rules.500control correctlyrejects. Rawerrorbody contains no CSS; collector raw/brotli52bytes represent HTML, not styles.
- Root `benchmarks/docs-page-css-size/shared.ts:166` exempts404 from failed-fetch rejection, while:132 unconditionally summarizes the body and result schema dropsHTTPstatus/content-type. Expected behavior for CSS-size report: failed stylesheet fetch must be rejected or explicitly excluded/flagged, not silently included as CSS. Controls establish the difference without depending on any public-site404.
- Classification benchmark tooling, not network limitation or Master CSS product rendering issue. First stylesheet-object-count assertion was harness error, preserved above; corrected test checks actual CSS rules and computed style. No source/snapshot/input edit was used.
- Fix direction (not implemented): preserve/report resource validity and avoid including HTTPerror documents as CSS; existing500error handling provides a rejection control. Live suite execution is complete, but trustworthy failure-aware collection remains unfinished underBH-0040. Benchmarks has no local lint script; local servers/browsers and isolated processes cleaned.

- Final reconciliation: [source/ownership/link/budget checks](../evidence/0072-final-checks.json), [inventory](../evidence/0072-file-inventory.json), [AI context check](../evidence/0072-ai-context.log).
