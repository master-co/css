# 0170 Benchmark runtime snapshot readers

- Previous turn made progress:BH-0032 fixed with binary delivery/accounting,42 browser controls and original report evidence.344 source hashes match. HEADac24c0f96, no commit/push.
- BH-0033 spans both stale state readers and private retained-set/observer/cleanup manipulation. Migrate readable state and readiness guards to public snapshots; retain every unconverted preparation/instrumentation requirement. Do not call the finding fixed until all its behavior is verified. BH-0034 CSSOM counting andBH-0042 array arguments remain distinct.
- Full original scope,11 unresolved findings,10 blocked coverage rows,4 gates,4 candidates, host shutdown limitation and0038 identity pause remain.

## Reproduction and reader contract

- Twelve actual delivery/interaction runtime/progressive browser controls fail before the change. Real snapshots contain49/54 output rule entries and1687/1940 UTF-8 CSS bytes; old readers report zero. Progressive delivery's private-instance hook happens to report adoption correctly, while the global interaction reader reports0. The distinct failures are preserved in0170-readers-before.log.
- One dependency-free public snapshot summarizer is serialized into generated page scripts. It reads immutable classRules, usageCounts, layers and hydration; page hosts add DOM-specific counts separately. Marker classes with usages but no rules remain in usageCounts and are excluded from generated class-name lists. Retained rules are deduplicated by layer/key and measured as UTF-8 bytes.
- runtime-generated-rule-count now has an explicit consistent definition: output entries across public snapshot layers, including keyframes blocks, excluding CSSOM layer wrappers. This fixes the previous ambiguous mixture of class-name counts and native CSSOM counts. Native CSSOM metrics remain a separate BH-0034 requirement. Descriptions for directly affected metrics are updated; original snapshots are not rewritten or compared as an unchanged metric definition.
- Delivery, lifecycle and interaction state readers, observe-time payload state and four readiness guards use the public snapshot. Real type imports exposed two old conflicting global declarations in delivery-modes.ts; remove those declarations and use the package's public globals. The first type-check failure was an implementation error, corrected before final validation.

## Verified bounded results

- Four new contract tests plus five prior payload tests PASS (9 total). Tests cover non-ASCII bytes, shared retained rule identity, generated rules versus marker class usages, independent output-layer counts, missing runtime/static state, explicit fallback diagnostics, immutable input ownership and executing the serialized browser function without module closures. Final benchmarks TypeScript noEmit check PASS; no package-local lint script exists.
- Twelve delivery/interaction browser controls PASS after the change; twelve original lifecycle page controls also PASS with runtime reader values independently compared to the actual public snapshot.
- Six actual DOM retention controls PASS across Chromium/Firefox/WebKit and runtime/progressive modes: z:17891 has computed z-index17891 and one usage, a marker has a usage but no generated rule; removal enters retained state, reader observes one retained rule/24bytes, and public deleteClassRules([name]) removes it. This proves the reader and public-delete control; it does not silently replace the existing broken benchmark force-cleanup helper.
- Six negative hydration controls corrupt only generated disposable SSR style text. Runtime publicly reports fallback with a failure reason; both readers correctly report progressiveAdopted0 and actual regenerated CSS/rules. Positive progressive controls report1. No fixture/snapshot or product runtime edits.
- Original interaction-cost now completes54 variants, including all progressive modes; original lifecycle initial-load4 modes, delivery16 and progressive-diagnostics4 complete with nonzero runtime rule/byte metrics and correct adoption. Report-smoke passes. Report generation does not prove remaining scenario validity: BH-0042 array/deferred removal andBH-0043 hardcoded validity checks remain.
- Original runtime-mutation-diagnostics now fails earlier with visible retained temporary classes after its supposed forced cleanup. The existing helper calls an absent private flushRetainedClassRules method; no flush occurred. Previously the stale reader hid these classes with zeros. This is remaining BH-0033 cleanup work, not a product regression.
- Original runtime-style-invalidation-diagnostics also completes, but its private retained-set instrumentation, retained-volume seeding and observer pause code still reads inaccessible fields. Successful report generation does not complete those experiments or validate their zero-valued preparation counters. Continue exact preparation controls in0171.

## Evidence and handoff

- Reproduction: isolated-package.py benchmarks + with-typescript-tooling-compat + `node --import tsx .../repros/benchmark-runtime-readers.mjs`. Before12 FAIL, after12 PASS; BH_FALLBACK=1 adds6 PASS. [Before](../evidence/0170-readers-before.log), [after](../evidence/0170-readers-first-fixed.log), [fallback](../evidence/0170-fallback-browser.log). Separate `benchmark-retained-readers.mjs` supplies6 actual retention/delete controls: [log](../evidence/0170-retained-browser.log).
- Original scripts run in disposable copies using `benchmark-runtime-report.mjs <suite>`: [lifecycle](../evidence/0170-lifecycle-fixed.log), [delivery](../evidence/0170-delivery-fixed.log), [progressive](../evidence/0170-progressive-fixed.log), [invalidation](../evidence/0170-invalidation-first.log), [smoke](../evidence/0170-report-smoke.log). Interaction/mutation used the preceding Wasm report runner: [interaction](../evidence/0170-interaction-first.log), [mutation failure](../evidence/0170-mutation-first.log). The new runner additionally asserts nonzero runtime outputs and correct hydration samples where those metrics exist; lifecycle generated pages are inspected in all three engines.
- `node scripts/with-typescript-tooling-compat.mjs pnpm --filter ./benchmarks exec vitest run tests`:9 PASS. `pnpm --filter ./benchmarks exec tsc --noEmit --incremental false --project tsconfig.json` under the same wrapper:PASS. [Tests](../evidence/0170-unit-tests.log), [final types](../evidence/0170-types-final.log), [first conflicting globals failure](../evidence/0170-types-first.log).
- No benchmark speed claims, golden/snapshot refreshes, runtime/source artifact changes or new commit/push. Concurrent local macOS/Node24.20.0 suite runs are correctness probes; BH-0034/0042/0043 still invalidate broad performance conclusions.
- Next0171: migrate the absent forced-cleanup call to a deliberate public operation with before/after evidence; preserve the distinction between product settle and out-of-trace forced cleanup. Discover how to pause the measured observer without using public disconnect blindly, because disconnect also clears runtime rules. Establish real retained-volume setup via DOM/public lifecycle and explicitly handle unavailable private Set-operation measurements instead of fabricating0. Complete all original BH-0033 requirements before marking it fixed.
- Counts remain47 historical/36 fixed/11 unresolved,65 checked/10 blocked. All BH-0004/gates/candidates/host-shutdown and0038 identity-confirmation requirements remain open. Original reports and temporary browser trees are cleaned when their commands terminate; hashes and final terminal statuses are recorded separately.
