# 0093 Figma definitions and modes

- Starting HEAD:dba77281f. BH-0022 reproduced: own exported definitions created0/namespace,0/key,0/value; legacy control and existing export test passed ([before](../evidence/0093-figma-before.log)).
- Normalize definition arrays and legacy nested objects into explicit Figma names/mode/value entries. Dot namespaces map to slash groups; modes arrays declare names instead of becoming variables. Preserve and reuse existing variables/modes; do not recreate variables to change immutable Figma types.
- Import native booleans and export them. Same-collection CSS variable aliases resolve by unique exported name, including forward references and different-mode directions. Missing/ambiguous aliases, unsupported array values, non-finite numbers, incompatible types and actual per-mode cycles reject before mutation. Cross-collection aliases and lossy/ambiguous names remain unsupported with explicit errors.
- Plugin RPC returns errors to the UI; failures no longer become an unhandled plugin rejection followed by a generic timeout. UI can retry. Existing STRING/color inference remains the import policy; no claim of exact arbitrary Figma document serialization.
- 4files/15tests PASS: original export/roundtrip, namespace/modes/aliases/boolean/idempotence, incompatible types unchanged, unsupported values before mutation, alias cycles vs opposite directions in separate modes, and plugin/UI error transport.
- lint, type-check, plugin build and both UI builds PASS. Initial implementation used replaceAll outside the package's ES2020 lib and a test Array<T> spelling disallowed by lint; corrected new code without changing config/dependencies ([final tests](../evidence/0093-figma-tests-final.log), [lint](../evidence/0093-figma-lint-final.log), [types](../evidence/0093-figma-types-final.log)).
- Actual built plugin JavaScript executed in a VM and actual built import UI in Chromium/Firefox/WebKit, bridged by real messages with an explicit in-memory Figma API mock. Definition/mode/forward-alias/boolean import, type mismatch preserving data, visible specific error and successful retry PASS ([log](../evidence/0093-figma-browser.log), [repro](../repros/figma-import-built.mjs)). No actual Figma document or collection touched. Real Figma API/host remains unverified, and this is not represented as real-host evidence.
- BH-0022 repaired at the API contract and built plugin/UI boundaries.44historical confirmed findings:14fixed,30unresolved. BH-0004 remains partial in0092. Integration lab追加驗證 remains paused. Goalactive, cleanup deferred; no new commit.
