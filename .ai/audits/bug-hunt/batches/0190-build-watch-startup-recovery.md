# Batch 0190: Build-watch startup reference recovery

- In progress;505sources match0189. Reproduce six retained build-watch startup missing-reference failures, then repair actual dependency/watch ownership. Preserve original tests and distinguish Vite/Rolldown lifecycle behavior from compiler or harness errors. All four open IDs/full requirements and0038identitypause remain. No commit/push.

## Lifecycle investigation after commit checkpoint c156b1433

- Previous goal turn made progress: committed completed0188–0189 audit records only; current product work remains uncommitted.0038 identity confirmation still not received.
- Host-only9controls: missing leaf/parent registrations in transform/buildStart/buildEnd/error.watchFiles do not rebuild on creation (6TIMEOUT), while3existing-file/input controls rebuild. Preserve realpath harness failure separately. Original26reference tests pass after failed-only reconciliation via an owned existing cache trigger.
- New lifecycle4mode tests exposed premature manifest closure: closeBundle runs after failed watch builds; pre-render and virtual manifest now close recovery on closeWatcher for watch builds. Lazy pre-render forwards closeWatcher. Pre-render managed-entry config initialization is deferred to buildStart so missing dependencies can be registered before reporting failure.
- Event traces show creating the cache trigger during a failed transform adds a spurious rebuild. Create it during configResolved before the watcher starts. Real watchChange events acknowledge dependency versions, avoiding a second reconciliation invalidation for the same host event.
- Host-only obsolete-existing control rebuilds after a dependency is no longer registered (3transform calls); do not misclassify this retained host watch as active adapter polling. New test checks cache mtime remains unchanged and current CSS survives any host rebuild. Reading dist during such a rebuild initially observed its temporary empty state; wait for completed current CSS instead. Raw failing logs remain.
- Extended local/managed ×4mode tests, close/exclude controls and original26reference regressions passed36tests. Full Vite validation, private trigger include/exclude filters, actual browser recovery, shared build/cache ownership and final preservation/budget checks remain before this batch can close. Other open requirements retain0189 scope; no new commit/push.

## Verified progress and next checkpoint

- Final Vite full suite619PASS/68files, lint/types/buildPASS. First full run had10 failures from pre-existing direct-hook tests passing partial config objects; new watch guards now accept omitted build config without changing those tests. Retain first failed log, distinguish this adapter regression from original startup6failures.
- Built-plugin actual HTTP/browser validation48PASS across local/managed ×4modes ×3browsers ×startup-recovered/subsequent-update. This uses default watch filters and disabled runtime injection to isolate CSS publication; it does not close full hydration/SSR/host requirements.
- Two new custom-filter tests bothFAIL after full619: authored-file include (`**/*.css`, `**/*.js`, `**/*.html`) and cache-directory exclude (`**/.vite/**`) prevent the private trigger from causing rebuild. The referenced CSS is accepted by each filter. Do not weaken user filters or call the entire build-watch feature complete. Next investigate a host invalidation mechanism that works with accepted authored dependencies while preserving all filter rules; also verify shared environment/cache lifetime and config-failure cleanup.
- Environment: Vite8.2.2/Rolldown1.2.7. Host source documents closeBundle per watch result and closeWatcher for overall lifetime. No package version, dependency, lockfile, fixture, snapshot, CI/release or engine/runtime source changes. Original58historical/54fixed/4unresolved and65checked/10blocked unchanged. All0189 remaining requirements continue;0038 still awaits identity confirmation. No new commit/push;0190 in progress.

## Owner lifetime follow-up

- Added3ownership checks: config failure leaves an owned file (FAIL); closing first owner initially removed another started owner's recovery file (FAIL); independent configurations sharing cacheDir isolated their files (PASS).
- Register all started build environments, rather than only failed environments, as cache owners. Closing one now preserves the remaining owner's recovery. Original36recovery checks plus2ownership checks PASS;3remainingFAIL are custom include/exclude and configuration-error cleanup. See0190-owners-preserved.log.
- Moving allocation to buildStart or Rolldown options reintroduced2extra initial-cache create rebuilds in static/runtime on this host; those attempts were rejected and logs preserved. Current allocation remains configResolved. Do not claim config cleanup fixed. Lower host API inspection found addWatchFile but no documented plugin-triggered build restart; original filter rules remain untouched. [Plugin context](https://rolldown.rs/reference/Interface.PluginContext), [watch lifecycle](https://rolldown.rs/apis/plugin-api).
-0190 stays unfinished with these3concrete failures. Continue its trigger/filter/config-lifetime work later; next bounded0191 investigates the existing internal Sass proxy direct-request500. Full goal scope and0038 pause unchanged. No commit/push.
