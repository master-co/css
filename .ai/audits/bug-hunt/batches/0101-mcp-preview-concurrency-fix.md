# 0101 MCP concurrent preview apply

- Starting HEAD b056fbf33; previous turn committed five completed fixes, classified progress.0092 compiler partial repair and three foreign Site files preserved. Integration lab0038追加驗證 remains paused pending explicit identity confirmation. No commit in this continuation.
- BH-0031 again reproduces before repair: same token and overlapping previews are accepted more than once through real Context and SDK calls ([before](../evidence/0101-concurrency-before.log)).0053 and0082 establish sequential rejection and cross-process lost-update expectations; no new ID.
- Context synchronously claims an in-flight token before the first await. A private MCP filesystem gate encloses all path/hash checks and writes across contexts and processes. Waiting rechecks token existence and expiry; errors release the in-flight claim so existing failure/retry behavior remains available. Successful apply consumes the token.
- Existing package/binding/compiler code has no suitable cross-process lock primitive. The private helper uses Lamport bakery registers: unique PID/UUID per operation, choosing publication before ticket selection, atomic same-directory rename, then ticket/ID ordering. Account home comes from os.userInfo rather than environment overrides. One gate covers all workspace roots, aliases and hard links without multi-file lock ordering. No dependency, binding ABI, runtime or CSS semantic change.
- Design references: [Lamport's original algorithm](https://lamport.azurewebsites.net/pubs/bakery.pdf), [Node userInfo](https://nodejs.org/docs/latest-v24.x/api/os.html#osuserinfooptions). This filesystem adaptation assumes coherent local filesystem reads/renames; tests are evidence for stated cases, not a formal proof or distributed-filesystem guarantee.
- Each owner removes only its unique register/temp file. Confirmed dead PIDs can be reclaimed by multiple waiters without deleting a replacement owner's lock. Keep the empty directory to avoid directory-generation races. Never expire/take over a live writer; a contender times out after30seconds. PID reuse or uncertain liveness is conservatively treated as live and can cause a timeout. All applies for the same OS account serialize, including independent roots; this trades parallel write throughput for alias-safe coordination.
- Initial new-file regression assertion expected a stale-hash error but supplied macOS /var alias paths, triggering the existing realpath-change guard instead. Output protection already worked. Corrected test setup to canonical temp roots so the intended created-after-preview branch is observed; no product path workaround ([initial tests](../evidence/0101-concurrency-tests.log)).
- New10tests cover20same-token attempts; competing existing/new/symlink/hardlink files across contexts; opposite-order multi-file previews; independent roots; stale failure and same-token retry after restoring preconditions; expiry/dispose while queued; timeout without stealing a live gate. Full MCP4files/34tests PASS; lint/types/build PASS ([final tests](../evidence/0101-mcp-tests-final.log), [lint](../evidence/0101-mcp-lint-final.log), [types](../evidence/0101-mcp-types-final.log), [build](../evidence/0101-mcp-build-final.log)). Native artifacts are unchanged this batch.
- Original same-process/SDK reproduction now PASS:10same-token,3overlapping-token and3SDK rounds each accept exactly one apply ([after](../evidence/0101-concurrency-after.log)). Two real OS Context processes pass10overlap rounds, sequential stale and independent-file controls; final contents always equal the accepted preview ([driver](../repros/mcp-multiprocess-fixed.mjs), [data](../evidence/0101-multiprocess.json)). Historical0082 evidence is not overwritten.
- Built MCP stdio controls use two actual child processes with distinct TMPDIRs:10same-token and10cross-process rounds accept exactly one apply, with correct disk output ([driver](../repros/mcp-preview-concurrency-stdio.mjs), [final](../evidence/0101-built-stdio-verified.log)). Neither observer nor tests replace product file I/O.
- Private gate stress runs6OSworkers x40critical sections, each using a real exclusive-create sentinel and delayed read/increment/write. No overlapping entry/lost count. A seventh worker is killed while holding the gate; two simultaneous recovering workers perform20more sections and clear dead registers.260sections total, all workers terminal, registers empty ([driver](../repros/mcp-preview-lock-stress.mjs), [log](../evidence/0101-lock-stress.log)). These explicit helper controls supplement actual Context/stdio behavior.
- Re-ran actual SDK permission/stale/retry controls from0085 with a new evidence path: normal, stale second file, unwritable first and unwritable second all retain their expected outcomes ([data](../evidence/0101-filesystem-faults.json), [log](../evidence/0101-filesystem-faults.log)). Prior evidence remains unchanged; only optional audit output routing was added to the driver.
- Re-ran0086 actual Context process interruption/restart: SIGKILL during1024file apply left3complete,1020unchanged and1empty/truncated file. Fresh preview recovered all1024files after reclaiming the dead gate ([data](../evidence/0101-process-interruption.json), [log](../evidence/0101-process-interruption.log)). This is an observed sequential-write limitation, not crash atomicity; existing README never promised rollback. Do not report that interrupted writes preserve every individual file intact.
- BH-0031 fixed for cooperating same-account local MCP processes using this protocol. External editors/older writers do not share the gate; edits after hash validation can still race. Cross-user, network filesystems, power loss and exhaustive scheduler/OS interleavings are unclaimed. Public MCP README states serialization, timeout and sequential-write boundaries.
- 44historical confirmed findings:24fixed/20unresolved;65checked/10blocked coverage unchanged. Keep BH-0004partial, four root baseline failures, mjs inspection-discovery candidate and all remaining findings in scope. Next actionable work: BH-0015 JavaScript Unicode escapes in ESLint, then remaining integrations/engine/benchmarks. No ledger cleanup or new commit; all child/command jobs consumed and temporary test roots removed.
- Final [source hashes](../evidence/0101-source-hashes.json), [inventory](../evidence/0101-file-inventory.json), [checks](../evidence/0101-final-checks.json); [AI context](../evidence/0101-ai-context.log)2486files PASS. No missing ledger links; other completed sources and excluded compiler/Site work unchanged. Diff whitespace and new repro syntax checks PASS.
