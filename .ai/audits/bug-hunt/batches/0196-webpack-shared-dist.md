# Batch0196: Webpack shared-dist verification

- In progress. Classify0105parallel failure loading dist/plugins/usage-graph.js while runtime-e2e rebuilds the same package. Current runtime-e2e still builds shared dist; relative-assets and fixed-runtime-filename tests import it. All533sources match0195checkpoint. Use only disposable copies for overlapping writes/reads; root products/artifacts,fixtures,dependencies/lockfiles/CI/release andSite remain unchanged.0038additional verification remains paused. No commit/push;full goal active.

## Classification and retained repair requirement

- Source ownership: runtime-e2e.test.ts calls execFileSync(pnpm --dir packageDir build) during its test; bug-hunt-relative-assets imports that same dist dynamically and bug-hunt-fixed-runtime-filename imports it at module collection. Shared tsdown config emits unbundled modules into dist. The reader/writer overlap belongs to the test harness, not class scanning or generated runtime URLs.
- New driver copies unchanged Webpack source/tests/package configuration and shared build support into a disposable tree. Only its copied webpack dist is rebuilt. Existing third-party/workspace dependencies are linked read-only; no install or root build. Before/after fresh public Node imports succeed. During the actual package build, fs existence observations catch index.js and plugins/usage-graph.js absent; two ready fresh reader processes receive ERR_MODULE_NOT_FOUND. No manual output deletion, fake source error, monkeypatched builder or deferred synthetic import gate.
- The unchanged copied seven-file parallel suite reproduces the original exact /dist/plugins/usage-graph.js load error:67testsPASS/1FAIL plus a failed six-test collection suite. Serial --no-file-parallelism then passes74tests/7files. Both test child processes completed before reader cleanup. These results classify the original0105candidate as shared build/read interference. No new product BH ID and no fixed-count increment.
- Default parallel suite isolation is STILL UNFINISHED. The serial control is useful validation, not a claim the official test flow is repaired. Existing tests, fixtures, build scripts and package configuration were not modified. Keep this explicit requirement after reducing original unclassified candidates from2to1 (legacy-native-compose insertion).
- Initial probe driver stalled while closing persistent reader children after all control results and suites were written. Explicit disconnect and stream drain attempts did not resolve this; pipe explanation remains unproven. Verified only the driver's own process trees before terminating them and removing their isolated directories. Raw logs and cleanup records are retained. Final driver explicitly terminates its own idle probe workers after replies and exits0, removing the tree. It does not claim to test graceful runtime/native shutdown. Final controls preserve the same import assertions; full unchanged suites were not repeated merely to obtain a green orchestration exit.
- All533prior sources and358prior artifacts unchanged;47rootWebpack artifacts additionally captured during isolated controls stay unchanged.534evidence sources include the new driver. No Site revalidation or browser/host scope expansion; all0195failures/fullremaining requirements and0038pause persist. No commit/push. See[final checks](../evidence/0196-final-checks.json) for exact hashes, results and next steps.
