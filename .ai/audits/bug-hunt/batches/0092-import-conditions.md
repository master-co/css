# 0092 Import conditions

- Starting HEAD: dba77281f. Prior turn made progress: committed the 13 verified repairs. Other-task Site files remain excluded. Integration lab追加驗證 remains paused pending explicit identity confirmation.
- BH-0004 reproduced on current source: original3 Rust regressions FAIL ([before](../evidence/0092-import-before.log)).
- Rust graph now parses each expanded import through existing Lightning CSS, wraps layer innermost, media next, supports outermost. Conditional layer declaration follows [CSS Cascade5](https://www.w3.org/TR/css-cascade-5/#at-import). No new dependency or ABI.
- Original3 regressions PASS. Expanded tests cover nested local imports, named/anonymous layers, media lists, combined supports conditions, and unchanged external imports.
- First expanded run:26 baseline PASS,7 regressions PASS,1 new test expected old-style min-width serialization while the existing printer normalizes to width>=500px. Corrected new expectation; this was a test expectation error.
- Remaining BH-0004 scope: a qualified local import containing an unresolved external import cannot simply be wrapped (`@import` is invalid inside conditional blocks). Current patch reports this explicitly instead of emitting invalid CSS or silently dropping conditions. Complete support and external cascade ordering remain unfinished; do not mark BH-0004 fixed based on the simple cases.
- Validation complete at the stated bounds:26 existing Rust tests +8 regressions PASS; Clippy PASS; native/compiler-Wasm rebuilt; compiler14files/126tests PASS; compiler lint PASS. First3 new TS assertions assumed minified CSS while the public API emits formatted CSS; corrected only those new assertions,123existing tests passed in both runs.
- Actual Chromium/Firefox/WebKit:8import scenarios ×2media ×2viewport sizes ×3browsers =96 comparisons PASS against browser-loaded original imports. Cases include print/screen/width, unsupported supports, layer cascade/conditional layer order, anonymous layers and nested local imports ([log](../evidence/0092-browser.log), [repro](../repros/import-conditions-browser.mjs)). Browser/server resources and temporary files cleaned.
- Native CSS import graph behavior changes; Master directive syntax, managed lowering, fixtures/snapshots and public ABI are unchanged. The old directive guide path in AGENTS is absent after the user's Site reorganization; it is not recreated.
- BH-0004 remains confirmed/unresolved, with local cases repaired. Next: design preservation of nested unresolved imports and their cascade order using provider-resolved controls and equivalent browser fixtures; do not consider the explicit diagnostic complete support. No commit or ledger cleanup in this batch.
