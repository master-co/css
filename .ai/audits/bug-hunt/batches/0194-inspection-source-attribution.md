# Batch 0194: Per-file inspection attribution

- In progress, BH-0060. Source hashes match0193; goal active, all previous remaining scope and0038identity pause retained. Capability discovery: scanner owns async Vue/Svelte extraction and Rust classification; scan returns a global-change boolean; collectCandidates mutates/deduplicates the global latent set. Diagnostics currently computes concurrent global deltas, unsuitable for per-file occurrence attribution. Expose the already-extracted candidates with the scan result at the owning tooling layer; compiler associates them with final Rust classifications after all scans complete. Preserve scan/scanModule boolean callers and classify repeated/native/blocked/safelisted controls before completing. No commit/push.

- Before:7source-attribution tests FAIL (`0194-attribution-before.log`). New test correction: stylesheet `@blocklist` is scoped to stylesheet output rather than global scanner classification; its per-file attribution control must retain the globally classified hidden candidate, then assert it is associated only with actual source files. Global scanner blocklist behavior will be tested directly through scanner options. This initial expectation error is not a separate product finding.

- New test material also initially omitted the required quotes around CSS @safelist/@blocklist class lists; corrected against the current directive guide. The first after run was9PASS/1FAIL at the safelist CSS assertion; preserve this log and verify the corrected input before classifying any additional behavior.

- Correct quoted directive inputs pass all7attribution tests. Scanner API control initially used a standalone JS string expression, treated as a directive prologue rather than a class-bearing expression by the existing extractor; replaced it with the same real className assignment as other positive controls. Native/Wasm6PASS/2FAIL initial log retained; no extraction heuristic change.

- Tooling lint/types/build and compiler lint passed; compiler types rejected the new test corpus inferred union cast. Added an explicit AttributionCase type without changing cases or assertions; initial type error retained as test-material error. Native/Wasm scanner and all337compiler/223tooling/43MCP tests already passed before this annotation-only correction.

## Completed repair

- BH-0060 fixed. `MasterCSSScanner.scanSource` exposes the candidates already extracted by the owning tooling pipeline alongside the existing change flag; exported `MasterCSSScannerSourceResult` documents the additive Node scanner API. Candidates remain available for cache hits, repeated files, native classes and blocklisted inputs. Existing `scan`/`scanModule` return booleans and preserve filters/change events.
- Compiler diagnostics waits for all source scans, builds membership sets from the final Rust scanner classifications once, then associates each file's own candidates with those classifications. This removes cross-file contamination and retains repeated occurrences. Invalid warning locations use the first actual containing file in report input order, including reverse ordering and asynchronous adapters. No TS parsing/classification fallback, new Rust semantics, ABI or generated binding changes.
- Per-file `discovered` now means actual occurrences, including already-seen classes; `changed` continues to report a scanner state change and may be false for a file containing existing classes. Tooling and compiler READMEs explain the intentional report/API behavior. Existing fixtures/snapshots, dependencies, lockfiles, CI/release, Site and other work remain unchanged.

| Validation | Result / raw evidence |
|---|---|
| New ownership tests | 7 compiler groups PASS; exact unique/repeated valid/invalid/native ownership, reversed inputs, Vue/Svelte, scoped source policies and empty files. [Final](../evidence/0194-attribution-final.log) |
| New scanner API controls | 4 groups × native/Wasm =8PASS within full tooling; cache hits, repeated files, filters/events, global safelist/blocklist/native state, adapters and reset. [Full](../evidence/0194-tooling-full.log) |
| Full scoped suites | compiler337 / tooling223 / MCP43 / CLI71 PASS. Logs `0194-{compiler,tooling,mcp,cli}-full.log`; compiler final7 rerun follows annotation-only test correction |
| Original built regressions | Original `.mjs` public compiler/MCP matrix12PASS and plain two-JS diagnostic control PASS. [Matrix](../evidence/0194-built-original.log), [warning location](../evidence/0194-js-original.log) |
| Expanded real SDK stdio | 22PASS: built compiler/MCP scans and traces for unique/repeated/async-adapter files, both input orders and invalid-class owners. [Driver](../repros/inspection-attribution-stdio.mjs), [log](../evidence/0194-built-attribution.log) |
| Vite downstream suite | 640PASS/3FAIL; exactly0190 authored-file include, cache-directory exclude and configuration-error recovery file cleanup. These remain unfinished. [Full](../evidence/0194-vite-full.log) |
| Lint/types/build | tooling/compiler/MCP PASS; compiler first new-test type error and corrected final runs preserved. All package/native/Wasm builds serialized away from consumers; no native/Wasm rebuild |
| Root API gates | Both remain FAIL. Package golden now additionally sees deliberate `MasterCSSScannerSourceResult`/scanner surface change alongside existing unfinished graph API changes; census retains prior consumer differences. No golden or census rewritten. [Contracts](../evidence/0194-package-contracts.log), [census](../evidence/0194-api-census.log) |

- Node-only overhead: scanner returns one result object; inspection retains per-source candidate arrays until all scans finish and constructs global membership sets once. Membership selection avoids repeated linear scans of global arrays. No engine/runtime hot-path edits, browser payload change or performance-gain claim; full benchmark requirements remain.
- All prior527source hashes match except6intended tooling/compiler source/docs changes;3newtest/repro files make530tracked evidence sources. Native/Wasm/runtime/Vite artifacts and foreign Site source hashes unchanged. Only tooling/compiler/MCP build outputs may differ. Exact hashes, results, test-material corrections and full remaining scope are in [final checks](../evidence/0194-final-checks.json).
- 60 historical /56fixed /4unresolved. All0190threefailures, full BH-0004 graph/public/host delivery, BH-0029/BH-0051/BH-0053, maps/gates/benchmarks/Site, two original candidates, native shutdown/WebKit constraints and10blocked coverage remain unfinished.0038additional verification stays paused; no identity confirmation received. Goal active; no commit/push. Next continue0190filtered invalidation/config-error cleanup and complete public/host graph delivery.
