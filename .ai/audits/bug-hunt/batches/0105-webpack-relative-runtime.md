# 0105 Webpack relative runtime assets — BH-0017 fixed

- Starting HEAD5dc6f2ad1. Previous goal turn made progress:0104 fixed Vite hydration URLs with106tests/36browser controls. That uncommitted repair, compiler0092 partial work and foreign Site files remain unchanged. No commit this turn. Integration lab0038追加驗證 remains paused pending explicit identity confirmation.
- Read current Webpack manifest/AI, runtime HTML/entry/manifest-loader/virtual-module sources, browser manifest facade, existing fake-compilation and built runtime tests, local Webpack EntryDescription schema and AutoPublicPathRuntimeModule implementation. Build-time asset injection belongs here; no Vite dependency or new shared public abstraction is needed.
- Original0021 regression reproduced: root resolves ./master-css-runtime.js, nested page does not ([before](../evidence/0105-relative-before.log), exit1). New HTML fix passes that original regression ([intermediate](../evidence/0105-html-regression.log)) but is insufficient for runtime startup.
- HTML transformation now receives each emitted HTML filename. It computes relative hrefs from that directory for script, runtime preload, manifest preload and identifiable runtime Wasm preload assets; absolute/root/CDN prefixes retain their existing spelling. Both asset-processing stages use the same page path, preserving duplicate prevention and classic/module tag contracts.
- Actual browser validation of the HTML-only fix found another manifestation of BH-0017: placing runtime in js/master-css-runtime.js causes its relative JSON import to request js/master-css-manifest.*.json, although the manifest is emitted at output root. Root page already fails in this layout; initial display remains block rather than inline-flex ([intermediate browser failure](../evidence/0105-html-browser.log)). This is product evidence, not a test error.
- Complete repair configures only the injected runtime entry with Webpack's native publicPath:auto when output.publicPath is a relative string. Webpack computes the output-root URL from currentScript/import.meta.url and its own chunk path; JSON/Wasm/lazy chunk loads then share that root. The compiler's output options and app entry declarations are not rewritten. Existing auto/absolute/CDN entry behavior stays unchanged. No hand-written runtime URL fallback or semantic implementation.

## Verification

| Scope | Result / evidence |
|---|---|
| Original regression | PASS after HTML repair; included in final suite |
| New matrix | 14tests:7publicPath shapes ×classic/module HTML controls, plus7entry-option cases; exact four asset URLs, two-stage duplicate prevention and unchanged compiler output options |
| Complete Webpack suite | 5files/63tests PASS using --no-file-parallelism; [final](../evidence/0105-webpack-final.log) |
| Built package / browsers | 45controls PASS:5output configurations ×3HTML paths ×Chromium/Firefox/WebKit. Runtime/JSON/Wasm/lazy chunks load with no missing files/errors, initial inline-flex renders and new grid class is observed; [driver](../repros/webpack-relative-runtime.mjs), [results](../evidence/0105-built-browser.log) |
| Shared runtime chunk | Additional9controls PASS with optimization.runtimeChunk:single; generated shared runtime and Master CSS entry execute correctly at all three page depths/browsers; [results](../evidence/0105-shared-browser.log) |
| Package lint/types/build | PASS; [lint](../evidence/0105-lint-final.log), [types](../evidence/0105-types-final.log), [build](../evidence/0105-build.log) |
| Existing Webpack example | Actual production build PASS to /tmp/mastercss-0105-webpack-example; [log](../evidence/0105-example-build.log). Reports expression-dependency and847KiB Wasm asset-size warnings; no warning suppression or dependency change |

- Built controls include ./ classic/module, empty classic, auto module and /mount/ classic; emitted runtime is in js/ and lazy chunks in chunks/. HTML paths are root, pages/deep/index.html and js/shared.html. Route fulfillment serves actual emitted bytes with JS/JSON/Wasm MIME types. CDN prefix correctness is checked by unit output assertions, not a live CDN claim. Function-valued publicPath and arbitrary external deployment rewrites are not newly claimed by this batch.
- Initial new test type-check failed because its callback parameter named assets recursively referenced typeof assets. Renamed only that new test parameter; final type-check passes. This was test material, not a product finding ([initial types](../evidence/0105-types.log)).
- First parallel full suite:62PASS/1FAIL loading /dist/plugins/usage-graph.js while the existing runtime-e2e test rebuilds shared package dist ([initial suite](../evidence/0105-webpack-full.log)). Serial full rerun passes all63. Shared dist build/read overlap is the suspected harness cause; not proven a new product bug or claimed repaired. Preserve the initial failure and use the verified serial command when continuing this package; no existing test/fixture was changed to suppress it.
- Runtime engine/core/global source and engine Wasm remain unchanged. Generated Webpack bootstrap for relative settings now includes Webpack's automatic asset-root resolution; its byte output can differ. No CPU/bundle improvement claim and no runtime benchmark: this changes host entry/URL generation, not DOM observation, CSSOM, engine or runtime core execution. No public exports, dependencies, lockfiles, generated contracts, CI/release or existing fixtures/snapshots changed.
- All browser/page handles closed and repro temporary projects removed in finally. All command sessions terminal. [Source hashes](../evidence/0105-source-hashes.json), [inventory](../evidence/0105-file-inventory.json), [checks](../evidence/0105-final-checks.json), [AI context](../evidence/0105-ai-context.log).
- BH-0017 fixed:44historical findings,27fixed/17unresolved;65checked/10blocked coverage unchanged. Four existing root failures, the observed parallel test-harness failure, benchmark/platform/nested-host blockers, BH-0004 partial repair and mjs inspection candidate remain unfinished. Next bounded batch: BH-0030 Webpack fixed output filename collision using0042 evidence. Keep separately paused0038 validation untouched.
