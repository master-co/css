# 0139 Inline server and worker hosts

- Previous goal turn made progress:0137/0138 fixes and validation completed; this batch verified all151source hashes before proceeding. HEAD087655705; no further commit/push.0038additional verification remains paused pending explicit identity confirmation.
- Scope: actual ESM/CJS library loading, multi-format outputs, SSR CSS insertion and asset policy, and worker-delivered CSS. BH-0004 remains incomplete.

## Evidence and classification

1. The [initial Node probe](../evidence/0139-node-hosts-before.log) reported16passes, but its output options contained explicit undefined format/entryFileNames fields that overrode Vite library defaults. All library outputs became entry.js, so those rows did not prove CJS or distinct multi-output loading. Checking local file URL existence was also too weak for SSR CSS destined for a browser. These are harness/verification errors, not evidence that the product was correct.
2. The [corrected Node probe](../evidence/0139-node-hosts-corrected.log) uses explicit mjs/cjs outputs, preserves library format defaults and rejects file URLs in SSR CSS.14builds/16load comparisons yield12PASS/4SSRFAIL. Real ESM/CJS and dual-format library loads pass; managed SSR embeds its server filesystem path in image URLs, while pure Vite returns a configured public asset path.
3. [Actual SSR browser reproduction](../evidence/0139-ssr-browser-before.log):four client/server pairs,8builds,12browser failures plus4asset-policy/URL failures. The stylesheet's retained import and image use file URLs, so SSR HTML cannot load them. With ssrEmitAssets disabled, the managed plugin additionally emitted resource/CSS assets after Vite had removed ordinary assets. [Four package regressions](../evidence/0139-ssr-tests-before.log) also fail before the fix.
4. The owning installed Vite code identifies server environments by consumer and suppresses assets via environment.config.build.emitAssets in its asset generateBundle hook. Our later publication bypassed that hook; no Vite source, dependency or lockfile change was needed.

## Fix and validation

- Server transforms now export the controlled CSS token without a runtime module-relative URL expression. During final composition, owned root URLs use the server environment's configured public base plus the actual emitted asset directory. Retained CSS children keep relative references inside that directory. Browser/library output keeps its existing module/worker URL behavior.
- The late inline publisher respects environment.config.build.emitAssets, so disabled SSR output publishes no plugin-owned assets. The matching client build remains responsible for those files. Enabled SSR output includes the required assets. Public Vite README and directive-contract prose explain this deployment distinction.
- [Corrected SSR tests](../evidence/0139-ssr-tests-after.log):4PASS, ESM/CJS × emitAssets false/true. [Full Vite suite](../evidence/0139-vite-tests-final.log):125PASS/22files. Vite lint/type-check/build and original example build PASS. No Rust/ABI, public export, dependency, fixture/snapshot or runtime changes.
- [Expanded SSR browser matrix](../evidence/0139-ssr-browser-expanded.log):12client/server pairs/24builds,12asset-policy checks and36Chromium-Firefox-WebKit browser comparisons PASS. Covers ESM/CJS, public path base, relative base, CDN base and both SSR emission settings. HTML receives the actual server export; disabled-emission cases load files from the paired client build, enabled cases from the server build. Retained external imports apply blue and queried/fragmented SVGs decode correctly, with no missing assets/page errors.
- [Final Node hosts](../evidence/0139-node-hosts-verified.log):14builds/16loads PASS, including actual mjs/cjs files and two distinct outputs per dual-format build. The driver asserts output count and distinct filenames to prevent the initial cache/format mistake. Library verification proves Node loading and file-resource existence; it is not evidence for arbitrary SSR serialization of a library build.
- [Workers](../evidence/0139-workers-verified.log):2builds/6browser comparisons PASS. Classic Worker uses IIFE output; module Worker uses ES output. The Vite worker plugin factory is configured explicitly. A worker returns compiled CSS to the main document, where retained external imports and relative-origin images load correctly from custom nested output directories. An earlier6PASS run used module Worker constructors for both output formats and is retained with that narrower scope.
- The original14build/84browser inline matrix PASS; Site prepare/lint PASS with0errors/75existing warnings. Source preservation, artifact hashes, AI-context/whitespace checks and raw logs remain alongside this batch.

## Remaining scope and continuation

1. Validate custom experimental.renderBuiltUrl callbacks and unusual encoded/public paths instead of assuming the standard base matrix proves them. Multi-environment shared builder state, additional output formats and cycle/root-back-reference publication remain unverified. Used strings retain a35byte anchor and unused roots still incur composition work; no performance improvement is claimed.
2. Continue original Sass source-map attribution through Rust diagnostics; then broader CSS Modules composition/global/import/watch, other preprocessors/PostCSS, virtual resources/reference, multiple roots/local-compose and HMR. Early WebKit automatic stylesheet timing remains open. Migrate remaining Webpack publication and original no-delivery/native/CLI no-export consumers.
3. All five0138 compiler/engine/runtime artifacts should remain byte-identical; no runtime benchmark repeated for this build-host fix. Root API failures retain0137evidence and other gates remain open; no golden refresh. Original Webpack3build/12browserFAIL (0133) and legacy39browserFAIL (0125) retain their historical scope.
4.45historical findings33fixed/12unresolved;75coverage65checked/10blocked;4root gates/4unclassified candidates;0038identity pause unchanged. Goal active.0128–0130identical progress paragraphs were moved verbatim from four primary ledgers into progress-history-0139.md to keep the primary files below their routing budget; fixed IDs and current progress remain intact.
