# 0094 Runtime CSP and cross-realm tracking

- Starting HEAD:dba77281f;0092–0093 changes retained. Previous goal turn made progress (BH-0022 repaired; BH-0004 partial, not completed). Integration lab追加驗證 remains paused. Other-task Site files untouched.
- Current original BH-0008/0009 regressions FAIL in all3browsers (6FAIL). Fetch and direct JSON import succeed under CSP; Function constructor rejects. iframe initial classes succeed; attribute mutations remain stale ([before](../evidence/0094-runtime-before.log)).
- Hydration now uses native dynamic import with JSON attributes, consistent with the existing global preset loader. Eliminates string evaluation and obsolete Function-construction/SyntaxError fetch fallback. Module request/JSON/schema errors still reject with INVALID_HYDRATION_MANIFEST; no automatic fetch after import failures. Actual supported browser matrix uses JSON import attributes; no claim for old browsers lacking that syntax.
- Three existing hydration tests tied to the removed Function-construction implementation were updated for JSON module/no-fetch, invalid-version rejection and structured HTTP failure. No existing fixture/snapshot updated. Browser-provided import error causes can differ from the former fallback's custom HTTP string; outer error code and URL remain tested.
- Class tracker identifies native DOM Elements by nodeType, which works across iframe realms for attributes and added/removed subtrees; no engine semantics or public API change.
- Original6regressions PASS after fixes. Added iframe Document/ShadowRoot tests include SVG attributes, subtree add/move/remove, ignored text/comment nodes and actual computed display.
- Full runtime e2e270PASS across Chromium/Firefox/WebKit; new built-global CSP test3PASS separately (273total). Built global test uses actual published JS, preset JSON and runtime Wasm, verifies progressive hydration, class update/computed CSS and zero CSP violations. lint/type-check/build PASS ([e2e](../evidence/0094-runtime-e2e.log), [built global](../evidence/0094-global-csp.log)).
- Standard Chromium benchmark before/after both complete with all built-in correctness controls. Same5measured+1warmup,1000scan/1000mutation/250hydration settings. Mutation median remained about2.8ms; noisy startup/scan/hydration timings do not justify a performance claim. Raw history remains outside the repository at /tmp/mastercss-0094-before.{json,log} and /tmp/mastercss-0094-after.{json,log}.
- Global bundle raw/gzip/brotli decreases277/112/96bytes; module bundle decreases421/114/100bytes. Default manifest and runtime Wasm hashes unchanged. No new cached state or extra DOM scan; nodeType check replaces cross-realm constructor check, and hydration stops creating an evaluated function. Generated CSS semantics, rule ordering and retention unchanged; correct foreign-root mutations now reach CSSOM.
- BH-0008/0009 fixed at these browser/public runtime boundaries.44historical findings:16fixed,28unresolved at this batch. No new commit; all browser/command jobs terminal.
- Runtime baseline built before edits; package benchmark completed with default5measured+1warmup rounds,1000scan/1000mutation/250hydration classes. Raw benchmark history is in /tmp/mastercss-0094-before.{json,log}, outside the repository. Bundle hashes/raw/gzip/brotli captured before/after.
