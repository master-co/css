# Batch 0199: Graph directive metadata

- In progress.541sources match0198. collectStylesheetDirectivesFromCSSGraph still flattens eachfile before scanning standalone policies;registerStylesheetSource additionally passes an alreadyflattened detection source. Verify lost qualified directives, incorrect relative source ownership andexternal import rejection through both collector andactualregistration. Preserve metadata-only behavior: do notlower orvalidateunrelatedmanaged/nativeCSS anddo notloadreference-only sources. Reuse Rust graph discovery andstandalonepolicy parsing, withNode owning file resolution andpattern paths. FullBH-0004 andall0198remainingrequirements/0038identitypause remain. No commit/push;allforeignhosts preserved.

## Verified metadata repair

- Added native and compiler-Wasm `resolveCSSStylesheetGraph(request)` bindings over the existing Rust graph discovery. This operation validates syntax/cycles and returns ordered imports, original file ownership, unresolved external edges and reference metadata. It does not compile managed/native CSS or load reference-only inputs. The existing graph result type is reused; no protocol/template regeneration or parser fallback.
- The Node metadata collector prepares original files and asks Rust to discover their graph, then merges existing Rust standalone extraction policies with Node-owned relative-path normalization. Registration now supplies original entry bytes, not the flattened detection source; package expansion remains enabled for registration and disabled by default for the standalone collector.
- Corrected baseline:14FAIL/1PASS. The first baseline mistakenly called scanner.destroy(), masking a registration assertion; corrected only the new test to await scanner.dispose(), retained both logs. Final16metadata tests PASS, including child source ownership, qualified imports, external imports, import cycles, ignored reference-only sources and package-owned policy.
- New native/Wasm discovery parity7PASS covers six qualifiers, repeated imports, UTF-16 spans, external/reference metadata, syntax/cycles and disposed sessions. Full binding24PASS. Initial compiler374PASS; after adding explicit retained failures and graph controls, current full compiler379PASS/5FAIL across52files. Compiler/binding/Wasm delivery lint andtypes PASS; native/Wasm clippy, workspacefmt, codegencheck andparity PASS.
- Built Node registration before:36browserFAIL/36authorcontrolsPASS. After metadata repair:36browserPASS/36authorcontrolsPASS acrossChromium/Firefox/WebKit andscreen/print. Initial old-built-consumer harness used a root node_modules link without the compiler binding; corrected to the compiler package dependency directory. Browser results prove extracted class behavior, not absence of every consumed directive.

## Retained product and test failures

- Five qualified legacy registration/composition cases still leave consumed @source/@safelist text inside native layer/supports/media wrappers. These appeared in both old andnew built outputs, belong to BH-0004 and remain five failing assertions in the new regression file. No additional finding ID or fixed-count increment.
- Five added controls call the existing graph-rendered compiler: policies are consumed before wrapping and native rules/conditions/dependencies survive. First controls expected minified `display:grid`; native rendering contains a space, so only that new formatting assumption was corrected. Original five product-failure assertions remain unchanged. Current full suite379PASS/5FAIL includes these controls.
- Source trace: legacy registration resolves/strips flattened source and stores it; createExtractedCSSResult then recompiles it and re-lowers aggregate manifest input. Replacing just registration's compile call would leave composition and native-output selection wrong. Next migrate stored graph plus composition together; preserve reference origins, merged manifests, class pruning and includeMasterBaseCSS/includeNativeCSS behavior. Rust graph compilation is the available semantic owner; do not recursively strip text in TypeScript or broaden standalone directive syntax.
- Compiler-Wasm original package suite3PASS/1FAIL: an exact result comparison rejects sourceMappings. An isolated copy of the original pre0199 source (hash-verified) andoriginal artifacts reproduces the same3PASS/1FAIL with unchanged original tests. The initial pnpm-exec copy failed dependency resolution before tests; corrected direct installed Vitest invocation establishes the baseline. This is retained pre-existing test-contract drift, not a new0199 regression. Existing test/fixture/snapshot not edited.

## Delivery and preservation

- Built native andWasm into isolated target/candidate paths. A standalone repro links the already-installed wasm-bindgen support library; no dependency install or build-flow edit. Staged binding/compiler tests passed before root delivery. New Wasm JS glue only adds the export wrapper; existing imports/init paths unchanged.
- Root Wasm directory andthree complete JSdist directories were exchanged atomically with verified macOS renamex_np; a completed native file replaced the prior inode atomically. No in-place shared-artifact build or foreign-host stop. Prior complete artifacts/dist retained at recorded temporary backup paths. Existing loaded hosts were not restarted or claimed revalidated.
- Final546source inventory:528of541 prior exact;13intentional existing edits plus5newfiles. Prior compilerREADME andforeignSitecontract bytes recover exactly by removing only the added paragraphs. Extended artifact inventory453files:13expected changes verified against promotion records;440unchanged,includingnativeCLI andengineWasm. No runtime-engine change.
- Isolated Site prepare/lintPASS,0errors/75warnings; copied outputs removed. Full current/liveSite verification remains pending. No existing fixtures/snapshots/tests, dependencies, lockfiles, CI/release modifications in0199.
- During interruption, user-authorized commit3b5c98d61 included0198audit only;0199 andproductwork stayed uncommitted. Previous goal turn was progress: completed commit andverified preservation. This continuation does not authorize another commit/push. Counts remain60historical/56fixed/4unresolved and65checked/10blocked. Fullgoalactive;0038identityconfirmationnotreceived.

See [final checks](../evidence/0199-final-checks.json) for exact remaining requirements, verification scope andlog hashes. The metadata behavior is verified; legacy registration/composition repair andallremaininggoal requirements are incomplete.
