# 0142 Production CSS Modules

- Previous goal turn made progress: completed 0141 source-location fixes, tests and ledger. All 164 recorded source hashes matched before this batch. HEAD `3e44f4fb3`; no new commit/push. Batch 0038 additional validation remains paused pending explicit identity confirmation.
- Scope: actual Vite production CSS/Sass Modules exports, scoped/native CSS usage, composition, globals, localsConvention, inline strings and bounded rebuild controls. BH-0004 and the whole goal remain incomplete.

## Reproduction and changes

1. [Original module matrix](../evidence/0142-css-modules-before.log): 12 builds, 3 managed `.module.css` build failures; the other 9 builds and 27 browser comparisons pass. Missing named exports affect ordinary scoped CSS, cross-file composition and camelCase locals. The plugin replaced managed CSS before Vite's Modules processing, leaving no export map. Pure Vite and the equivalent Sass Modules cases pass.
2. The existing public Vite preprocessing bridge now accepts `.module.css` alongside Sass, preserving Vite's module exports and processed CSS. It reuses the existing private JS/CSS proxy identities, dependency/cache handling and CSS graph pipeline; it does not implement CSS Modules semantics or add another dependency. Raw requests still bypass the bridge, inline requests produce a stylesheet string, and `css.modules: false` retains the ordinary CSS path.
3. The first correction passes 12 builds / 36 browser comparisons. The stronger [default-pruning matrix](../evidence/0142-css-modules-pruning-before.log) then finds a second real problem: all 20 builds succeed, but 12 of 60 browser comparisons fail. Both CSS and Sass Modules lose referenced scoped rules because the generic usage graph intentionally skips null-prefixed modules. Tests with explicit `@preserve native` had not covered default pruning.
4. The bridge now sends its own generated export module through the existing scanner under a file-owned JavaScript identity. Rust still owns candidate extraction/class policy. Exported scoped names participate in native class usage, including composed names. Cached export modules transform again on rebuild so a fresh scanner receives that usage. Generic third-party virtual-module scanning is unchanged.
5. The diagnostic adapter identifies unavailable original positions as CSS Modules rather than Sass when appropriate. An actual `.module.css` invalid-compose regression verifies original filename and UTF-16 range after scoping. The 15 Sass diagnostic regressions also remain green.

## Validation and watch limitation

- [Final module matrix](../evidence/0142-css-modules-final.log): **20 builds / 60 Chromium-Firefox-WebKit comparisons PASS**. Ten pure-Vite/managed pairs cover CSS/Sass, local and composed names, global selectors, localsConvention, and default native pruning. Named/default exports agree, computed foreground/background colors match, and no page errors or missing assets occur.
- Vite **154 PASS / 25 files**, eight new tests. Covers managed named/default exports, composition, scoped native retention in CSS/Sass, camelCase names, disabled Modules, inline strings and structured original-source diagnostics. Focused Modules + Sass diagnostic control: 23 PASS. Lint/type-check/build and original Vite example build PASS.
- Existing Sass matrix: **15 builds / 90 browser comparisons PASS**. Existing inline matrix: **14 builds / 84 browser comparisons PASS**. No fixture/snapshot or dependency changes.
- [Managed watch first](../evidence/0142-modules-watch-first.log): initial CSS Modules build and JS-only rebuild pass all six browser comparisons. Changing only `shared.module.css` then produces no build event within the explicit 30-second observation; the driver exits and closes the watcher. It does not reach Sass. This is incomplete watch behavior, not a passing third build.
- [Pure Vite watch control](../evidence/0142-modules-watch-vite-control.log), with the Master plugin and directives removed, exhibits the same CSS dependency-only timeout after six passing comparisons. [Public preprocessCSS observation](../evidence/0142-module-dependencies.log) returns the composed CSS/export map but `deps: []`; the child portion of the source map is also `\0<no source>`. This establishes an existing host limitation for this case, not a new Master-only failure, and does not complete the requirement.
- Inspection of installed Vite 8.2.2/PostCSS Modules 9.0.1 shows module resolution/loading inside the host's Modules processing. Its public result omits the composed child dependency in this case. No private host patch, transitive dependency import, dependency installation or CSS Modules parser fallback was introduced.
- [Root-edit control](../evidence/0142-modules-watch-root-control.log): **6 builds / 18 browser comparisons PASS** across CSS and Sass, initial red → JS-only red → changed child plus explicit root edit green. This proves cached usage replay and fresh composition when the owner is invalidated; it does not prove dependency-only invalidation. Run with `BH_ROOT_EDIT=1`; `BH_PURE_VITE=1` selects the host control and `BH_EXTENSION=css` or `scss` isolates a source type.
- Final successful browser matrices total **252 comparisons** (60 Modules + 90 Sass + 84 inline + 18 root-edit controls). The two timeout probes retain their separate partial results and are not relabeled successful.
- Site prepare/lint and final source/context checks are recorded in adjacent evidence. Five compiler/engine/runtime artifact hashes and raw/gzip/Brotli sizes remain identical to 0141. No runtime benchmark repeated; this adds build-time Modules processing and scans generated export text. Exported names are conservatively available to native pruning; no unused-export tree-shaking or CPU/memory improvement is claimed.
- No public export or ABI change. Root API failures retain 0141 evidence and unchanged surface; no golden refresh. Other root gates stay open.

## Direct continuation

1. Resolve CSS Modules composition dependency-only watch invalidation using supported host capabilities, preserving the pure-Vite baseline evidence. Cover both CSS/Sass, nested composition, rename/delete/error recovery and accurate source ownership; a root-edit workaround is not completion. The public result's missing dependencies and child source-map attribution remain concrete obstacles to investigate.
2. Validate development Modules/HMR and other modes; this bridge applies to production builds. Broader module scope/export options, user PostCSS transforms, other preprocessors, aliases/virtual sources/resources/references, multiple roots, cycles and SSR/multi-environment state still need evidence. Do not extrapolate the production browser matrix to these paths.
3. Continue automatic stylesheet custom URL mapping, early WebKit automatic CSS readiness and remaining Webpack graph publication/no-delivery/native/CLI no-export consumers. Historical Webpack 3 build / 12 browser failures and legacy 39 browser failures remain open. Published CSS maps and inline anchor/unused-work overhead are unchanged follow-ups.
4. Keep 45 historical findings at 33 fixed / 12 unresolved, coverage at 65 checked / 10 blocked, four root gates/four unclassified candidates and batch 0038 identity pause unchanged. Goal active. Product/tests/audit changes stay uncommitted.
