import { IconAdjustments, IconDatabaseExport, IconFileExport } from '@tabler/icons-react'
import ImageCallToPreview from 'websites/components/ImageCallToPreview'
import Features, { Feature } from 'websites/components/Features'
import clsx from 'clsx'

Master CSS [Static Extraction](/docs/rendering-modes/static-extraction) integrates build tools to **scan** project source code, **extract** class names, **generate** CSS rules, and write them into a **virtual CSS module**.
<div className="flex flex:wrap@<sm gap:12x mt:12x">
    <Features className={clsx(props.img ? 'w:35%@sm' : 'grid-cols:3@sm')}>
        <Feature>
            <IconFileExport className="app-icon-primary" />
            <div>
                **Zero runtime**
                <p className='text:16'>Generate virtual CSS modules at build time</p>
            </div>
        </Feature>
        <Feature>
            <IconAdjustments className="app-icon-primary" />
            <div>
                **Semi-dynamic**
                <p className='text:16'>Scan all source code to extract class names</p>
            </div>
        </Feature>
        <Feature>
            <IconDatabaseExport className="app-icon-primary" />
            <div>
                **Cross-page caching**
                <p className='text:16'>Download the CSS bundle for all pages at once</p>
            </div>
        </Feature>
    </Features>
    {props.img && <div className="flex:1|1|65%@sm">
        <ImageCallToPreview {...props.img} />
    </div>}
</div>