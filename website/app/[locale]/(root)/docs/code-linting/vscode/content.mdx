import VSCodeSvg from 'websites-shared/icons/visualstudiocode.svg'

<StepSection className="mt:50">
    <Step $row>
        <StepL>
            ### <StepNum />Install ESLint extension
            Install the [ESLint extension](https://marketplace.visualstudio.com/items?itemName=dbaeumer.vscode-eslint) in the Visual Studio Marketplace if you haven't already.
        </StepL>
        <StepR>
            <a className="app-object app-object-interactive flex:col px:25 h:48 font:14 font:semibold r:4 lh:1 flex:row!" href="vscode:extension/dbaeumer.vscode-eslint">
                <VSCodeSvg className="ml:-4 mr:10" width="20" height="20" />
                <span>Install to Visual Studio Code</span>
            </a>
        </StepR>
    </Step>
    <Step $row>
        <StepL>
            ### <StepNum />Install Master CSS ESLint
            Install the Master CSS ESLint Config into your project via package managers.
        </StepL>
        <StepR>
            ```install
            -D @master/eslint-config-css@beta eslint
            ```
        </StepR>
    </Step>
    <Step $row>
        <StepL>
            ### <StepNum />Set up ESLint config
            Create [`.eslintrc.json`](https://eslint.org/docs/latest/use/getting-started) if you don't have one.
            - Add `@master/css` to the `extends` section.
        </StepL>
        <StepR>
            ```json name=.eslintrc.json
            {
                "extends": ["@master/css"]
            }
            ```
        </StepR>
    </Step>
    <Step $row>
        <StepL>
            ### <StepNum />Add VS Code settings
            Create a `.vscode/settings.json` or set it in [user settings](https://code.visualstudio.com/docs/getstarted/settings).

            - Add `@master/css/class-order` to the `eslint.codeActionsOnSave.rules` section.
        </StepL>
        <StepR>
            ```json name=.vscode/settings.json
            {
                "eslint.codeActionsOnSave.rules": [
                    "@master/css/class-order"
                ]
            }
            ```
        </StepR>
        <StepEnd/>
    </Step>
</StepSection>