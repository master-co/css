## Overview [sr-only]
<Overview/>

## Conditionally apply
Apply styles based on different states using [selectors](/docs/state-selectors) and [conditional queries](/docs/conditional-queries).
```html
<div class="scroll-snap-align:center**:hover** scroll-snap-align:center**@sm** scroll-snap-align:center**@dark** scroll-snap-align:center**@print**">…</div>
```

