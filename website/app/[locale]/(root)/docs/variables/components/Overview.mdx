```js name=master.css.js
import { variables } from '@master/css'

export default {
    variables: {
        'font-family': {
            sans: 'Inter'
         },
        'box-size': { md: 1024 },
        spacing: { sm: 10 },
        primary: '#000000'
    }
}
```
Apply custom variables using ambiguous shorthand:
```html
<div class="**font:sans** **max-w:md** **m:sm** **bg:primary**">…</div>
```