import { Metadata } from 'websites-shared/types/Metadata'

const metadata: Metadata = {
    title: 'Font Family',
    description: 'Style syntax for setting the font for an element.',
    category: 'Font'
}

export default metadata