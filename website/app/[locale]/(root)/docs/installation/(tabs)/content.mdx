import brands from 'websites/data/brands'

Installation guides for popular frameworks and build tools integrated with [rendering modes](/docs/rendering-modes) of Master CSS.

## Web frameworks
<IconButtons url="/docs/installation">
    {brands.filter(({ name }) => ['Next.js', 'Svelte', 'Nuxt.js', 'Laravel', 'Astro', 'Remix', 'Blazor', 'React', 'Vue.js', 'Angular'].includes(name))}
</IconButtons>

## Build tools
<IconButtons url="/docs/installation">
    {brands.filter(({ name }) => ['Vite', 'Webpack'].includes(name))}
</IconButtons>

## CDN providers
<IconButtons url="/docs/installation">
    {brands.filter(({ name }) => ['esm.sh'].includes(name))}
</IconButtons>

---

Don't have a preferred framework or tool yet? Try the [online playground](/play) or copy and paste [CDN](/docs/installation/cdn) to quickly start using Master CSS.

(i) Quickly create a blank Master CSS project locally with hot updates using the official CLI.
```bash name=Terminal
npm create @master/css@rc __project__
cd __project__
npm run dev --open
```