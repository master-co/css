import StaticExtractionIntro from '../../../rendering-modes/components/StaticExtractionIntro.mdx'

<StaticExtractionIntro img={{
    href: "https://vite.se.rc.css.master.co",
    ...require('~/public/images/vite-dev.png').default,
    dark: require('~/public/images/vite-dev@dark.png').default,
    alt: 'Vite and Master CSS',
    width: 665, height: 400 }} />

---

## Quick start
<StepSection>
    <Step $row>
        <StepL>
            ### <StepNum />Clone the example
            Copy-paste the following commands to quickly start using the [vite.se.rc.css.master.co](https://vite.se.rc.css.master.co).

            You can skip all [installation](#installation) steps.
        </StepL>
        <StepR>
            ```bash name=Terminal
            npx degit master-co/css/examples/vite-with-static-extraction __my-project__
            cd __my-project__
            npm install @master/css-extractor.vite@rc
            npm install
            npm run dev
            ```
        </StepR>
        <StepEnd/>
    </Step>
</StepSection>

---

## Installation

<StepSection>
    <Step $row>
        <StepL>
            ### <StepNum />Create a project
            If you don't have a Vite project, create one first. It's recommended to refer to [Getting Started - Vite](https://vitejs.dev/guide/)
        </StepL>
        <StepR>
            <CodeTabs>
                {[
                    {
                        name: 'Terminal',
                        lang: 'bash',
                        code: `
                            npm create vite@latest project -- --template vanilla-ts
                            cd project
                            npm install
                        `
                    }
                ]}
            </CodeTabs>
        </StepR>
    </Step>
    <Step $row>
        <StepL>
            ### <StepNum />Install Master CSS Extractor
            Install the Master CSS extractor into your project via package managers.
        </StepL>
        <StepR>
            ```install
            **@master/css-extractor.vite@rc**
            ```
        </StepR>
    </Step>
    <Step $row>
        <StepL>
            ### <StepNum />Initialize configuration file
            Run `npx @master/css init` to create a configuration file [master.css.ts](/docs/configuration).
        </StepL>
        <StepR>
            ```bash name=Terminal
            npx @master/css init
            ```
        </StepR>
    </Step>
    <Step $row>
        <StepL>
            ### <StepNum />Set up CSS extractor
            - Create a `vite.config.ts` file
            - Add a `CSSExtractorPlugin` to the vite `plugins`
        </StepL>
        <StepR>
            ```ts name=vite.config.ts
            +import { defineConfig } from 'vite'
            +import { CSSExtractorPlugin } from '@master/css-extractor.vite'

            +export default defineConfig({
            +    plugins: [
            +        CSSExtractorPlugin()
            +    ]
            +})
            ```
        </StepR>
    </Step>
    <Step $row>
        <StepL>
            ### <StepNum />Import virtual CSS module
            Import the virtual CSS module `.virtual/master.css` into the `main.ts`.
        </StepL>
        <StepR>
            ```ts name=main.ts
            import './style.css'
            +import '.virtual/master.css'
            ```
        </StepR>
    </Step>
    <Step $row>
        <StepL>
            ### <StepNum />Launch server
            Run the Vite development server.
        </StepL>
        <StepR>
            ```bash name=Terminal
            npm run dev -- --open
            ```
        </StepR>
    </Step>
    <Step>
        ### <StepNum />Hello world!
        Now style your first element using Master CSS syntax!
        ```html name=index.html
        <!DOCTYPE html>
        <html lang="en" style="display: none">
            …
            <body>
        +        <h1 class="font:40 font:heavy italic m:12x text:center">Hello World</h1>
                …
            </body>
        </html>
        ```
        <p className="mt:12x">Open your browser to watch the changes.</p>
        <HelloWorld url="localhost:5173" />
    </Step>
</StepSection>