import { Metadata } from 'websites/types/Metadata'

const metadata: Metadata = {
    title: 'Clear',
    description: 'Style syntax for moving an element below floating elements instead of floating to the left or right.',
    category: 'Layout',
    unfinished: true
}

export default metadata