## Overview [sr-only]
<Overview/>

### Align contents to the left
Use `text:left` to align contents to the left.
<Basic className="text:left" />

### Align contents to the right
Use `text:right` to align contents to the right.
<Basic className="text:right" />

### Center contents
Use `text:center` to center contents.
<Basic className="text:center" />

### Align contents to the left and right
Use `text:justify` to align contents to the left and right.
<Basic className="text:justify" />

---

## Conditionally apply
Apply styles based on different states using [selectors](/docs/state-selectors) and [conditional queries](/docs/conditional-queries).
```html
<div class="text:left**:hover** text:left**@sm** text:left**@dark** text:left**@print**">…</div>
```

