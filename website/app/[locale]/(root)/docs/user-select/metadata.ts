import { Metadata } from 'websites/types/Metadata'

const metadata: Metadata = {
    title: 'User Select',
    description: 'Style syntax for controlling whether the user can select text.',
    category: 'Interactivity',
    unfinished: true
}

export default metadata