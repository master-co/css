## Default [sr-only]
<Demo $py={0}>
    <DemoPanel className="flex flex:col gap:30">
        <div className="w:full">
            <DemoLabel>sans</DemoLabel>
            <DemoP className="font:sans">Heavy boxes perform quick waltzes and jigs.</DemoP>
        </div>
        <div className="w:full">
            <DemoLabel>serif</DemoLabel>
            <DemoP className="font:serif">Heavy boxes perform quick waltzes and jigs.</DemoP>
        </div>
        <div className="w:full">
            <DemoLabel>mono</DemoLabel>
            <DemoP className="font:mono">Heavy boxes perform quick waltzes and jigs.</DemoP>
        </div>
    </DemoPanel>
</Demo>
There are three default [font family](/docs/font-family) variables, `sans`, `serif`, and `mono`, to facilitate your use as fallbacks when adding custom fonts.

By default, `font-family` variables are inherited by the <Heirs /> rules.
<details>
    <summary>View the default variables</summary>
    <div>
        <Code lang="js">
            {JSON.stringify(require('../../../../../../packages/css/src/config/variables').default['font-family'], null, 4)}
        </Code>
    </div>
</details>

---

## Basic usage
### Add a font
Add a font token by defining [variables](/docs/variables). For example, add `Inter` as the general font and `Fira Code` as the code font.
```js name=master.css.js
import { variables } from '@master/css'

export default {
    variables: {
        'font-family': {
+            sans: ['Inter', ...variables['font-family'].sans],
+            mono: ['Fira Code', ...variables['font-family'].mono]
        }
    }
}
```

### Apply the added font
Apply the defined font variables using the syntax of the [font family](/docs/font-family). For example, presetting the global fonts in `html <body>`:
```html
<body class="**font:sans** **font:mono_:where(code,pre)** font-feature:'salt'">…</body>
```

---

## Font faces
### Google Fonts
The fastest way to declare custom font faces is through [Google Fonts](https://fonts.google.com/).
```html name=index.html
<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
+    <link href="https://fonts.googleapis.com/css2?family=Fira+Code&family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
</head>
</html>
```

### Self-hosted fonts
You can also self-host the fonts via native CSS, as usual.
```css name=styles/fonts.css
@font-face {
    font-family: 'Inter';
    font-style: normal;
    font-weight: 400;
    font-display: swap;
    src: url("/fonts/Inter-Regular.woff2") format("woff2"),
        url("/fonts/Inter-Regular.woff") format("woff");
}
```

---

## See also
- [Font](/docs/font)
- [Font Family](/docs/font-family)
- [Font Feature Settings](/docs/font-feature-settings)
- [Font Variant Numeric](/docs/font-variant-numeric)