import { Metadata } from 'websites/types/Metadata'

const metadata: Metadata = {
    title: 'Background Origin',
    description: 'Style syntax for setting the background\'s origin.',
    category: 'Background',
    unfinished: true
}

export default metadata