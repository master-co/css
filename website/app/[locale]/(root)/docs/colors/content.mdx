## Default [sr-only]
<Default />

---

## Basic usage
### Add a color
Add color variables using hexadecimal codes and extend the defaults.
<Code lang="js" name="master.css.js">{require('!!raw-loader!../../../../../../packages/server/tests/add-a-color/config.js').default}</Code>
Apply it using color-related syntax like [`fg:`](/docs/color).
<Code lang="html">{require('!!raw-loader!../../../../../../packages/server/tests/add-a-color/template.html').default}</Code>
<details>
    <summary>View the generated CSS</summary>
    <div>
        <Code lang="css" beautify>{require('!!raw-loader!../../../../../../packages/server/tests/add-a-color/generated.css').default}</Code>
    </div>
</details>

### Add color shades
Sets multiple shades for a single color.
```js name=master.css.js
export default {
    variables: {
+        primary: {
+            '': '#1192e8', // primary
+            10: '#e5f6ff', // primary-10
+            20: '#bae6ff', // primary-20
+        }
    }
}
```
Apply it using color-related syntax like [`fill:`](/docs/fill).
```html
<svg class="**fill:primary-20** …">20</svg>
```

### Add an alias
Create an alias for a color to link its value to an existing color. This allows you to implement [design tokens](/docs/design-tokens) and makes managing design updates more efficient.
<Code lang="js" name="master.css.js">{require('!!raw-loader!../../../../../../packages/server/tests/add-an-alias/config.js').default}</Code>
Apply it using color-related syntax.
<Code lang="html">{require('!!raw-loader!../../../../../../packages/server/tests/add-an-alias/template.html').default}</Code>
<details>
    <summary>View the generated CSS</summary>
    <div>
        <Code lang="css" beautify>{require('!!raw-loader!../../../../../../packages/server/tests/add-an-alias/generated.css').default}</Code>
    </div>
</details>

For example, say you have multiple color variables referencing the same color token. If that color needs updating, you would only need to update the source instead of manually updating every instance of the color.

This is similar to [Figma's variables](https://help.figma.com/hc/en-us/articles/15145852043927-Create-and-manage-variables).

### Override default colors
(!) Access the same key as the preset to override default colors, like `blue-5` ~ `blue-95`.
```js name=master.css.js
export default {
    variables: {
+        blue: '#4589ff'
    }
}
```

(o) Use `app-*` to avoid conflicts with defaults and keep `blue-*` colors consistent.

<Code lang="js" name="master.css.js">{require('!!raw-loader!../../../../../../packages/server/tests/override-default-colors/config.js').default}</Code>
Apply custom colors for the current application.
<Code lang="html">{require('!!raw-loader!../../../../../../packages/server/tests/override-default-colors/template.html').default}</Code>
<details>
    <summary>View the generated CSS</summary>
    <div>
        <Code lang="css" beautify>{require('!!raw-loader!../../../../../../packages/server/tests/override-default-colors/generated.css').default}</Code>
    </div>
</details>

---

## Advanced usage
### Add colors by theme
Add color variables based on different themes.
<Code lang="js" name="master.css.js">{require('!!raw-loader!../../../../../../packages/server/tests/add-colors-by-theme/config.js').default}</Code>
Set the theme to `.light` or `.dark` to apply the target theme colors.
<Code lang="html">{require('!!raw-loader!../../../../../../packages/server/tests/add-colors-by-theme/template.html').default}</Code>
<details>
    <summary>View the generated CSS</summary>
    <div>
        <Code lang="css" beautify>{require('!!raw-loader!../../../../../../packages/server/tests/add-colors-by-theme/generated.css').default}</Code>
    </div>
</details>

### Add theme colors with a fallback color

---

## Custom references
### Using text color tokens
| Token             | Description                                                                                                                       |
|-------------------|-----------------------------------------------------------------------------------------------------------------------------------|
| `text-accent`     | Accent color emphasizes active navigations and states, strongly contrasting the background.                                             |
| `text-strong`     | Strong color, used for main titles, subheadings, or slightly emphasized fields, creating a high contrast with the background.     |
| `text-neutral`    | Soft color, a foreground color harmonizing with the background.                                                                   |
| `text-light`      | Light color, a foreground color contrasting slightly with the background.                                                         |
| `text-lighter`    | Lighter color, a foreground color contrasting slightly with the background.                                                       |
| `text-lightest`   | Dim color, a foreground color contrasting slightly with the background.                                                           |

Create foreground color variables for titles, descriptions, icons, and other body text.
```js name=master.css.js
export default {
    variables: {
        text: {
+            primary: '$(blue-60)'
        }
    }
}
```
Apply the defined `text-*` variables using the inherited rules <TextHeirs />.
```html
<h1 class="**fg:primary**">page title</h1>
```

### Using surface color tokens
| Token             | Description                                                                                                                       |
|-------------------|-----------------------------------------------------------------------------------------------------------------------------------|
| `base`            | The background color for apps, modals, ...                                                                                        |

Create color variables for backgrounds in apps, panels, cards, and more.
```js name=master.css.js
export default {
    variables: {
+        base: '$(white)'
    }
}
```
Use [background-color](/docs/background-color) syntax to set the app background color:
```html
<body class="**bg:base**">…</body>
```
