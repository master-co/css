import { Metadata } from 'shared/types/Metadata'

const metadata: Metadata = {
    title: 'Grid Template',
    description: 'Style syntax for setting grid columns, grid rows, and grid areas.',
    category: 'Grid',
    unfinished: true
}

export default metadata