export const metadata = {
    title: 'Master CSS plan query e2e'
}

export default function RootLayout({ children }) {
    return (
        <html lang="en">
            <body>{children}</body>
        </html>
    )
}
