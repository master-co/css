import type { Config } from '@master/css'

export const config = {
    components: {
        named: 'inline-flex'
    }
} as Config

export default {
    components: {
        fallback: 'block'
    }
} as Config
