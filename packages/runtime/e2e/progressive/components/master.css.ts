import { Config } from '@master/css'

export default {
    components: {
        btn: 'bg:foo'
    },
    variables: {
        color: {
            foo: 'oklch(0% 0 none)',
        }
    }
} satisfies Config