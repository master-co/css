import { Config } from '@master/css'

export default {
    modes: {
        light: {
            primary: '#000000'
        },
        dark: {
            primary: '#ffffff'
        }
    }
} satisfies Config