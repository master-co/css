export const OPTIONS_ESM_TEXT = `/** @type {import('@master/css-compiler').Options} */
const options = {
    sources: [],
    classes: {
        fixed: [],
        ignored: []
    }
}

export default options
`