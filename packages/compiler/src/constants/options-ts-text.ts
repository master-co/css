export const OPTIONS_TS_TEXT = `import type { Options } from '@master/css-compiler'
const options: Options = {
    sources: [],
    classes: {
        fixed: [],
        ignored: []
    }
}

export default options
`