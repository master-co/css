export const enum Layer {
    // native shorthand
    NativeShorthand = -300,
    // shorthand
    Shorthand = -200,
    // native
    Native = -100,
    // normal
    Normal = 0
}

export const enum CoreLayer {
    // semantic
    Semantic = -401,
    // native shorthand
    NativeShorthand = -301,
    // shorthand
    Shorthand = -201,
    // native
    Native = -101
}
