const enum Layer {
    // semantic
    Semantic = -4,
    // native shorthand
    NativeShorthand = -3,
    // shorthand
    Shorthand = -2,
    // native
    Native = -1,
    // normal
    Normal = 0,
}

export default Layer