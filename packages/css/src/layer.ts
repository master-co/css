export const enum Layer {
    // semantic
    Semantic = -8,
    // native shorthand
    CoreNativeShorthand = -7,
    // native shorthand
    NativeShorthand = -6,
    // shorthand
    CoreShorthand = -5,
    // shorthand
    Shorthand = -4,
    // native
    CoreNative = -3,
    // native
    Native = -2,
    // normal
    Core = -1,
    // normal
    Normal = 0,
}
