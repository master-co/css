export const enum Layer {
    // semantic
    CoreSemantic = -401,
    Semantic = -400,
    // native shorthand
    CoreNativeShorthand = -301,
    NativeShorthand = -300,
    // shorthand
    CoreShorthand = -201,
    Shorthand = -200,
    // native
    CoreNative = -101,
    Native = -100,
    // normal
    Normal = 0
}
