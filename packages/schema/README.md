# @master/css-schema

Public Master CSS schema and wire-format contracts.

This package owns dependency-light types, serializable constants, and pure codec helpers shared by the compiler, engine, runtime, integrations, and tooling.
