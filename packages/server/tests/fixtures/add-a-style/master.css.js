export default {
    components: {
        btn: [
            { selector: '&', declarations: { display: 'inline-flex' } },
            { selector: '&', declarations: { height: '2.5rem' } }
        ] // [!code highlight]
    }
}
