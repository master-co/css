import MasterCSSRule from '../rule'

export default class extends MasterCSSRule {
    static override id = 'CounterReset'
    static override propName = 'counter-reset'
    static override unit = ''
}