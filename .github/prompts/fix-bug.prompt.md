# Fix Bug

Fix the confirmed bug at its owning layer and add a focused regression test.

Use [the bugfix context pack](../../.ai/context/bugfix.md) for task-specific guidance; repository policy remains in `AGENTS.md`. Do not reread guidance already in context.

## Completion

Complete the fix and affected validation. Report the root cause, intentional behavior changes, regression coverage, and remaining limitations.
