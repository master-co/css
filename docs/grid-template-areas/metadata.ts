import { Metadata } from 'shared/types/Metadata'

const metadata: Metadata = {
    title: 'Grid Template Areas',
    description: 'Style syntax for setting areas in the grid container',
    category: 'Grid',
    unfinished: true
}

export default metadata