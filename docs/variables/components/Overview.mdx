```js name=master.css.js
export default {
    values: {
        '2x': 32
    }
}
```
Use them in HTML:
```html
<div class="w:**2x** mt:**2x**"></div>
```
Use `2x` instead of `2` as the token value to avoid the built-in unit conversion behavior.