import { IconAdjustments, IconDatabaseExport, IconFileExport } from '@tabler/icons-react'
import ImageCallToPreview from 'shared/components/ImageCallToPreview'
import Features, { Feature } from 'shared/components/Features'
import { l } from 'to-line'

Master CSS [Static Extraction](/docs/rendering-modes/static-extraction) integrates build tools to **scan** project source code, **extract** class names, **generate** CSS rules, and write them into a **virtual CSS module**.
<div className="flex flex:wrap@<sm gap:50 mt:50">
    <Features className={l('grid-cols:1', props.img ? 'w:35%@sm' : 'grid-cols:3@sm')}>
        <Feature>
            <IconFileExport className="app-icon-primary" />
            <div>
                **Zero runtime**
                
                Generate virtual CSS modules at build time
            </div>
        </Feature>
        <Feature>
            <IconAdjustments className="app-icon-primary" />
            <div>
                **Semi-dynamic**
                
                Scan all source code to extract class names
            </div>
        </Feature>
        <Feature>
            <IconDatabaseExport className="app-icon-primary" />
            <div>
                **Cross-page caching**
                
                Download the CSS bundle for all pages at once
            </div>
        </Feature>
    </Features>
    {props.img && <div className="flex:1|1|65%@sm">
        <ImageCallToPreview {...props.img} imgClass="outline:1|solid|divider outline-offset:-1 r:6" />
    </div>}
</div>