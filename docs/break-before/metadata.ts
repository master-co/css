import { Metadata } from 'shared/types/Metadata'

const metadata: Metadata = {
    title: 'Break Before',
    description: 'Style syntax for controlling how page, column, or region breaks should occur before the specified element.',
    category: 'Layout',
    unfinished: true
}

export default metadata