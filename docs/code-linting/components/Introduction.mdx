import { IconStatusChange, IconTextSpellcheck, IconArrowMerge, IconSettingsExclamation, IconScriptPlus } from '@tabler/icons-react'
import Features, { Feature } from 'shared/components/Features'

Master CSS establishes an official set of linting rules to unify the coding style of your team, making your template markup more organized and catching errors early.

<Features className="grid-cols:3@sm mt:50">
    <Feature>
        <IconStatusChange className="app-icon-primary" />
        <div>
            **Consistent class order**
            
            Sort by generated CSS rule priority and syntax name case
        </div>
    </Feature>
    <Feature>
        <IconTextSpellcheck className="app-icon" />
        <div>
            **Conflicting class checks 🚧**
            
            Avoid declaring the identical CSS property repeatedly
        </div>
    </Feature>
    <Feature>
        <IconSettingsExclamation className="app-icon" />
        <div>
            **Illegal class checks 🚧**
            
            Check the validity of classes with your configuration
        </div>
    </Feature>
    <Feature>
        <IconArrowMerge className="app-icon" />
        <div>
            **Enforce shorthand 🚧**
            
            Combine multiple classes into a shorthand If possible
        </div>
    </Feature>
    <Feature>
        <IconScriptPlus className="app-icon" />
        <div>
            **No external class 🚧**
            
            Avoid using custom classes from traditional CSS
        </div>
    </Feature>
</Features>