import { Metadata } from 'shared/types/Metadata'

const metadata: Metadata = {
    title: 'Accent Color',
    description: 'Style syntax for setting the accent color of user interface elements or controls.',
    category: 'Appearance',
    unfinished: true
}

export default metadata