import { Metadata } from 'shared/types/Metadata'

const metadata: Metadata = {
    title: 'Color',
    description: 'Style syntax for setting foreground color of an element.',
    category: 'Appearance',
    unfinished: true
}

export default metadata