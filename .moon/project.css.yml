$schema: 'https://moonrepo.dev/schemas/global-project.json'

tasks:
    build-css:
        command:
            - esbuild
            - src/index.css
            - '--bundle'
            - '--outfile=dist/index.css'
            - '--minify'
